# Prompt for Claude Code — Phase 02, F1–F6

Copy the following into the existing coalshift Claude Code session.

```text
Work in /Users/jakubtesarik/Programování/coalshift on redesign. Complete only F1–F6 in docs/phase-02-final-polish.md. Read AGENTS.md, CLAUDE.md, docs/agent-instructions.md, docs/brief.md, docs/plan.md, docs/Phases/02-homepage-and-design.md, docs/content.md, docs/design-reference.md, docs/quality.md and docs/references/phase02-round4/README.md first. Preserve the existing uncommitted phase-02 implementation. Verify HEAD and record the actual starting state; the expected review baseline is 8f1db89f2e01dad80eb6678a5cbd0df0187b797a.

The owner's latest follow-up replaces the phone in the faux browser with TWO numerical cards for EACH of its five topics, using the exact coalios composition. Read /Users/jakubtesarik/Programování/coalios/src/_includes/components/desktop-screen.njk, src/_data/modules.json and the associated CSS. Keep that repository read-only and do not inspect coalios-manual. Whenever unsure about design, proportions or content composition, inspect the actual reference instead of approximating it.

F1: Replace the conditional phone layout with a stable main-content area and two supporting metric cards on every tab. Use the metric copy, values and evidence statuses in content.md. The owner authorized provisional numerical examples; visibly label each illustrative card “Ilustrační údaj” and keep these values separate from confirmed counts in the data model. They must be easy to replace after product-team confirmation and must not appear as verified claims in metadata or other sections. Remove only the phone's browser placement, preserving original assets and the image pipeline. Keep consistent column/text widths and stable desktop frame height/CTA position across selection. Follow the reference's responsive stacking instead of forcing three columns onto smaller screens. Preserve accessible working tabs, changing display-only addresses and existing CTA destinations. Do not hide layout jumps with a fade or crop content.

F1 also includes the latest surface-fidelity feedback: reproduce the exact separate workspace, top strip, address pill, main/metric panels, selected tab and fact-chip surfaces from desktop-screen.njk in BOTH themes. Use the explicit color/border table in F1. The dark main/metric panels are neutral-800 on a neutral-900 workspace; light panels are neutral-100 on opaque neutral-200, with visible 2px neutral borders. Do not flatten them into one background or let generic glow-border styles override these browser-specific layers.

F2: Center “A pro všechny další, kdo plánují směny.” below the audience cards and add a decorative outlined plus with a tasteful blue accent. No enclosing card, pill, border or new CTA.

F3: Give all five pricing cards the existing reference-derived border glow, preserving the permanent rim and Lite featured state. Keep all prices, employee bands, monthly-only offer, VAT, short CTA labels, helper text and the approved interior layout. Use valid list markup and real hover/focus verification.

F4: Add one reusable translucent blue highlighter behind the lower roughly 70–75% of the brand word at the selected headline/prose placements in content.md. No box/pill, changed text metrics or global DOM replacement. Preserve lowercase spelling, readable text and contrast in both themes.

F5: Use a scoped light-theme icon accent, starting with #009AC0, for the bare information-card icons, browser facts and plus. Verify actual contrast on the rendered backgrounds. Preserve exact #00B5E2 brand/CTA colors, dark icons and unrelated text/border tokens. Keep the approved gray card surfaces.

F6: Port the reference header's actual two states from header.njk and nav.js. At the top, show the family strip and floating rounded navigation with source proportions/50%-opaque surface. The source switches at scrollTop >= 100px: family strip moves out of view, the navigation surface spans the viewport, sits flush at top:0, loses the pill radius and uses white/90 or black/90. Restore the floating state when scrolling back. Match source transitions, item gaps/link padding and aligned container content. Desktop action order is secondary trial, unframed accessible theme switch, primary Přihlásit se. Keep keyboard order identical and do not shrink text to fit. Preserve mobile-menu behavior and adjust native/JS anchor offsets to the actual visible header without making the page spacer jump. Verify top/scrolled/return states, direct fragment and legal-page entry, both themes, real mobile focus/scroll, reduced motion and intermediate-width fit.

Apply Q-004/Q-007/Q-008/Q-009/Q-010/Q-011/Q-012/Q-015 and the limited Q-003/Q-006 preservation/navigation checks described in F1–F6. Run typecheck and pages:build, then verify fresh served out with genuine pointer/keyboard input, actual widths, both themes and reduced motion. Record stable panel geometry and screenshots for all five topics, distinct browser surfaces, pricing rest/hover, the audience line, highlighter, icon color and both header states. Use a permitted foreground or isolated local browser if the fixed-size bridge cannot perform these checks; preserve pre-existing browser tabs and servers. Do not mark source/synthetic checks as real interaction PASS.

Preserve the successful E1–E5 work, the six-function overview, contacts, primary login, two-column footer, exact Waulter shells, GTM, healthcare redirect and retained hidden routes. Quanda stays removed; the withdrawn tabs/theme incident is closed. Real Waulter policy delivery remains unverified and outside this polish; do not treat an assumed provider-domain restriction as a verified cause.

Update the current handoff, F checklist and affected evidence without rewriting historical reports or weakening acceptance criteria. Keep the provisional-metric confirmation task explicit. Complete only F1–F6, leave phase 02 in_review and changes uncommitted, then stop and return Correction Report — Phase 02, F1–F6 in English with actual evidence, exact review URL, metric statuses and limitations. Do not start phase 03, run another phase automatically, commit, push, deploy or change Cloudflare/GTM.
```
