# Coalshift — Quality profile

## Context and reference basis

This profile covers the Czech marketing website with four public content routes, retained source for three unavailable legacy routes, their required redirects, the owner-retired healthcare redirect, the static Cloudflare Pages build, the shared design system and the late Calendly integration. It is the canonical definition of quality requirements; phases refer to the IDs below. It does not certify the separate Coalshift application.

Selection follows the shared web-quality skill and workflow version 1.2, dated 2 September 2026. Relevant modules considered: content/SEO, technical SEO, responsive UI, accessibility, media/performance, forms, analytics/consent, security/privacy, maintainability and release verification. The requirements below are standalone; access to the shared skill folder is not needed to interpret them.

Primary sources opened during discovery on 2 September 2026:

- [Next.js static exports](https://nextjs.org/docs/app/guides/static-exports) and [Cloudflare static Next.js deployment](https://developers.cloudflare.com/pages/framework-guides/nextjs/deploy-a-static-nextjs-site/): `out` is the selected static output; the default runtime image optimizer is not supplied by static export.
- [Cloudflare preview branch controls](https://developers.cloudflare.com/pages/configuration/branch-build-controls/) and [preview deployments](https://developers.cloudflare.com/pages/configuration/preview-deployments/): provider behavior to verify against the actual project settings and deployed version.
- [Next.js August 2026 security release](https://nextjs.org/blog/august-2026-security-release): patched releases 15.5.24 and 16.3.3 were published. Select a currently supported compatible release in phase 01; these numbers are dated observations, not instructions to install an older version later. [Earlier RSC advisory](https://nextjs.org/blog/CVE-2025-66478) does not by itself prove exploitability of this static production website.
- [WCAG 2.2 contrast guidance](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html) and [reflow guidance](https://www.w3.org/WAI/WCAG22/Understanding/reflow.html): basis for the selected interface checks. This project does not claim a full WCAG certification.
- [Reduced-motion guidance](https://www.w3.org/WAI/WCAG22/Understanding/animation-from-interactions.html): reduced-motion support is a project requirement. The linked interaction criterion is AAA; it is not presented as a blanket AA requirement.
- [MPSV Labour Code reference](https://ppropo.mpsv.cz/zakon_262_2006), [breaks](https://ppropo.mpsv.cz/VIII4Prestavkavpraciabezpecnostn) and [daily rest](https://ppropo.mpsv.cz/viii81nepretrzitydenniodpocinek): support correction of the PDF's inaccurate section reference, not a legal audit of the product.
- [Calendly embed options](https://calendly.com/help/embed-options-overview): supports inline/link approaches; the owner's event, integration mode and account configuration remain unknown until Phase 05.

## Area selection

| Area | Decision | Rationale and revisit trigger |
| --- | --- | --- |
| Content, copywriting and SEO | selected | Public content and offer are being revised across existing routes |
| Technical SEO and semantics | selected | Shared metadata, headings, indexation and retained URLs need explicit treatment |
| Responsive UI and family design | selected | Core redesign, both themes and mobile CTA |
| Accessibility | selected | Selected navigation, keyboard, focus, contrast, semantics and reflow requirements |
| Performance and media | selected | Confirmed image failures; new effects and portraits must be checked |
| Maintainability | selected | Framework update, duplicate config, shared components and public data |
| Dependency/security/privacy boundaries | selected | Patched supported dependencies, external links/scripts and no exposed secrets; no application penetration test |
| Release and preview verification | selected | Automatic production deployment requires branch separation and exact-version evidence |
| New forms/Basin | not_applicable | This task retains outbound registration and contacts; no new enquiry form is requested |
| Analytics expansion and cookie work | deferred | Owner explicitly manages the functioning banner through GTM; revisit only on a new request. Existing integration is preserved by Q-016 |
| Calendly | deferred | Phase 05 after owner finalizes scope/account/event inputs; Q-017 defines the later check |
| CMS/Decap | not_applicable | No content editor workflow in the target; do not inherit reference-site CMS |
| GEO/llms.txt | not_applicable | Clear public content is covered by SEO; no separate AI discovery artifact or visibility promise is requested |

## Agreed requirements

| ID | Observable requirement and scope | Responsible phase | Verification and conditions |
| --- | --- | --- | --- |
| Q-001 | Preview work uses `redesign`; production remains the verified Cloudflare production branch, recorded as `cloudflare-deploy`. Phase 04 integrates the reviewed release into GitHub default `master` and production, with exact-version preview and production evidence | 01; release 04 | Record actual branch/HEAD/status, current remote ancestry and live Pages branch/build settings. Verify successful preview deployment, source SHA, URL and search exclusion before production; then verify the production deployment SHA and origin. A local build or push to master alone is not proof of release |
| Q-002 | One reproducible static build uses supported compatible dependencies and `npm run pages:build` produces `out`. Preview/configuration agree on that directory and do not require an absent server optimizer | 01; recheck 04 | Record exact Node/npm/framework versions and lockfile state. Run a clean install under the authorized implementation assignment, build, suitable TypeScript check, and serve exported output. Check official support/security guidance at version selection. Do not ignore failures or run a publishing command as a test |
| Q-003 | All active in-scope raster assets render on the static site with correct proportions and correct contact identities; no image uses a failing default `/_next/image` endpoint. Phase 04 may remove unused phone/old-portrait assets and registry entries after whole-repository usage checks; preserve active originals and the repaired image pipeline | 01 original images; 02 new assets; cleanup/regression 04 | Inspect source/registry/metadata/manifests/CSS references before deletion. Check browser requests, status/content type, actual file/rendered dimensions, variants and console at narrow/wide sizes and DPR 1/2 on fresh export and the authorized preview. Remove stale generated outputs before the exact hosting build |
| Q-004 | Registration CTAs and login links navigate to their existing app destinations. The Phase 02 header has no registration CTA: desktop exposes theme then login; compact closed mobile exposes logo, theme and menu only; activating any open-mobile-menu destination closes it and navigates. Link and action semantics remain correct for every active affected CTA/navigation caller | 01 repair; 02–04 regression | Click and keyboard-activate desktop/mobile CTAs and menu destinations. Inspect rendered anchor/button semantics, targets and rel attributes; test active links with onClick and action controls, including in-page pricing navigation; deletion of an unused legacy Button does not require keeping a test-only caller. No nested interactive elements or hidden focusable menu links |
| Q-005 | Code, assets, configuration and documentation have a current purpose. Phase 04 removes verified dead code/config/dependencies/assets and obsolete narratives, consolidates factual documentation and preserves intentional legacy source, licenses and accepted behavior | 01 foundation; 04 cleanup | Trace all callers and data-driven/build-time references before deletion; inspect dependency/config ownership. Keep concise non-obvious comments, current architecture/design/content-SEO/operations documents and a compact quality/handoff record. Verify surviving links, clean build/types, exported assets and affected UI; report removals and measured repository/output differences |
| Q-006 | The four public content routes in brief.md load directly and after refresh. In phase 02 retire `/zdravotnici` and `/zdravotnici/` with 301 to `/#industries`; keep healthcare in the audience overview. Phase 03 exposes `/reference` through footer **Navigace**. Source for `/registrace`, `/wait-list` and `/wait-list/thank-you` stays in the repository, while both slash forms of each URL return 301 to `/` and remain absent from the active internal link graph | 01 original route regression; 02 retirement; 03–04 affected routes | On fresh exported output served by Wrangler and an authorized current preview, check four content routes, the two healthcare redirects, all six legacy redirects and the final audience anchor. Verify the footer reference link and absence of every active internal `href` to the three dead legacy route families or retired healthcare URL. Ensure `_redirects` reaches `out`; assert exact status and `Location` without following redirects |
| Q-007 | Reference fidelity follows design-reference.md, F1–F6 and G1–G4/R1–R6 in coalsoft blue: preserve E1–E5; use stable browser widths and compact active-panel natural height with two numerical cards per topic, centered audience-plus line, pricing-card glow, restrained brand-word highlighting and a scoped brighter light icon accent; exact browser surfaces, centre-out link motion, responsive vertical browser navigation, source mobile menu and floating/pinned header hierarchy/spacing | 02 foundation/correction; 03 remaining pages; 04 regression | Compare actual source plus supplied reference screenshots against implementation layout and rest/hover/focus states in both themes. Record reference revision and actual viewport; compare before/after page composition and height under equal conditions. Source resemblance alone is not visual acceptance |
| Q-008 | Both themes are usable; first visit defaults to dark, explicit choice persists between routes and reloads, and toggling does not cause hydration errors or an avoidable opposite-theme flash | 02; recheck 03–04 | Test fresh storage, light selection, navigation, reload and return to dark. Inspect both logos/text/backgrounds and console. A blocked storage API must not break rendering. Give theme control an accessible name and state |
| Q-009 | Content, pricing, contacts and navigation remain usable at 320, 390, 768 and 1440 CSS px in both themes, without page-level horizontal overflow or covered content. The browser exposes its complete vertical tab list without horizontal swipe at every width. Sticky header and anchor offsets agree | 02; recheck 03–04 | Record viewport screenshots and interactions, plus 1024/1280 breakpoint checks. Check 200% text zoom and equivalent 320 CSS px reflow, open mobile menu, complete browser labels, long Czech labels and footer links; pricing may stack as labeled cards |
| Q-010 | Affected UI has logical headings/landmarks, unique IDs, meaningful labels, visible focus, keyboard operation and appropriate contrast. Browser tabs expose selection/panel relationships with one always-vertical roving-focus model; hidden content has no focus targets. Effects remain decorative and respect reduced motion | 02; recheck 03–04 | Inspect DOM and real keyboard order; test menu, vertical tabs (Up/Down, Home/End, Enter/Space), FAQ, theme and CTAs. One accessible CTA label despite decorative animation clones. Normal text ≥4.5:1, large text ≥3:1, required control/state boundaries ≥3:1; test changed color pairs, zoom, reduced motion and coarse pointers. Automated checks supplement manual evidence |
| Q-011 | Public copy follows content.md, consistent vykání, lowercase family brand spelling (including metadata/alt/labels) and the 14-day trial. No invented product/payment terms, incorrect §79 break/rest reference, blanket legal guarantee or false historical submission claim. Keep the six illustrative numbers visibly labelled and separate from approved counts. The owner authorized release of the reviewed current content in Phase 04; that does not establish measured accuracy or independent testimonial provenance | 02 homepage and spelling-only retained-page corrections; 03 remaining copy/SEO; recheck 04 | Search source and rendered public copy/metadata/labels for old trial lengths, stale claims/contacts and uppercase brands. Exclude internal identifiers and historical evidence from public-copy checks; preserve attributed quote substance and people’s names |
| Q-012 | All five pricing cards show only the canonical monthly offer, explicitly excluding VAT and differing only in employee count | 02; affected regression 04 | Compare monthly amounts and employee boundaries to content.md. No annual switch/state/data/amounts/11-month note/inquiry, per-tier feature checklist or per-employee surcharge in active output. Verify monthly unit and mobile/keyboard CTA behavior. Paid button says Vyzkoušet with Prvních 14 dní zdarma. below; Free says Začít zdarma without that helper. Full labels and arrows remain visible at rest/hover |
| Q-013 | Contact section displays the two approved people, roles, matching portraits and working phone/email links; obsolete Miroslav contact details disappear from active website output | 02; affected regression 04 | Compare against content.md; inspect tel/mailto targets without sending messages. Check image/person association and rendered crops at narrow/wide widths. Supplied source portraits must be integrated before full contact acceptance |
| Q-014 | Public routes have meaningful Czech metadata and canonical handling; homepage uses the required title. `/`, `/reference`, `/gdpr` and `/cookies` are indexable production self-canonicals and the only sitemap entries. The six slash/no-slash legacy URL forms return 301 to `/`; retained route-body metadata is defense-in-depth/source history, not the public indexing mechanism. Preview must never be canonicalized as production | 02 basic metadata for /gdpr and /cookies; 03 full SEO + dead-route redirects; recheck 04 | Inspect generated HTML for the four public routes: title/description, canonical and social metadata. Verify exactly the four approved absolute sitemap URLs and an allow policy that exposes the sitemap. From Wrangler-served fresh output and later an authorized preview, verify every legacy URL form returns 301 with `Location: /` without a loop. Confirm no preview hostname appears in canonical/sitemap output |
| Q-015 | Media/effects have appropriate responsive delivery and loading; fonts and decorative JS do not create avoidable layout shifts or obstruct interaction | 02; recheck 03–04 | Record transfer sizes and requested variants at mobile/desktop widths, image dimensions/sizes and priority choices. Compare the working phase 01 baseline against the redesign under the same browser/cache/network conditions; investigate concrete regressions. Below-fold portraits/calendar are not eagerly prioritized. Test disabled/reduced animation and third-party loading that succeeds, fails or remains pending; first-party controls must remain usable independently of third-party loading. Quanda is retired; do not restore it. Verify stable F1 panel width and wide-desktop frame geometry without claiming a CWV result. No unmeasured Lighthouse/CWV claim |
| Q-016 | Owner-managed GTM `GTM-NQDZKVLF` and Waulter remain; Quanda is removed as explicitly requested. /gdpr and /cookies provide the exact initially empty provider containers and readable injected content without duplicate loaders or React overwrites | 01–04 affected changes; legal shells/removal 02 | Verify Quanda absence in active code/output/requests; preserve GTM/consent configuration. Check exact waulterGdpr/waulterCookies IDs, direct/refresh/footer entry and real provider population separately from fixture styling. Domain/runtime restrictions remain explicit; no unrelated cookie audit |
| Q-017 | Calendly uses the owner-approved real event, integration mode and placements; an embed has a direct booking-link fallback. Exact scope is finalized before Phase 05 planning review | 05 | Inspect event/organizer identity and approved placements. Test keyboard, responsive height/scrolling, themes, loading failure and navigation. A real booking/email notification test requires explicit owner authorization; report it separately from link/embed rendering |
| Q-018 | Concise handoff identifies the exact reviewed/released version, actual checks, factual limits, remaining owner inputs and a practical recovery target. Phase 04 executes the expressly authorized production release after cleanup review; later phases use their own publication authorization | 04 release; 05 affected handoff | Record branches/commits, preview and production deployment IDs/URLs, actual operations, selected requirement evidence and rollback procedure. Distinguish local, preview and production results. Keep latest meaningful evidence and unresolved items when deleting historical reports; never turn NOT_RUN into PASS |

## Phase 02 acceptance — 3 September 2026

After Codex's source/diff review of I1–I8, Jakub explicitly accepted Phase 02. The authoritative phase decision and accepted version are in [plan.md](plan.md). The corrected local behavior is supported by Claude's reported fresh build/Chrome 152 matrix and Codex's independent passing typecheck and diff check. Codex's build repeat was blocked by the review environment's local-port restriction. Existing physical-device, screen-reader, Back-position, provider, preview and later-phase evidence remains `NOT_RUN` or limited as recorded; owner acceptance does not claim those checks passed or authorize production release.

## Complete Phase 02 recheck assessment — 3 September 2026 (historical)

The complete `/recheck` covered the entire baseline-to-working-tree Phase 02 scope, ran fresh type/build/export and broad served-browser checks, and found no critical/data/security issue or plan omission. Codex validated the accepted findings against current source. Phase 02 remains `in_review` because Q-009/Q-010 still have reproduced failures: stacked native fragment clearance, reduced-motion-bypassing JavaScript scroll, missing fragment history/focus behavior, background focus behind the mobile overlay, unreliable skip-target focus, insufficient illustrative-label text contrast, insufficient selected-tab visual state and insufficient resting non-parent family-icon contrast. These are assigned as [I1–I8](phase-02-recheck-corrections.md); the theme-toggle semantic/first-paint observation is included in the same bounded correction.

The reported secondary CTA outline is not treated as a Q-010 failure: its high-contrast text and arrow identify the control and it has a distinct focus ring. W3C's [SC 1.4.11 boundary guidance](https://www.w3.org/WAI/WCAG22/understanding/non-text-contrast.html#boundaries) states that a contrasting hit-area boundary is not required when visible content identifies the control. Other minor observations remain optional or later-phase evidence; they do not expand this correction. Existing remote/device/provider/performance limitations retain their recorded status.

**I1–I8 correction implemented and locally verified — 3 September 2026.** All eight items were applied on `redesign` over the uncommitted recheck working tree (HEAD still `68fdbcb`) and verified against a fresh `npm run pages:build` export in isolated headless Chrome 152 plus `npm run typecheck` (exit 0) and `npm run pages:build` (exit 0). Q-009 and Q-010 now record local PASS for the corrected behaviour (single fragment offset, reduced-motion-aware and history/focus-correct JS scroll, contained modal-menu focus with `inert` background, focusable `#main` skip targets, ≥4.5:1 illustrative-label text and ≥3:1 selected-tab / non-parent family-icon cues); Q-008 records I8's stable name + `aria-pressed` model and bootstrap-driven first-paint icon. Physical-device touch/keyboard, a screen-reader pass over the new dialog/inert semantics, and browser-native Back scroll restoration remain NOT_RUN and owner-assisted. Codex evaluates the correction report and diff; the owner accepts the phase. Phase 02 stays `in_review`.

## Owner acceptance and complete-recheck handoff — 3 September 2026 (historical)

After receiving the H1 and tooltip reports, Jakub confirmed that he reviewed the result and that its appearance and behavior work correctly. This is owner visual/functional acceptance for Q-007 and the observed Q-009/Q-010 interactions, followed by authorization for the full Phase 02 `/recheck`. It does not assert a named physical device, viewport matrix, provider-population result or measured performance score. Formal phase acceptance remains pending that review; status is maintained only in plan.md.

Codex checked the current H1/tooltip source, both reports, Claude's saved first-tab captures at 1728/light and 1440/dark, and `git diff --check`. The compact browser composition is restored in those captures; the tooltip layering/offset change is present in source. Claude separately reports passing typecheck/static build and the H1/tooltip browser matrices; Codex did not independently rerun those commands or matrices in this handoff. The tooltip matrix covers all five brands at 1440/1728 in both themes on hover/focus, with no clipping or overlap with the theme/login controls. The current review baseline and complete accumulated scope are in plan.md.

Historical NOT_RUN entries for owner visual acceptance are superseded by this acceptance; specifically unperformed device/provider/performance checks retain their actual status. Historical requirements for a phone placement, horizontal tabs, a header trial CTA or forced identical panel heights are superseded by the completed owner corrections, not defects to restore.

## G1–G4 / R1–R6 assignment preparation (historical, 3 September 2026)

[Assignment](phase-02-responsive-navigation-corrections.md) and [Claude prompt](phase-02-responsive-navigation-corrections-prompt.md). This is Codex source/screenshot review and assignment preparation, not implementation or runtime PASS. It supersedes affected F6/header and mobile-browser expectations while preserving completed F1–F5 behavior.

| Scope | Current evidence | Required next evidence |
| --- | --- | --- |
| G1 / Q-007, Q-010, Q-015 | Target header source declares `origin-left`; reference pseudo-element has no non-centre transform origin. Footer uses instant `hover:underline` while reference `.link` scales for 500ms | Genuine fine-pointer and keyboard-focus capture in both themes; reduced-motion state; no instant footer blink |
| G2 / Q-007, Q-009, Q-011 | Footer visible heading is `Web`; long mobile legal/address wrapping was not run before publication | **Navigace** in source/rendered output; 320/390 reflow and destination/semantics regression |
| G3–G4 / Q-004, Q-006, Q-007, Q-008, Q-009, Q-010, Q-015 | Published header retains trial actions. Owner now requires no header trial, desktop theme→login and compact logo+theme+hamburger. Previous mobile/zoom checks are NOT_RUN | Top/scrolled/return states, exact action hierarchy, source gaps, mobile closed/open menu, focus/scroll lock, direct fragment clearance and 200% zoom |
| R1 / Q-004, Q-008, Q-009, Q-010 | Current mobile overlay differs from source composition; real mobile input/focus was NOT_RUN | Full-screen top-origin overlay in both themes; real pointer/keyboard, Escape/focus return, internal scroll and no hidden focus targets |
| R2 / Q-007, Q-009, Q-010, Q-015 | `FunctionsBrowser.tsx` uses `overflow-x-auto` horizontal tabs below `lg`; reference `desktop-screen.njk` uses `flex-col` and `aria-orientation=vertical` at all widths | All five labels visible vertically at 320/390/768/1440; real vertical keyboard model; natural mobile panel height; no internal/page horizontal scroll; 1728 F1 geometry regression |
| R3–R5 / Q-003, Q-007–Q-015 | Publication report explicitly records responsive grids/browser stacking/portraits/200% zoom as NOT_RUN | Complete homepage and legal-shell visual/overflow matrix, both themes, portraits, pricing/contact/footer, zoom and reduced motion |
| R6 / Q-007, Q-011 | Owner supplies screenshots as visual reference and identifies Coalios source as the reusable code authority | Report named source rules actually ported and confirm no Coalios-only copy/routes/identity/dependency |

The current correction baseline is committed `68fdbcbd2562f73db91a683315d259c4fca4ef04`. The complete Phase 02 recheck baseline remains `8f1db89f2e01dad80eb6678a5cbd0df0187b797a`. The new correction is local-only and must remain uncommitted for review.

## Previous correction review — 3 September 2026 (F1–F6)

[Assignment](phase-02-final-polish.md), [owner inputs and E report](references/phase02-round4/README.md). This is Codex source/screenshot review and planning, not a new implementation PASS or runtime test. E report's typecheck/build and browser outcomes are attributed to Claude. Historical tables below remain version-specific evidence.

| Scope | Current evidence | Required next evidence |
| --- | --- | --- |
| F1 / Q-007, Q-009, Q-010, Q-015 | Conditional first-tab phone causes unequal main-content widths; owner now chooses two metric cards per topic instead. Current main/workspace surfaces also differ from the explicit reference light/dark layer hierarchy | Same text/card widths, stable desktop frame/CTA geometry, five matching metric pairs and addresses, real keyboard/pointer, responsive stacking |
| F6 / Q-004, Q-006, Q-007, Q-008, Q-009, Q-010, Q-015 | Header source has no reference scroll state, keeps its pill/family strip and places theme after login. Reference nav.js uses scrollTop >= 100px with distinct header.njk surface/order rules | Both header states and return, actual visible-height anchor clearance, theme/keyboard order, source spacing and opacity, mobile menu/reflow |
| F2 / Q-007, Q-009, Q-010 | Current audience closing sentence is a left-aligned plain paragraph | Centered open line + decorative plus; mobile wrapping and reduced-motion state |
| F3 / Q-007, Q-010, Q-012 | Pricing lacks the shared glow system; offer itself remains approved | Genuine border hover on ordinary/featured cards, preserved resting emphasis, valid list/CTA semantics and unchanged pricing |
| F4 / Q-007, Q-008, Q-010, Q-011 | New owner-requested decoration; not implemented | Subtle partial-height marker at named placements; unchanged text metrics and accessible copy; actual composited text contrast |
| F5 / Q-007, Q-008, Q-010 | Current #0091B8 icons; candidate #009AC0 calculates 3.018:1 on #f5f5f5, 3.152:1 on #fafafa, 3.290:1 on white, opaque sRGB | Actual color/background/opacity and rendering in both themes; icon-only scope, exact brand/CTA color retained |
| F1 content / Q-011, Q-018 | Four approved product counts and six explicitly illustrative values, per content.md | Visible Ilustrační údaj labels during design; product-team confirmation/replacement before final publication; no illustrative data in SEO/structured data |
| F1 media / Q-003 | Owner supersedes the required browser phone placement | Preserve original file/pipeline and remaining portrait rendering; absence of the removed placement is not a regression |

The current correction is not a new blanket audit. Existing responsive/input checks remain outstanding where unperformed. Real Waulter policy delivery also remains unverified: the E report observes a loaded SDK but empty local containers. Provider-domain gating is only a hypothesis, and the presence of suppressHydrationWarning is not evidence that provider content survives later updates. No new provider investigation is assigned under F1–F6.

## Verification evidence

**Current assessment — third phase-02 review, 2 September 2026:** [E1–E5](phase-02-interaction-content-corrections.md) is active. The owner reports tabs/theme still inert and authorizes Quanda removal plus new copy/design/legal-page details. Codex independently clicked all five panels and switched the theme successfully on the existing export with real external requests, with Quanda pending, and on localhost:3000. The owner subsequently withdrew the incident, explaining confusion about the dev/IP-served version. This closes the owner report without asserting an independently proven cache/IP cause. The owner also requires neutral-100 light card surfaces and an always-visible 2px gradient rim for resting depth. The current border effect changes pixels but remains a thin 1px treatment; orientation-specific arrow behavior is still wrong in source. [Latest evidence](references/phase02-round3/README.md) preserves method, limits and screenshots. No new build or app change by Codex. New E requirements remain unimplemented.

**Owner-approved acceptance exception, 2 September 2026:** after inspecting preview `6011b67f`, the owner requested moving to phase 02 and handling the remaining detailed checks later. Phase 01 is accepted as the technical foundation under that decision; the missing Q-003 viewport/DPR and Q-004 real mobile/keyboard/modified-link/menu-focus/completed-scroll checks remain NOT_RUN. They transfer to verification of the redesigned UI in phase 02, with unresolved items accounted for in the phase 04 release handoff. The general owner preview inspection is not recorded as completion of the detailed matrix. This supersedes the former requirement to finish those checks before phase 02 starts; the behavioral criteria remain unchanged.

Codex independently checked the published version on 2 September 2026: remote redesign is `8f1db89f2e01dad80eb6678a5cbd0df0187b797a`, production remains `0138dbb2073c31e72a9a61c987ee02a8ae9ce4dd`; all six exact-preview routes return 200 text/html and noindex, all 14 distinct img src URLs return 200 image content types, and no route HTML references /_next/image. GTM/Quanda markup and the healthcare video ID are preserved. The branch alias returns 200/noindex. Cloudflare authenticated build-log, screenshot and runtime-network evidence below is attributed to Claude; Codex did not independently repeat those browser checks.

Historical baseline: at initial document generation no implementation had been verified. Discovery failures are documented in brief.md and phase 01; they do not make an unimplemented requirement pass. Update each row with exact evidence as work proceeds. If one requirement spans several phases/environments, retain the separate results rather than overwriting an earlier limitation with an unrelated pass.

Environment for the phase 01 rows below: Node 24.20.0, npm 11.19.0, Next.js 16.3.4 (Turbopack), Chrome via Claude-in-Chrome at 1728 CSS px / DPR 2, static export served from `wrangler pages dev out`. Branch `redesign`, uncommitted on baseline `0138dbb2073c31e72a9a61c987ee02a8ae9ce4dd`.

Codex reviewed both submissions on 2 September 2026. After the R1/R2 corrections, a fresh Codex `pages:build`, typecheck and focused local HTTP/decoded-image checks passed. After R4/R5, Codex independently reran the build, typecheck and `npm audit --json` (all exit 0; zero vulnerabilities), and executed the actual exported guard after TypeScript transpilation: 12 activation cases plus missing-destination and absent-document checks passed. Clean-install evidence remains attributed to Claude. Browser initialization remained blocked; Claude's fixed 1728 CSS px / DPR 2 synthetic-event and CSS-forced menu checks are separate evidence. The remaining mobile, viewport/DPR, pointer/keyboard and completed-scroll checks are NOT_RUN. Passing source/build/HTTP checks do not establish a complete local PASS for Q-003 or Q-004. Requirements are unchanged.

| Requirement | Result | Evidence, tested version/environment | Next action |
| --- | --- | --- | --- |
| Q-001 | PASS (local + preview); NOT_RUN (production) | Phase 01 local: branch `redesign`, `origin/cloudflare-deploy` and `master` untouched. Preview (2 Sep 2026): commit `8f1db89` pushed to `origin/redesign` triggered Cloudflare Preview deployment `6011b67f-f080-4428-9a64-87d0e0ad8ae6` = `https://6011b67f.coalshift.pages.dev` (alias `redesign.coalshift.pages.dev`), build **success**. All six routes return `x-robots-tag: noindex` (curl). Production branch/URL unchanged; no merge or deploy script run | Production release is a separate later instruction; recheck 04 |
| Q-002 | PASS (local + preview); NOT_RUN (production) | Local: Codex passed `pages:build` + `typecheck` on Node 24.20.0; R5 `npm audit fix` (no `--force`) → 10 transitive dev nodes updated within ranges, `npm ci`/`pages:build`/`typecheck` exit 0, `npm audit` 0 vulnerabilities. Preview build log (deployment `6011b67f`): `wrangler.toml` `pages_build_output_dir: out`; Node **24.20.0** installed from `.nvmrc` (npm 10.9.2); `npm clean-install` "added 174 packages … found 0 vulnerabilities"; `npm run pages:build` → `images:generate` (7 WebP derivatives, exact dimensions) → Next 16.3.4 Turbopack, "Compiled successfully", "Finished TypeScript", 9/9 static pages, static export; deploy success. `glob@10.5.0` deprecation notice printed (benign, audit still 0) | Recheck 04 |
| Q-003 | PASS (metadata/HTTP local + preview); NOT_RUN (real narrow/wide + DPR rendering) | Local R1 evidence retained. Preview (`6011b67f`): homepage `<img>` for phone = `src=/img/derivatives/mocup-coalshift-426.webp`, `srcSet` `213w,320w,426w`, layout `sizes`, `loading="eager"` `fetchPriority="high"` `width/height` 426×519; portrait = `adamec-640.webp` with `srcSet` `240w,320w,480w,640w`. All 7 derivative URLs return HTTP 200 `image/webp` with byte sizes matching the build log. **Zero** `/_next/image` references across all six routes' HTML. WebP naturals decode correctly (`mocup` 426×519; portrait density-corrected 299 per MDN). SVG assets return 200 `image/svg+xml` and render post-hydration. Phone mockup renders with correct proportions (screenshot). **Phase 02 (local `wrangler pages dev out`):** the redesigned homepage still has 0 `/_next/image` on every route; phone mockup `srcSet` `213w/320w/426w` + `width/height` 426×519 + `loading="lazy"` (now below fold); new contact portraits `martina-adamcova` / `sarka-melisova` `srcSet` `240w/320w/480w/640w`, `loading="lazy"`, all derivative URLs HTTP 200 `image/webp` — the correct two people render in a consistent crop. **Corrections (2 Sep 2026):** unchanged — phone mockup now sits in the practical-use browser's first panel (`<ResponsiveImage>`, `srcSet` 213/320/426, `width/height` 426×519), portraits unchanged; still 0 `/_next/image` on all five routes | Owner-deferred: verify real viewports/DPR with the phase 02 phone and portrait layouts; account for any remaining case in 04 |
| Q-004 | PASS (guard logic + semantics local + preview); NOT_RUN (real mobile pointer/keyboard, completed smooth scroll) | Preview (`6011b67f`) synthetic-event guard matrix on the live `<a>` elements: plain fragment click → `defaultPrevented` true; `metaKey`/`ctrlKey`/middle-button → not prevented; Header home-logo plain → prevented, `metaKey` → not prevented; external register CTA (`target="_blank" rel="noopener"`) plain → not prevented. `document.querySelectorAll('a a, a button, button a')` = **0**. Desktop login `target` null (same-tab); mobile login `target="_blank"`. A plain fragment activation calls `window.scrollTo({ top: 2276, behavior: "smooth" })` where 2276 = element document-top − 72 (header height = `scroll-padding-top` = 72). The visual smooth animation does **not** complete in the browser-automation environment: `window.scrollTo({behavior:"smooth"})` is inert there while `{behavior:"auto"}` works — an observed environment limitation that does not prove the absence of a code defect. Real mobile-viewport pointer/keyboard and completed animation NOT_RUN. **Phase 02 (redesigned shared nav, local):** `a a, a button, button a` = 0 on the homepage; register CTA `target="_blank" rel="noopener"` (plain click not intercepted); desktop "Přihlásit se" same-tab, mobile login `_blank`; fragment nav guard re-verified on the new header — plain "Nejčastější dotazy" click → `defaultPrevented` + `scrollTo({top: elementTop − 108, behavior:"smooth"})` (offset = measured header height = `--header-height`), `metaKey`/middle click not intercepted; mobile menu markup is `inert` when closed with `aria-expanded`/`aria-controls` + Escape + focus-return. Completed animation + real mobile pointer/keyboard still NOT_RUN. **Corrections (2 Sep 2026):** "O aplikaci" nav now targets `#features`; the "Zdravotníci" link is gone (page retired); rebuilt `CtaButton` keeps the link/action split, `rel="noopener"` for `_blank`, one `aria-label`, and a `:focus-visible` outline; browser-tab keyboard model verified (roving focus, manual activation). Live `#zdravotnici` → `/#industries` 301 confirmed on Wrangler. **F6 (3 Sep 2026):** login/register targets unchanged through the header rework (login same-tab, register `_blank rel="noopener"`); `smoothScroll` now reads `[data-nav-bar]` height (82 px) + 8 px gap — a plain "Ceník" click lands `#pricing` at 90 px vs nav-bottom 82; modifier/middle clicks still fall through; `/gdpr` direct load + scroll behave the same. Completed smooth-scroll animation and real mobile pointer/keyboard still NOT_RUN. **G3/R1 (3 Sep 2026, isolated headless Chrome 140, real viewports):** the trial CTA is gone from every header branch; a latent `.cta`-display bug that kept the login CTA visible at all widths was fixed (`<span class="hidden xl:inline-flex">` wrapper) — verified login hidden at 390/768/1024 and visible at 1280/1440, hamburger the inverse. Mobile menu (390): real pointer open, `<header>` `inert`, body scroll lock, focus into the menu, **Escape closes + returns focus to the hamburger**, `aria-expanded` flips, activating "Ceník" closes the menu and smooth-scrolls with the heading visible. Login destination + same-tab semantics unchanged. Completed-animation and physical-touch still NOT_RUN. **Phase 03 (3 Sep 2026, isolated headless Chrome 152):** the redesigned `/reference` and the three rewritten dead routes route their outbound links through the accepted primitives — `CtaButton` (link/action split, `target="_blank"` only where passed, `rel="noopener"` auto-added for `_blank`) for the register/app CTAs, `next/link` for `/#contact` and `/` , plain `<a>` for `LOGIN_URL`. Register/app CTAs → `https://app.coalshift.cz/register` in a new tab; `Přihlásit se` (secondary on `/registrace`) → `https://app.coalshift.cz/login`; no nested interactive elements introduced. Cross-route `/#contact` from `/wait-list` resolves and scrolls (see Q-009). | Owner-deferred: physical-device touch + completed smooth-scroll on the published preview |
| Q-005 | PASS | Phase 01: active Tailwind/PostCSS pair confirmed by reversible probe, then `tailwind.config.ts` + `postcss.config.mjs` deleted; theme + autoprefixer preserved. Broken `Inter-Italic.ttf` `@font-face`, dead `'CustomFont'` rule and unused Geist loading removed; no rendered typographic change (screenshots). Build + typecheck pass; no route/offer change. R5 (2 Sep 2026): transitive dev-dependency advisories cleared within existing ranges; one intentional implementation of each primitive is unaffected | Complete |
| Q-006 | PASS seven routes + both redirects + Phase 03 link graph (local export); real Waulter population NOT_RUN | **E5 (3 Sep 2026):** `npm run pages:build` emits `/`, `/reference`, `/registrace`, `/wait-list`, `/wait-list/thank-you`, `/gdpr`, `/cookies`. On `wrangler pages dev out`: all seven return 200 on direct load; `/zdravotnici` and `/zdravotnici/` → 301 → `/#industries`. `out/gdpr.html` / `out/cookies.html` each contain exactly one `<div id="waulterGdpr">` / `<div id="waulterCookies">`, empty. Footer links to the legal routes are plain `<a href>` (full-document nav); no primary-nav link and no link to retained legacy routes. **Phase 03 (3 Sep 2026, isolated headless Chrome 152, fresh `out/`):** build emits all 7 content routes + `/sitemap.xml` + `/robots.txt`. Direct load **and browser reload** confirmed on `/registrace`, `/wait-list` and the two-level `/wait-list/thank-you` (H1 identical after reload, `overflowX` 0, no `pageerror`). `_redirects` in `out/` unchanged; `/zdravotnici` still 301 → `/#industries` (fragment applied client-side, final path `/#industries`). Footer now carries exactly one added item — `Reference` → `/reference` (`next/link`, `.link` class) — verified present and in order on `/`, `/reference`, `/gdpr`. Source + built-HTML grep: **zero** `href` to `/registrace`, `/wait-list`, `/wait-list/thank-you` or `/zdravotnici` in any route. **P03-C3 (3 Sep 2026, `wrangler pages dev out` 4.128.0 — "Parsed 8 valid redirect rules"):** `public/_redirects` gains the six legacy rules (healthcare rules byte-preserved); `out/_redirects` byte-identical to the required 8 lines. Without following redirects: `/registrace`, `/registrace/`, `/wait-list`, `/wait-list/`, `/wait-list/thank-you`, `/wait-list/thank-you/` all → **301** `Location: <origin>/` (the static redirect wins over the still-generated `.html` assets); follow-through = 1 hop, final `/`, 200, **no loop**. `/zdravotnici` + `/zdravotnici/` → 301 `/#industries` (unchanged). `/` + the four public routes + `/sitemap.xml` + `/robots.txt` → 200. Sitemap still the four public URLs; no internal link to any legacy family. Production `Location` header + no-loop on the deployed domain = deployment-only BLOCKED. | Real Waulter population + production redirect headers remain published-preview checks |
| Q-007 | **PASS (H1 local reference proportions; owner visual acceptance on 3 Sep 2026)** | **H1 (3 Sep 2026, isolated headless Chrome 140, fresh `out/`).** The grid-stack + `lg:invisible` + `mt-auto` strategy is removed: `FunctionsBrowser` panel wrapper is `flex-1` (no `lg:grid`); inactive panels are the native `hidden` attribute + `hidden` class (`display:none` at every width — computed `display: none` on all 4 inactive panels at 1024/1280/1440/1536/1728; `inactiveInLayout` false; not focusable); main card `flex flex-col gap-10 lg:gap-12` (reference `~gap-10/12`); CTA follows the content group in normal flow (`self-start`, `mt-auto` gone). Measured **facts-to-CTA gap = 40 px (<lg) / 48 px (lg+)** on every tab — was hundreds of px. Wide-desktop outer frame: **≈593 px** at 1536/1728 (was ≈920 px) with no empty field before the CTA; screenshots of the first `Směny a AI` view (light + dark, 1440 + 1728) show the void gone and the frame ending after the metric cards + padding. F1–F6 / G1–G4 / R1–R6 evidence otherwise unchanged. **Phase 03 (3 Sep 2026, isolated headless Chrome 152, fresh `out/`):** `/reference` rebuilt into the accepted system — shared `SubpageIntro` (real `<h1>` "Reference", `Domů → Reference` breadcrumb, blue radial wash), header/`<main>`/footer as siblings, `.glow-border` + `SpotlightGroup` cards on the reference `neutral-100 / dark:neutral-900` interior, coalios person glyph in a neutral circle, native `<details>`/`<summary>` "Zobrazit více"/"Skrýt" disclosure. No star rating, no per-card logo. No page-level horizontal overflow at 320/390/768/1024/1280/1440 in both themes; visual check (screenshots light + dark). Dead-route bodies use the shared shell + `CtaButton` (no legacy `bg-bila`/`text-modra`/`Button` tokens). **P03-C1/C2 (3 Sep 2026, `wrangler pages dev out` + headless Chrome 152, `/reference` at 320/390/768/1024/1280/1440 × both themes):** collapsed testimonial quote now `line-clamp-3` (was `line-clamp-4`) → renders exactly **3 visual lines** (`-webkit-line-clamp: 3`), full quote intact on expand; `<details>/<summary>` structure, `bg-neutral-200 dark:bg-neutral-800` avatar, grid, colours, disclosure labels unchanged; no horizontal overflow. Intro string set exactly to `Přečtěte si zkušenosti s plánováním směn v coalshiftu.` (old `…zkušenosti uživatelů coalshiftu…` absent from source + `out/`). P03-PRE-01 resolved; P03-PRE-02 (avatar) ratified as-is. | Published-version visual verification remains separate |
| Q-008 | PASS (toggle both directions + persistence + I8 first-paint/name-state, local) | **E1 (3 Sep 2026):** on `wrangler pages dev out` at 1728/DPR2, the header toggle switches dark→light and light→dark; `localStorage['coalshift-theme']` records the choice and `<html>` class/`data-theme` follow; first visit is dark. `.legal-content` has full `.dark …` variants in the compiled CSS so injected policy HTML is themed both ways; the div is static/server-rendered and `suppressHydrationWarning`, so a theme toggle does not re-render it. Real cross-route reload persistence spot-checked on `/` and `/gdpr`. **I8 (3 Sep 2026, isolated headless Chrome 152, fresh `out/`):** `ThemeToggle` now has one coherent model — a stable accessible name `"Tmavý režim"` in every state plus `aria-pressed` for the dark-mode state (`mounted ? isDark : undefined`, so server and first client render match). The sun/moon glyphs are pure Tailwind `dark:` variant classes (`hidden size-5 dark:block` / `block size-5 dark:hidden`), driven by the bootstrap-applied `<html class="dark">`, not React state. Fresh visit → `dark`, `aria-pressed="true"`, sun shown. Persisted-light visitor: class sampled at `waitUntil:"commit"` = **not dark** (no opposite-theme flash), moon shown, `aria-pressed="false"`. Toggle to light then back to dark: `aria-pressed` and glyph flip both directions, `localStorage` records `light`/`dark`; cross-route nav to `/gdpr` sampled at `commit` already carries `class="dark"` (first paint correct before React). 0 console/hydration/`pageerror`. Button `size-9`, sun-before-moon DOM order and dark-first storage guards preserved. **Phase 03 (3 Sep 2026, isolated headless Chrome 152):** the redesigned `/reference` and the three rewritten dead routes are dark-safe — `SubpageIntro` carries `bg-white dark:bg-black` + `dark:` text/border, `LegacyPage` adds no page background so the themed `body` shows through, `.glow-border` cards resolve their `dark:bg-neutral-900` interior; `/registrace` sampled `body` background `rgb(0,0,0)` in the default dark theme (screenshot). `ThemeToggle` and the theme bootstrap in `layout.tsx` were not touched. The `SubpageIntro` extraction left `/gdpr` + `/cookies` with the same H1/breadcrumb/`.legal-content` intro markup and `.dark` legal styling. | Confirm injected Waulter content re-themes on the published preview |
| Q-009 | **PASS (I1–I2 single fragment offset + responsive/H1 matrix, local isolated headless); physical device NOT_RUN** | **H1 (3 Sep 2026, isolated headless Chrome 140).** All 5 tabs at 320/390/768/1024/1280/1440/1536/1728 in **both** themes: inactive panels `display:none`, only the active panel sizes the frame. Outer-frame heights are now the **natural per-tab content height** (e.g. dark 1728: 593/569/569/569/603; dark 1440: 771/747/747/747/767 — the column layout stacks main + 2 metric cards) with **no forced 0 px delta and no empty field**. Active main column and metric-rail widths are **identical across all 5 tabs** at each width (e.g. 1728 main 748 / rail 384; 1440 main 892 / metrics-below 892). `documentElement.scrollWidth === clientWidth` at every width; no nested vertical scroller. The G/R matrix (no overflow 320–1728, 200 % reflow, vertical tab list, mobile menu) is otherwise unchanged. **I1 (3 Sep 2026, isolated headless Chrome 152, fresh `out/`, viewport 1280):** the stacked clearance is gone — `html { scroll-padding-top: calc(var(--header-height, 5rem) + 0.5rem) }` is the single native mechanism and `Section` no longer carries `scroll-mt` (computed `scroll-margin-top: 0px` on every section). Direct `/#pricing` lands the target **6 px** below the pinned nav-bar bottom; `/#industries`, `/#faq`, `/#contact` land **8 px** below; cross-route `/gdpr` → `/#pricing` **6 px**; the `/zdravotnici` → `/#industries` 301 lands **6 px** below — never one or two extra header heights. `scroll-padding-top` computes to `90px` with `--header-height` measured at `82px`. **I2 JS path (same run):** a plain in-page `#pricing` link click lands **8 px** below the nav bar, updates the hash to `#pricing`, moves focus to the section (`document.activeElement === #pricing`, `tabindex="-1"`), and animates when motion is allowed (mid-scroll 22 px → settle 4072 px). Under `prefers-reduced-motion: reduce` the same click is instant (60 ms sample == 700 ms sample == 4142 px) and the home-logo scroll is instant to top. History: exactly one entry added when the destination changes, **zero** when the same fragment is re-activated; Back clears the hash. A `metaKey` activation is not intercepted (`defaultPrevented` false). `history.scrollRestoration` is left at `"auto"` (no override); the browser-native scroll restoration on Back was not exercised headless — NOT_RUN for that pixel check only. **Phase 03 (3 Sep 2026, isolated headless Chrome 152):** `/reference` at 320/390/768/1024/1280/1440 in both themes — `documentElement.scrollWidth === clientWidth` at every width; the redesigned `/registrace` / `/wait-list` / `/wait-list/thank-you` also show `overflowX` 0 at 390. Cross-route `#contact`: activating `Kontaktujte náš tým` (`href="/#contact"`) on `/wait-list` navigates to `/`, `location.hash === "#contact"`, and the section lands **8 px** below the pinned nav bar (the same native-clearance path as I1). | Repeat native/JS fragment landing and Back scroll on a physical-device preview |
| Q-010 | **PASS (I2–I8 focus/reduced-motion/contrast + vertical-tab semantics, local isolated headless); physical-device matrix NOT_RUN** | **I2–I8 (3 Sep 2026, isolated headless Chrome 152, fresh `out/`).** **I3 modal mobile menu (390 px, both themes):** `#mobile-menu` exposes `role="dialog" aria-modal="true" aria-label="Navigace"`; opening from the hamburger moves focus onto the in-menu close control; `<main>`, `body > footer`, `<header>` and the `#main` skip link all carry `inert` while open; a background link cannot take focus (`bgLink.focus()` → `activeElement !== bgLink`); forward Tab from the last of the 7 focusables wraps to the first and Shift+Tab from the first wraps to the last; Escape closes, restores `body` overflow, removes `inert`, and returns focus to the hamburger; activating a menu fragment link closes the menu, scrolls, and lands focus on the destination section (`activeEl === "faq"`, `<main>` no longer inert). **I4 skip link (homepage, `/gdpr`, `/cookies`):** `#main` has `tabIndex={-1}` + `outline-none`; it is focusable but not a sequential Tab stop; activating "Přeskočit na hlavní obsah" puts focus on `#main` past the shared header. **I5/I6/I7 contrast (viewport 1280, both themes, WCAG formula, effective composited colours):** every `Ilustrační údaj` badge (all 6 instances across the 5 tabs) = `#525252` on `#E5E5E5` → **6.20:1** light, `#D4D4D4` on `#404040` → **6.99:1** dark (wording/uppercase/`10.4px` size preserved). Selected browser tab border `#00728F` (coalsoft-700) at rest → **4.38:1** vs the list surface, **5.06:1** vs the tab fill (light); `#1FC3EC` (coalsoft-400) → **8.60:1** / **7.26:1** (dark) — neutral surfaces, vertical layout and roving focus unchanged. Non-parent coalfamily icons `#737373` (neutral-500) → **4.74:1** on white, **4.43:1** on black; the parent `coalsoft` mark keeps its `#00B5E2` identity colour (not recoloured). **I8:** see Q-008. **E1/E3/E5 (3 Sep 2026):** `FunctionsBrowser` key handler is now orientation-aware — at `aria-orientation=vertical` (≥1024px) ArrowDown/ArrowUp move roving focus (`defaultPrevented=true`) without activating, ArrowLeft/ArrowRight are not `preventDefault`ed (page scroll preserved), Home/End jump, Space/Enter activate the focused tab; the horizontal branch is symmetric (Left/Right move, Up/Down pass through) but real <1024px keyboard is NOT_RUN (harness fixed at 1728px). All 5 clicks/Space update the panel heading + the mock address. FAQ `aria-expanded` toggles. `/gdpr` + `/cookies` have a real `<h1>` (not a span) and a `Domů → title` breadcrumb `<nav><ol>` with `aria-current="page"`. Nav `<li> ::before` underline also fires on `has-[a:focus-visible]`. **F1/F6 (3 Sep 2026):** grid-stacked panels keep inactive copies `inert` + `visibility:hidden` (excluded from AT/keyboard, one DOM copy each — no duplicate accessible text); the scrolled header makes the family strip `inert` so hidden links are not offscreen tab stops; F2 plus is `aria-hidden` and not focusable; the unframed theme toggle stays a real `<button>` with the global focus ring and its visible order matches keyboard order. Icon-accent `#009AC0` on `#f5f5f5` = 3.02:1 (decorative, text-labelled; large/UI ≥3:1). **G/R (3 Sep 2026, isolated headless Chrome 140):** browser tablist `aria-orientation="vertical"` + `flex-direction: column` at 320/390/768/1440; ArrowDown ×2 moves roving focus without activating, Space activates + syncs heading/address/metrics; **no horizontal swipe/scroll** (`scrollWidth == clientWidth`); inactive panels `display:none` (< lg) or `inert` + `visibility:hidden` (lg+) — no hidden focus targets. Mobile menu: focus enters on open, `<header>` `inert`, Escape returns focus to the hamburger, logical Tab order. Header/footer underline also fires on `:focus-visible` (keyboard). Theme toggle `size-8` unframed real `<button>`. Real physical-keyboard Tab traversal on a device still owner-assisted. **H1 (3 Sep 2026):** after removing the grid-stack, inactive panels are `display:none` (native `hidden`) at every width — one semantic DOM copy each, `tabIndex=-1`, not focusable, not in layout, not announced as active. Real keyboard @1440: ArrowDown ×2 → roving focus (index 2), `aria-selected` unchanged; Enter → activates, panel heading + mock address sync. **Phase 03 (3 Sep 2026, isolated headless Chrome 152) — touched pages only (`/reference` + 3 dead routes):** one `<h1>` per route; `<header>`/`<footer>` are **not** nested inside `<main>` on `/reference` (was a Phase 02 defect); every route that renders `<Header/>` now has `<main id="main" tabIndex={-1} class="outline-none">` — skip link focusable, activation lands `document.activeElement` on `#main` past the header, `#main` not a sequential Tab stop. `/reference` testimonial names are `<h2>` under the `<h1>` (no skipped level); the `<details>` summary opens via Enter and click; the invented `stars: 5` graphic and per-card logo are removed. **Contrast on the `/reference` card interior (WCAG formula, composited): name 16.4 / 17.9, role (coalsoft-700 / coalsoft-300) 5.06 / 10.5, quote (neutral-700 / neutral-300) 9.5 / 12.1, "Zobrazit více" 5.06 / 10.5 — all ≥ 4.5:1, light / dark.** Homepage + legal Q-010 (I1–I8) unchanged and not re-audited. Screen-reader pass over the new `<details>` / skip-target semantics remains owner-assisted. **P03-C1 (3 Sep 2026, headless Chrome 152):** `/reference` collapsed quote realigned to `line-clamp-3` — `<summary>` still focusable, Enter/click toggles the disclosure, visible focus outline `solid`, full quote exposed on expand at 320–1440 both themes. The three retained legacy bodies are now redirect-intercepted (P03-C3) and are not an accessibility surface. | Repeat keyboard/focus/contrast on a physical-device preview; screen-reader pass with the new dialog/inert semantics remains owner-assisted |
| Q-011 | PASS (twelve-card copy, path/label sync, legal titles — local) | **E2/E4/E5 (3 Sep 2026):** all twelve `InfoCard` paragraphs are the verbatim expanded `content.md` copy (2 sentences each), no new capability or legal promise. Mock-address paths match the `content.md` selected-tab table exactly and change with pointer/Space, not focus alone. Header/mobile action labels unchanged text, hierarchy swapped. Legal `<title>`s = `Zásady ochrany osobních údajů (GDPR) — coalshift` / `Podmínky cookies — coalshift`; descriptions per `content.md`. Policy bodies still come solely from Waulter — no invented legal text. Monthly amounts/bands/VAT/14-day trial untouched. F1 numerical examples are now authorized only as clearly labelled working data in content.md; six illustrative metrics require product-team confirmation/replacement before final publication. F4 is decoration and must not alter copy or accessible names. **Phase 03 (3 Sep 2026, source + built `out/` grep):** the three dead routes rewritten to the exact `content.md` copy — H1s `Vyzkoušejte coalshift na 14 dní zdarma` / `Začněte s coalshiftem` / `Děkujeme za zájem o coalshift`; leads, CTAs (`Přejít k registraci` / `Vyzkoušet na 14 dní zdarma` / `Přejít do aplikace`), secondary links (`Už máte účet? Přihlásit se.` / `Máte otázky? Kontaktujte náš tým.` → `/#contact` / `Zpět na úvodní stránku` → `/`) all match. `/registrace` support tiles reuse the approved "Směny / Nepřítomnosti / Exporty" practical-use copy verbatim. **Removed (0 occurrences in source and `out/`):** `1. 7. 2025`, `1. července 2025`, `seznamu čekatelů`, `Potvrzení e-mailem`, `Přednostní přístup`, `Speciální cena`, `exkluzivní cenové podmínky`, `CHCI SI VYZKOUŠET`, `Buďte mezi prvními`, the commented "Bonus pro čekající" block, the dead `gift-icon` reference, and tykání forms (`Zaregistruj`, `Začni`, `Získej`, `tvé přihlášení`, `dáme ti`). All retained text is vykání; brand spelling lowercase throughout including new titles/labels. `/reference`: the three quotes + attributions (Michal Uhlíř — coalfamily; Jana Novotná — HR Manager; Petr Svoboda — Provozní ředitel) preserved **verbatim** (including the HR-integration sentence and the " - " punctuation); no claim lifted into H1/intro/metadata; no rating/JSON-LD. 14-day trial wording consistent across the new metadata descriptions and CTAs; Free plan not mentioned on these pages. **P03-C2 (3 Sep 2026):** `/reference` intro reset to `Přečtěte si zkušenosti s plánováním směn v coalshiftu.` — drops the word `uživatelů`, so no unverified customer-status claim is introduced in newly authored copy; matches the metadata description register. Testimonial quotes/names/roles re-confirmed verbatim after P03-C1. | Owner still confirms testimonial attribution/content before publication; two provenance flags recorded |
| Q-012 | PASS (monthly data + labels preserved through F3, local) | **F3 (3 Sep 2026):** pricing wrapped in `SpotlightGroup as="ul"` with `<li>`-only children (valid list semantics); each card keeps the white/dark interior via `data-surface="white"`, and the approved interior layout, `Vyzkoušet` + `Prvních 14 dní zdarma.` / `Začít zdarma`, `za měsíc, bez DPH`, VAT note, 5 amounts/bands and Lite emphasis (now a permanent `ring-coalsoft` over the shared rim) are unchanged. No annual/switch/surcharge/checklist added. | Recheck only if pricing copy/CTA changes again |
| Q-013 | PASS (data/targets/pattern/desktop render); NOT_RUN (real narrow/wide crop) | **D5 (2 Sep 2026).** `Contact` ported from `tym-item.njk`: round **112px** portrait above a separate `.glow-border` card, offset `ml-6`; name Lekton 20px bold, role in coalsoft blue (5.5:1 light / 10.5:1 dark), phone + mail icon rows; 2-col grid. Martina Adamcová (Obchod a produkt) + Šárka Melišová (Podpora) with exact `tel:`/`mailto:` — inspected, not dialled/sent. Portraits `<ResponsiveImage fill>` `object-cover rounded-full` `loading="lazy"`, originals preserved; both people render correctly (screenshot). 0 "miroslav" in the build. **R3 (3 Sep 2026, isolated headless Chrome 140):** `#contact` grid is 1-column (`358px`) at 390 with both portraits present and correct alts (`Martina Adamcová — Obchod a produkt`, `Šárka Melišová — Podpora`), no page overflow, email strings `word-break: break-all`; two-column at 1440. | Rendered-crop close inspection on the published preview |
| Q-014 | PASS (full route/indexing matrix, local export); production HTTP-header/canonical/crawler NOT_RUN | **E5 (3 Sep 2026):** exported HTML — `/gdpr`: `<title>Zásady ochrany osobních údajů (GDPR) — coalshift</title>`, description `Informace o zpracování osobních údajů na webu coalshift.`, canonical `https://coalshift.cz/gdpr`; `/cookies`: title `Podmínky cookies — coalshift`, description `Informace o používání souborů cookies na webu coalshift.`, canonical `https://coalshift.cz/cookies`. **Phase 03 (3 Sep 2026, isolated headless Chrome 152, fresh `out/` HTML grep per route):** route metadata centralised in `app/lib/seo.ts`, consumed by every `page.tsx` + `app/sitemap.ts` + `app/robots.ts`. `app/layout.tsx` no longer sets `alternates`/`openGraph`/`twitter` — nothing inherits a per-route canonical or social card. Per route: `/` title `coalshift | AI plánovač směn a docházky` (once), self-canonical `https://coalshift.cz`, `og:*` + `twitter:card=summary` (no image); `/reference` title `Reference | coalshift`, canonical `https://coalshift.cz/reference`, OG/Twitter summary; `/gdpr` + `/cookies` unchanged canonicals + added OG/Twitter summary; `/registrace` + `/wait-list` + `/wait-list/thank-you` → `<meta name="robots" content="noindex, follow">`, **0** `<link rel="canonical">`, **0** `og:*`. `out/sitemap.xml` = exactly `https://coalshift.cz`, `https://coalshift.cz/reference`, `https://coalshift.cz/gdpr`, `https://coalshift.cz/cookies` (no `lastmod`/`priority`). `out/robots.txt` = `User-Agent: *` / `Allow: /` / `Sitemap: https://coalshift.cz/sitemap.xml`, **no `Disallow`**. No `*.pages.dev` / preview hostname anywhere in generated output; no JSON-LD (`application/ld+json`, `AggregateRating`, `@type` all absent). `next.config.ts` pins `trailingSlash: false`; canonical = `og:url` = sitemap `<loc>` for every route (homepage form is the bare origin `https://coalshift.cz` — Next's metadata resolver returns `result.origin` for root — a documented deviation from the plan table's `https://coalshift.cz/`, still self-consistent). 404 (`out/404.html`) now emits **no** canonical (was inheriting `/`). **NOT_RUN / BLOCKED (deployment-only):** `X-Robots-Tag` header intent — Cloudflare injects `x-robots-tag: noindex` on every `*.pages.dev` route, so header-level index vs noindex cannot be shown on a preview and is meaningful only against the production origin; production canonical resolution and real crawler `noindex` observation; `/reference` in a real Search Console. **P03-C3 (3 Sep 2026, `wrangler pages dev out`):** the six legacy URL forms now return **301 to `/`** (verified without following redirects; one hop, no loop) — the public indexing mechanism for those families is the redirect, not the retained route-body `noindex` meta (defense-in-depth / source history only). `out/sitemap.xml` unchanged (4 public URLs); no preview hostname; no JSON-LD. Production 301 `Location` header remains deployment-only BLOCKED. | Deployment-only: verify `X-Robots-Tag`, production canonical resolution, crawler `noindex` and the six legacy-URL 301 `Location` headers after an authorized publish |
| Q-015 | PASS (first-party controls independent of any third party, local); NOT_RUN (full perf comparison, live glow) | **E1/E2 (3 Sep 2026):** Quanda is gone entirely, so first-party startup no longer has any third-party gate — tabs/theme/FAQ all operate with GTM/Waulter/Leady requests aborted or pending. `SpotlightGroup` still gates the pointer glow to `(hover:hover) and (pointer:fine)` and not `prefers-reduced-motion`, with listener cleanup; the `.glow-border` neutral rim is CSS-only and remains at rest and under reduced motion (`--glow-intensity:0`). Phone mockup stays `loading="lazy"`; new `InfoCard` icons are inline SVG (no extra requests). Live glow motion + CWV/transfer baseline NOT_RUN. **F1/F6 (3 Sep 2026):** the browser phone `<img>` is removed entirely (its file/registry/derivatives kept for the pipeline); numerical cards are pure text/SVG, no new requests. F1 frame geometry is stable across selection (0 px at 1728). F6 header uses one scoped scroll listener with cleanup + `motion-reduce:!transition-none`; the scroll-state transitions do not shift page content (fixed header + constant `calc(--family-block-h + --header-height)` spacer). Real scroll-driven animation NOT_RUN (`window.scrollTo` emits no `scroll` in automation). **G/R (3 Sep 2026):** removing the horizontal swipe strip drops the `overflow-x-auto` scroll container from the browser; the mobile menu adds one scoped `<div>` with an `origin-top` transform transition (no image, `motion-reduce:!transition-none`); reduced-motion run confirms the `.glow-border` rim `linear-gradient` background is present and link `::before` `transition-duration` is `1e-06s` (snaps). No new fonts, images or third-party requests. Full CWV/transfer comparison still deferred. **H1 (3 Sep 2026):** removing the grid-stack means only the active panel is rendered/measured — less layout work per tab switch; no fixed height, `min-height`, measuring clone or JS height reservoir was introduced. No new fonts/images/requests. **Phase 03 (3 Sep 2026):** no image is created or referenced (`twitter.card = "summary"`, no `og:image`); `/reference` uses inline SVG for the person glyph (no `next/image`, no request); the `<details>` disclosure is native (no JS) and its `transition-all` is snapped by the existing global reduced-motion rule; `ReferenceList` reuses the existing `SpotlightGroup` client island (fine-pointer + motion gated, listener cleanup) already shipped for Pricing/Capabilities. `SubpageIntro` and `LegacyPage` add no fonts/images/JS. Layout metadata refactor removed markup only. No new third-party request. | Perf/CWV comparison on the published preview |
| Q-016 | PASS (Quanda removed, GTM/Waulter preserved, shells correct — local); NOT_RUN (real provider injection) | **E1/E5 (3 Sep 2026):** `grep -rni quanda` over `app/`/`public/`/config → only the removed `<Script>` + comment; `out/` has 0 `quanda`/`onquanda`; loaded DOM has no Quanda script or global. GTM `GTM-NQDZKVLF` inline + `<noscript>` iframe intact, `window.dataLayer` populated; the GTM container still injects `https://cdn.waulter.cz/sdk.js` (Waulter loader preserved, not touched, no second loader). `/gdpr` + `/cookies`: exactly one exact-ID container each, empty in the export, inside a `.legal-content` wrapper; footer entry is a full-document `<a>`; `suppressHydrationWarning` server-rendered div so React never overwrites injected HTML. **Outstanding:** on `127.0.0.1` the Waulter SDK loads but injects no policy HTML — the cause is unverified; domain/provider configuration is a possible explanation, not a confirmed diagnosis. Verify real population on an appropriate published domain. No GTM edit made. **Phase 03 (3 Sep 2026, `out/` grep + headless Chrome 152):** the `app/layout.tsx` metadata refactor removed only `alternates`/`openGraph`/`twitter` — GTM `GTM-NQDZKVLF` (×3 refs), the `<noscript>` `googletagmanager.com/ns.html` iframe, `window.dataLayer`, the render-blocking theme bootstrap and the three font `<link rel="preload">` all still present in `out/index.html`. No second consent loader added; `grep -rni quanda` over `app/`/`out/` still returns nothing. `LegalPage.tsx` keeps the exact `<div id="waulterGdpr">` / `<div id="waulterCookies">` (one each, empty), `suppressHydrationWarning` and the `.legal-content` wrapper; only the intro `<section>` was factored into the shared `SubpageIntro`, and `/gdpr` + `/cookies` still render the same H1 + `Drobečková navigace` breadcrumb + `aria-current="page"`. Footer legal links stay full-document `<a href="/gdpr">` / `/cookies` (the added `Reference` entry is a separate `<Link>`). | Publish the redesign preview, then confirm each policy appears once, survives a theme toggle and reads correctly |
| Q-017 | NOT_RUN | Calendly account/event pending | Owner input before Phase 05 |
| Q-018 | NOT_RUN for the new release | Phases 01–03 accepted; cleanup and exact-version production handoff assigned to Phase 04 | Phase 04 |

## Follow-up from the implementation review

The current build/typecheck/HTTP successes remain valid. Codex independently verified generator rejection of missing source, wrong registry dimensions and a width above the original in an isolated fixture (exit 1 as intended for all three). No application source was changed for those checks.

R4 and R5 were applied and passed Codex's focused review on 2 September 2026. R4 completed the Q-004 non-self-target clause in the shared `isPlainActivation` guard (verified against the actual module, including currentTarget versus a nested target; no current fragment caller supplies a non-self target). R5 resolved the seven confirmed transitive development-dependency audit findings within existing ranges via `npm audit fix` without `--force` — `package.json` and selected direct versions unchanged. Claude reports successful `npm ci`; Codex independently reran `pages:build` / `typecheck` / `npm audit --json`, all exit 0, audit = 0 vulnerabilities. The remaining `glob@10.5.0` deprecation notice does not establish an unresolved advisory in this audit snapshot. No further full /recheck is requested without a concrete new risk.

On 2 September 2026 the first preview was published under explicit authorization: commit `8f1db89`, deployment `6011b67f` (`https://6011b67f.coalshift.pages.dev`, alias `redesign.coalshift.pages.dev`), with a successful build reported by Claude and independently verified HTTP/HTML delivery by Codex. Real viewport/DPR rendering and mobile/keyboard/modified-click/menu-focus/completed-scroll evidence remains open. After inspecting the preview, the owner accepted progression and deferred these checks to the redesigned UI and final handoff as recorded above. Phase acceptance does not alter those NOT_RUN results.

## Inputs, exceptions and limits

Owner-supplied portraits, Calendly configuration and reference-testimonial provenance have the owners and timing recorded in brief.md. The historical-route indexing decision is resolved in the Phase 03 matrix. Preview validation needs actual branch publication and dashboard configuration; an unavailable preview must remain explicitly unverified.

The viewport set, selected contrast checks, dark default and reduced-motion behavior are project choices. They are not a claim that all accessibility or legal requirements have been audited. There is no promised Lighthouse score, search ranking or measured field performance at this stage. Do not create additional audits merely to fill the area catalog.

The explicit cookie/GTM exclusion overrides the original PDF suggestion. The owner also approved the phase 01 Q-003/Q-004 verification deferral recorded above and in plan.md; it permits phase 02 to begin without claiming those checks passed. Other material changes to these criteria require the owner's decision, not only a review-plugin recommendation.

## Owner-approved scope update — 2 September 2026

Q-006/Q-007/Q-010/Q-011/Q-012/Q-016 now reflect the explicit phase-02 feedback and follow-up: keep the six-function overview; add a practical-use browser immediately before monthly pricing; match reference details; lowercase brands; retire only healthcare with a redirect. The old video-preservation clause does not require relocating the healthcare video. No other route deletion, annual offer, new quality area or publication permission is introduced. Phase-01 verification deferrals remain as recorded in plan.md.

The second owner review extends the same Q-007/Q-009/Q-011/Q-012/Q-013 details through D1–D7, and the independently demonstrated startup defect is addressed by existing Q-010/Q-015/Q-016. No additional quality area, phase or publication permission is introduced.

Third owner review: E1–E5 supersedes Quanda preservation and fixed mock URL, adds richer shared cards/menu/footer and two provider-populated legal routes. Existing Q-014 applies narrowly to their basic metadata in phase 02; whole-site SEO stays in 03. The owner supplied exact IDs; no missing markup input remains.

## Owner-approved Phase 03 route/indexing update — 3 September 2026

Q-006 and Q-014 now reflect the final explicit owner decision: `/reference` becomes a footer-linked, indexable subpage in the accepted design; source for `/registrace`, `/wait-list` and `/wait-list/thank-you` remains in the repository, but both URL forms of each return 301 to `/`, receive no internal links and have no sitemap entries. `/`, `/reference`, `/gdpr` and `/cookies` are the complete sitemap set. Retained `noindex` source metadata may remain but is not relied upon while static redirects are active. This resolves the prior decision dependency without deleting source, changing application destinations or authorizing publication. Existing testimonial provenance and all previous verification limitations remain explicit.

## Phase 03 implemented locally — 3 September 2026

Phase 03 was implemented by Claude Code on branch `redesign`; HEAD unchanged at `68fdbcb`; all work uncommitted; no push/deploy/external change. `npm run typecheck` and `npm run pages:build` (fresh `out/`) exit 0; verified in isolated headless Chrome 152 against the served export. A dated Phase 03 evidence segment is appended to Q-004, Q-006, Q-007, Q-008, Q-009, Q-010, Q-011, Q-014, Q-015 and Q-016; no earlier Phase 02 / I1–I8 result or `NOT_RUN` / `BLOCKED` item was overwritten. Deployment-only checks — Q-001 preview build, `X-Robots-Tag` header intent, production canonical resolution, real crawler `noindex`, the `_redirects` `Location` fragment on a real preview, and real Waulter policy injection — stay `BLOCKED` / `NOT_RUN` pending an authorized publish; a `*.pages.dev` `X-Robots-Tag: noindex` is expected and cannot prove production route intent. Owner pre-publication inputs unchanged: confirmation of the three testimonials' attribution/content, with Michal Uhlíř's "coalfamily" role and the Petr Svoboda HR-system-integration sentence flagged. The homepage canonical / `og:url` / sitemap `<loc>` render as the bare origin `https://coalshift.cz` (Next's metadata resolver returns `result.origin` for the root path under `trailingSlash: false`) — self-consistent across all three, a documented deviation from the `/replan` table's `https://coalshift.cz/`.

## Phase 03 — Codex pre-recheck evidence, 3 September 2026

This segment supplements the implementation report and does not replace Phase 02 evidence or accept Phase 03. Reviewed `redesign` at `68fdbcbd2562f73db91a683315d259c4fca4ef04` plus the accepted Phase 02 tree and uncommitted Phase 03 delta.

| Check / affected requirement | Result and evidence | Limit |
| --- | --- | --- |
| Typecheck / local integrity | PASS — independent `npm run typecheck` on pinned Node 24.20.0; `git diff --check` PASS | Not a new build or browser-matrix result |
| Q-014 route signals | PASS for inspected source and existing export — four public self-canonicals and sitemap URLs; three legacy `noindex, follow` routes without canonical/social fields; own title/description per route; allow-all robots plus sitemap | Existing `out/` inspected, not freshly rebuilt by Codex; no production HTTP/crawler evidence |
| Q-006, Q-010 structural checks | PASS for parsed export — seven routes with one H1 and focusable `#main`, sibling landmarks, no duplicate IDs, one footer Reference link and no legacy-route discovery links | Interactive focus, navigation and direct-load HTTP behavior remain attributed to Claude’s browser report |
| Q-011 testimonial preservation | PASS — programmatic comparison of all three quotes, author names and roles against pre-port `References.tsx`: exact matches | Attribution/content authenticity remains unverified |
| Q-007, Q-008 reference fidelity | FAIL for the two locked details — P03-PRE-01: `ReferenceList.tsx:76` has `line-clamp-4`, expected three lines; P03-PRE-02: line 61 avatar backgrounds neutral-200/800, expected neutral-100/900 | Bounded design deviations, not demonstrated contrast/keyboard failures; no code fix applied |

Root canonical/OG/sitemap consistently use `https://coalshift.cz`, accepted as a project-specific equivalent root form; active requirements are clarified in Phase 03. Next resolver control checks do not support a universal “no configuration can emit the slash” rule. Cloudflare’s default preview noindex policy is not evidence of production response headers or of every `*.pages.dev` hostname.

Claude’s reported fresh build and Chrome 152 checks retain their original attribution. Physical-device/screen-reader/Back-position, CWV, live Waulter population, current preview, remote redirect and production indexing checks retain their recorded NOT_RUN/BLOCKED status. Six illustrative metrics stay labelled; testimonial provenance remains a pre-publication input. The next action is a bounded Phase 03 `/recheck`, without fixes or publication.

## Phase 03 — /recheck assessment and owner scope clarification, 3 September 2026

The six-agent `/recheck` reported no critical issue and independently reconfirmed typecheck, build and the four-public-route SEO output. Its 28/29 checklist result is narrowed by the owner’s final decision: the active design/content review surface is the accepted homepage plus `/reference`; the three legacy bodies are frozen retained source. Their six URL forms are assessed only for HTTP 301 to `/`, no active discovery and sitemap exclusion. Legacy body metadata, breadcrumb, styling, link-target and code-cleanup observations are not quality failures for this phase.

Accepted corrections: Q-007 P03-C1 changes the collapsed testimonial from four to the locked three lines; Q-011 P03-C2 replaces the unverified “uživatelů” intro with the approved neutral reference description. The neutral-200/800 avatar circle is accepted under Q-007 as a context-aware adaptation because the surrounding card interior is neutral-100/900. Keep the native coalios `<details>/<summary>` port; review confirmed name/role/state and found no WCAG A/AA failure. SEO type hardening, import organization, a new sitemap assertion and other cleanups are optional maintainability suggestions, not current failures.

Phase 03 remains `in_review`. Run focused `/reference` responsive/theme/disclosure checks plus typecheck/build after P03-C1/C2, and verify the six exact 301 responses against fresh Wrangler-served output after P03-C3. Preserve the four-route public SEO output; no complete `/recheck` is required unless the correction report exposes a new concrete risk.

## Phase 03 — Codex post-correction assessment, 3 September 2026

P03-C1–C3 pass targeted review. Source contains `line-clamp-3`, the exact neutral `/reference` introduction and all eight required `public/_redirects` rules. The four frozen legacy-source SHA-256 values match Claude's before/after table; `git diff --check` passes. Codex independently ran Wrangler 4.128.0 against the fresh `out/` and requested URLs without following redirects: `/registrace`, `/registrace/`, `/wait-list`, `/wait-list/`, `/wait-list/thank-you` and `/wait-list/thank-you/` each returned **301** with exact `Location: /`; `/zdravotnici` and `/zdravotnici/` each returned **301** with exact `Location: /#industries`; `/` returned **200**. Claude's successful typecheck/build and headless Chrome 152 `/reference` matrix retain their own attribution. No new failure or repeat `/recheck` is indicated. Production-domain redirect headers and other deployment-only evidence remain BLOCKED until an authorized publish; Phase 03 stays `in_review` pending owner acceptance.

## Owner-approved Phase 04 cleanup and release — 3 September 2026

Jakub accepted Phase 03 and requested cleanup followed by merge and production deployment. The new Phase 04 selects maintainability, media/build integrity and actual release verification; prior UI, accessibility, content, pricing, contacts, SEO and integration requirements apply as regression protection. Calendly/Q-017 moves to Phase 05. No new CMS/forms/analytics/SEO campaign, framework migration, blanket dependency update or Lighthouse/CWV score target is added.

Q-005 now includes concise current-state documentation and evidence-based dead-code/asset/config removal. Q-003 permits retiring unused media/registry entries previously preserved only because cleanup was out of scope. The three legacy route implementations and their necessary dependencies remain intentionally retained; their six public URL forms must keep 301 to `/`. Q-001/Q-018 include the owner's already-authorized final release after cleanup review, including the default and production branch distinction. General release checks formerly in the Calendly phase move here.

The owner accepts publication of the reviewed current website; preserve its visible illustrative labels and testimonial attribution. This acceptance is not independent verification of numerical results or provenance. The concise replacement documents must retain those factual limits and any genuinely unperformed checks, without continuing to present completed correction rounds as active work.

**Preparation evidence:** Codex read the shared maintenance/release/media modules, audited the source/config/docs inventory and fetched origin. Approximately 66 MB of docs are mostly historical screenshot/report artifacts. `redesign` (`68fdbcb`) contains both `master` (`2aaef8e`) and recorded production `cloudflare-deploy` (`0138dbb`); accepted Phase 02/03 changes remain uncommitted. No source cleanup, build, commit, push or deployment was performed during this preparation. Read-only `npx wrangler pages project list` was BLOCKED by missing non-interactive authentication; current Cloudflare settings are not yet independently verified.

[Cloudflare branch controls](https://developers.cloudflare.com/pages/configuration/branch-build-controls/) and [Git integration](https://developers.cloudflare.com/pages/configuration/git-integration/) were opened on 3 September 2026: the configured production branch and automatic-deployment setting govern publication, independently of GitHub's default branch. Verify the project's actual settings and exact deployment SHA before claiming a release.

During documentation consolidation, preserve Q-ID meanings and the latest valid evidence/remaining limitations; replace long historical transcripts with concise versioned results. Old workflow prohibitions and superseded design expectations are not current requirements. Phase 04 stage A returns for review; its stage B performs the authorized release with exact-version verification.
