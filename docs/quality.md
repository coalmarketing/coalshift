# Coalshift — Quality profile

## Context and reference basis

This profile covers the existing six-route Czech marketing website, its static Cloudflare Pages build, the shared design system and the late Calendly integration. It is the canonical definition of quality requirements; phases refer to the IDs below. It does not certify the separate Coalshift application.

Selection follows the shared web-quality skill and workflow version 1.2, dated 2 September 2026. Relevant modules considered: content/SEO, technical SEO, responsive UI, accessibility, media/performance, forms, analytics/consent, security/privacy, maintainability and release verification. The requirements below are standalone; access to the shared skill folder is not needed to interpret them.

Primary sources opened during discovery on 2 September 2026:

- [Next.js static exports](https://nextjs.org/docs/app/guides/static-exports) and [Cloudflare static Next.js deployment](https://developers.cloudflare.com/pages/framework-guides/nextjs/deploy-a-static-nextjs-site/): `out` is the selected static output; the default runtime image optimizer is not supplied by static export.
- [Cloudflare preview branch controls](https://developers.cloudflare.com/pages/configuration/branch-build-controls/) and [preview deployments](https://developers.cloudflare.com/pages/configuration/preview-deployments/): provider behavior to verify against the actual project settings and deployed version.
- [Next.js August 2026 security release](https://nextjs.org/blog/august-2026-security-release): patched releases 15.5.24 and 16.3.3 were published. Select a currently supported compatible release in phase 01; these numbers are dated observations, not instructions to install an older version later. [Earlier RSC advisory](https://nextjs.org/blog/CVE-2025-66478) does not by itself prove exploitability of this static production website.
- [WCAG 2.2 contrast guidance](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html) and [reflow guidance](https://www.w3.org/WAI/WCAG22/Understanding/reflow.html): basis for the selected interface checks. This project does not claim a full WCAG certification.
- [Reduced-motion guidance](https://www.w3.org/WAI/WCAG22/Understanding/animation-from-interactions.html): reduced-motion support is a project requirement. The linked interaction criterion is AAA; it is not presented as a blanket AA requirement.
- [MPSV Labour Code reference](https://ppropo.mpsv.cz/zakon_262_2006), [breaks](https://ppropo.mpsv.cz/VIII4Prestavkavpraciabezpecnostn) and [daily rest](https://ppropo.mpsv.cz/viii81nepretrzitydenniodpocinek): support correction of the PDF's inaccurate section reference, not a legal audit of the product.
- [Calendly embed options](https://calendly.com/help/embed-options-overview): supports inline/link approaches; the owner's event and account configuration remain unknown until phase 04.

## Area selection

| Area | Decision | Rationale and revisit trigger |
| --- | --- | --- |
| Content, copywriting and SEO | selected | Public content and offer are being revised across existing routes |
| Technical SEO and semantics | selected | Shared metadata, headings, indexation and retained URLs need explicit treatment |
| Responsive UI and family design | selected | Core redesign, both themes and mobile CTA |
| Accessibility | selected | Selected navigation, keyboard, focus, contrast, semantics and reflow requirements |
| Performance and media | selected | Confirmed image failures; new effects and portraits must be checked |
| Maintainability | selected | Framework update, duplicate config, shared components and public data |
| Dependency/security/privacy boundaries | selected | Patched supported dependencies, external links/scripts and no exposed secrets; no application penetration test |
| Release and preview verification | selected | Automatic production deployment requires branch separation and exact-version evidence |
| New forms/Basin | not_applicable | This task retains outbound registration and contacts; no new enquiry form is requested |
| Analytics expansion and cookie work | deferred | Owner explicitly manages the functioning banner through GTM; revisit only on a new request. Existing integration is preserved by Q-016 |
| Calendly | deferred | Approved for 04 after account/event inputs; Q-017 defines the later check |
| CMS/Decap | not_applicable | No content editor workflow in the target; do not inherit reference-site CMS |
| GEO/llms.txt | not_applicable | Clear public content is covered by SEO; no separate AI discovery artifact or visibility promise is requested |

## Agreed requirements

| ID | Observable requirement and scope | Responsible phase | Verification and conditions |
| --- | --- | --- | --- |
| Q-001 | Work and preview remain on `redesign`; production branch stays `cloudflare-deploy`. Preview build and search exclusion are verified on the actual deployed version before remote acceptance | 01; recheck 04 | Record branch/HEAD/status and actual publication actions. On an authorized preview, capture URL, source commit, successful deployment and `X-Robots-Tag` response. Do not substitute localhost or production evidence |
| Q-002 | One reproducible static build uses supported compatible dependencies and `npm run pages:build` produces `out`. Preview/configuration agree on that directory and do not require an absent server optimizer | 01; recheck 04 | Record exact Node/npm/framework versions and lockfile state. Run a clean install under the authorized implementation assignment, build, suitable TypeScript check, and serve exported output. Check official support/security guidance at version selection. Do not ignore failures or run a publishing command as a test |
| Q-003 | Phone mockup and all in-scope raster assets render on the static site with correct proportions; new contact portraits display the correct people. No image relies on a failing default `/_next/image` endpoint | 01 original images; 02 new assets; recheck 04 | Inspect browser Network, status/content type, actual rendered/natural dimensions and console on served `out` and authorized preview. Check narrow and wide sizes, loading and fallback behavior. Record delivery/transformation strategy; disabling optimization alone is not image optimization |
| Q-004 | Registration/login links navigate to their existing app destinations; mobile registration closes the menu and navigates. Link and action semantics remain correct for every affected shared Button caller | 01 repair; 02–04 regression | Click and keyboard-activate desktop/mobile CTAs. Inspect rendered anchor/button semantics, targets and rel attributes; test a link with onClick and an action-only button, including in-page pricing navigation. No nested interactive elements or hidden focusable menu links |
| Q-005 | Configuration, fonts and shared primitives have one intentional implementation; phase 01 cleanup does not alter the offer or remove routes | 01 | Explain active Tailwind/PostCSS configs before consolidating. Check font requests for missing assets, remove unused code only after reference search, run the repaired build/types, and inspect representative pages for unintended changes |
| Q-006 | All six routes listed in brief.md load directly and after refresh; existing navigation scope and external registration destinations are preserved. Historical pages do not require a forced refresh query parameter | 01 regression; 02–04 affected routes | Verify each route on exported output and authorized preview, including nested thank-you URL. Record status, content and refresh behavior. Compare homepage links against baseline; do not add links to previously unlinked legacy pages |
| Q-007 | Visual language follows the Coalios reference patterns using COALsoft blue, adapted to Next/React with reusable shared components and actual Coalshift content | 02 foundation; 03 remaining pages | Compare available reference render and implemented screenshots for typography, spacing, rounded surfaces, buttons, navigation, brand bar and footer. Record reference version and tested widths. Source analysis alone is not a completed visual comparison |
| Q-008 | Both themes are usable; first visit defaults to dark, explicit choice persists between routes and reloads, and toggling does not cause hydration errors or an avoidable opposite-theme flash | 02; recheck 03–04 | Test fresh storage, light selection, navigation, reload and return to dark. Inspect both logos/text/backgrounds and console. A blocked storage API must not break rendering. Give theme control an accessible name and state |
| Q-009 | Content, pricing, contacts and navigation remain usable at 320, 390, 768 and 1440 CSS px in both themes, without page-level horizontal overflow or covered content. Sticky header and anchor offsets agree | 02; recheck 03–04 | Record viewport screenshots and interactions. Check 200% text zoom and equivalent 320 CSS px reflow, open mobile menu, long Czech labels and footer links; pricing may stack as labeled cards |
| Q-010 | Affected UI has logical headings/landmarks, unique IDs, meaningful labels, visible focus, keyboard operability and appropriate contrast. Decorative effects do not obstruct content and respect reduced motion | 02; recheck 03–04 | Inspect DOM and keyboard order; test menu open/close/Escape/focus, FAQ state, theme and CTAs. Check no hidden content traps focus. Measure actual color pairs: normal text ≥4.5:1, large text ≥3:1, required control boundaries/state indicators ≥3:1. Inspect image alt text, zoom and reduced-motion/coarse-pointer behavior. Use automated checks as a supplement, not proof of all manual criteria |
| Q-011 | Edited copy uses content.md, consistent vykání and the 14-day trial, without invented product/payment terms. Revised legal wording avoids the incorrect §79 reference for breaks/rest and blanket legal guarantees. Historical pages make no unverified submission promise | 02 homepage; 03 all pages; recheck 04 | Search rendered/source copy, metadata, accessible labels and duplicate components for old trial lengths, launch dates, obsolete contacts and conflicting claims. Compare intentional retained testimonial quotes separately; do not rewrite attributed speech as new evidence |
| Q-012 | Every pricing view matches the canonical table in content.md, explicitly excludes VAT, and differentiates plans only by employee count | 02; affected regression 04 | Compare all five tiers, monthly/annual values, employee boundaries and any toggle state. No per-tier feature checklist or per-employee surcharge. Verify keyboard/mobile labels and no fabricated annual price for Pro |
| Q-013 | Contact section displays the two approved people, roles, matching portraits and working phone/email links; obsolete Miroslav contact details disappear from active website output | 02; affected regression 04 | Compare against content.md; inspect tel/mailto targets without sending messages. Check image/person association and rendered crops at narrow/wide widths. Supplied source portraits must be integrated before full contact acceptance |
| Q-014 | Routes have meaningful Czech metadata and canonical handling; homepage uses the required title. Sitemap/robots reflect the agreed indexing choices and never accidentally index preview as production | 03; recheck 04 | Inspect generated HTML per route, canonical URL, title/description and social preview metadata. Verify headings, internal links, HTTP status and sitemap/robots output. Canonicals use the production domain, not a preview hostname. Do not globally canonicalize every route to `/` or silently deindex preserved pages |
| Q-015 | Media/effects have appropriate responsive delivery and loading; fonts and decorative JS do not create avoidable layout shifts or obstruct interaction | 02; recheck 03–04 | Record transfer sizes and requested variants at mobile/desktop widths, image dimensions/sizes and priority choices. Compare the working phase 01 baseline against the redesign under the same browser/cache/network conditions; investigate concrete regressions. Below-fold portraits/calendar are not eagerly prioritized. Test disabled/reduced animation and blocked third-party loading. No unmeasured Lighthouse/CWV claim |
| Q-016 | GTM `GTM-NQDZKVLF` and existing owner-managed consent integration are preserved. Cleanup or new third-party content does not silently introduce duplicate trackers, exposed credentials or a new consent system | 01–04 affected changes | Diff global integration and inspect script requests on the relevant page. Confirm any removed Quanda script has no consumer first. Keep existing video usable. For Calendly, document its own loading behavior and coordinate an actual conflict with the owner; do not expand this into a banner audit |
| Q-017 | Final consultation CTA and inline contact calendar use the owner's real approved Calendly event; a direct booking link remains available if the embed cannot load | 04 | Inspect both placements and event/organizer identity. Test keyboard, responsive heights/scrolling, both themes, loading failure and navigation. A real booking/email notification test requires explicit owner authorization; report it separately from link/embed rendering |
| Q-018 | Handoff identifies the exact reviewed version, actual checks, remaining limitations, release procedure and recovery approach without automatically publishing production | 04 | Record branch/commit or uncommitted scope, preview URL, selected requirement evidence, owner dependencies, approved commands and how to recover the prior accepted deployment/version. The release report must distinguish local, preview and production results |

## Verification evidence

At document generation no implementation has been verified. Discovery failures are documented in brief.md and phase 01; they do not make an unimplemented requirement pass. Update each row with exact evidence as work proceeds. If one requirement spans several phases/environments, retain the separate results rather than overwriting an earlier limitation with an unrelated pass.

Environment for the phase 01 rows below: Node 24.20.0, npm 11.19.0, Next.js 16.3.4 (Turbopack), Chrome via Claude-in-Chrome at 1728 CSS px / DPR 2, static export served from `wrangler pages dev out`. Branch `redesign`, uncommitted on baseline `0138dbb2073c31e72a9a61c987ee02a8ae9ce4dd`.

Codex reviewed both submissions on 2 September 2026. After the R1/R2 corrections, a fresh Codex `pages:build`, typecheck and focused local HTTP/decoded-image checks passed. After R4/R5, Codex independently reran the build, typecheck and `npm audit --json` (all exit 0; zero vulnerabilities), and executed the actual exported guard after TypeScript transpilation: 12 activation cases plus missing-destination and absent-document checks passed. Clean-install evidence remains attributed to Claude. Browser initialization remained blocked; Claude's fixed 1728 CSS px / DPR 2 synthetic-event and CSS-forced menu checks are separate evidence. The remaining mobile, viewport/DPR, pointer/keyboard and completed-scroll checks are NOT_RUN. Passing source/build/HTTP checks do not establish a complete local PASS for Q-003 or Q-004. Requirements are unchanged.

| Requirement | Result | Evidence, tested version/environment | Next action |
| --- | --- | --- | --- |
| Q-001 | PASS (local); NOT_RUN (remote) | Phase 01: branch `redesign`, HEAD `0138dbb`, `origin/cloudflare-deploy` untouched, no commit/push/deploy. Owner supplied Preview screenshots on 2 Sep 2026 and confirmed saving Custom branches with only `redesign`, empty exclusions and production still `cloudflare-deploy`. This confirms setup, not deployment. Preview URL / source commit / `X-Robots-Tag` still need an authorized publication | Perform the prepared owner-assigned preview publication and verify its actual deployment; recheck 04 |
| Q-002 | PASS (local); NOT_RUN (remote) | Claude reports reproducible npm ci. Codex independently passed the corrected pages:build and typecheck on Node 24.20.0, then served the fresh out/ using Wrangler 4.128.0. The earlier Codex internal-port limitation was overcome with escalated local execution. Next 16.3.4 / React 19.2.8 / TS 5.9.3. R5 (2 Sep 2026): `npm audit fix` without `--force` updated 10 transitive dev-dependency nodes inside existing ranges (`package.json` and direct versions unchanged); `npm ci` / `pages:build` / `typecheck` exit 0 and `npm audit` = 0 vulnerabilities on the updated lockfile | Verify the real Cloudflare build after authorized publication in phase 01; recheck 04 |
| Q-003 | PASS (candidate metadata/HTTP); NOT_RUN (remaining browser/remote) | R1: ResponsiveImage emits actual file widths for all seven WebP candidates. Codex checked fresh HTML, decoded dimensions and HTTP 200 image/webp; all 14 distinct img src URLs across six routes returned 200 image Content-Type and no img tag uses /_next/image. Claude reports rendered selection at 1728/DPR2. Narrow/wide and DPR 1/2 coverage remains incomplete | Complete required rendered selection/proportions checks; verify authorized preview; new portraits in 02 |
| Q-004 | PASS (guard logic/source); NOT_RUN (required interactions/remote) | Modifier/primary and missing-target guards are present in all affected callers; Claude reports synthetic-event tests and a CSS-forced desktop menu. These do not establish actual mobile registration, keyboard order/activation, new-tab outcomes or completed smooth scrolling. Desktop login remains same-tab. R4 (2 Sep 2026): the shared `isPlainActivation` guard now also reads the activating anchor's `currentTarget.target` and intercepts only absent/empty or `_self`; `_blank`/`_parent`/`_top`/named targets fall through. Applied through the shared guard for every fragment handler and the Header home-logo handler. Verified by focused direct checks (12-case guard matrix) plus the earlier browser synthetic-event run; built HTML confirms no fragment anchor carries a `target`, so this is latent hardening | Actual narrow/mobile pointer and keyboard checks with unmodified responsive CSS on the authorized preview; focused R4/R5 review complete |
| Q-005 | PASS | Phase 01: active Tailwind/PostCSS pair confirmed by reversible probe, then `tailwind.config.ts` + `postcss.config.mjs` deleted; theme + autoprefixer preserved. Broken `Inter-Italic.ttf` `@font-face`, dead `'CustomFont'` rule and unused Geist loading removed; no rendered typographic change (screenshots). Build + typecheck pass; no route/offer change. R5 (2 Sep 2026): transitive dev-dependency advisories cleared within existing ranges; one intentional implementation of each primitive is unaffected | Complete |
| Q-006 | PASS (local); NOT_RUN (remote) | Phase 01: all six routes HTTP 200 on direct load and refresh over `wrangler pages dev out`, incl. nested thank-you; client-side navigation to the three historical routes renders with no full reload and no `?refreshed=true` (temporary fixture, removed); homepage link set unchanged | Verify same routes on authorized preview |
| Q-007 | NOT_RUN | Reference source inspected; no live visual comparison | Phase 02 comparison |
| Q-008 | NOT_RUN | Theme behavior not implemented | Phase 02 |
| Q-009 | NOT_RUN | No redesigned responsive UI | Phases 02–03 |
| Q-010 | NOT_RUN | Existing semantic issues located | Verify affected UI during redesign |
| Q-011 | NOT_RUN | Canonical copy prepared, not implemented | Phases 02–03 |
| Q-012 | NOT_RUN | Owner/PDF pricing captured | Phase 02 |
| Q-013 | NOT_RUN | Contact data confirmed; both supplied PNG portraits inspected, not yet integrated | Phase 02 rendering and contact checks |
| Q-014 | NOT_RUN | Global canonical finding from source | Phase 03 |
| Q-015 | NOT_RUN | No performance benchmark run | Record comparable baseline and redesign evidence |
| Q-016 | PASS (local); NOT_RUN (remote) | Phase 01: GTM `GTM-NQDZKVLF` inline snippet + `<noscript>` iframe in every built route; Quanda `beforeInteractive` `<Script>` retained; `/zdravotnici` YouTube embed unchanged; no duplicate trackers. Only the unused Geist import was removed from `app/layout.tsx` | Inspect script requests on authorized preview; recheck 02–04 |
| Q-017 | NOT_RUN | Calendly account/event pending | Owner input before phase 04 |
| Q-018 | NOT_RUN | No implemented phase to hand over | Phase 04 |

## Follow-up from the implementation review

The current build/typecheck/HTTP successes remain valid. Codex independently verified generator rejection of missing source, wrong registry dimensions and a width above the original in an isolated fixture (exit 1 as intended for all three). No application source was changed for those checks.

R4 and R5 were applied and passed Codex's focused review on 2 September 2026. R4 completed the Q-004 non-self-target clause in the shared `isPlainActivation` guard (verified against the actual module, including currentTarget versus a nested target; no current fragment caller supplies a non-self target). R5 resolved the seven confirmed transitive development-dependency audit findings within existing ranges via `npm audit fix` without `--force` — `package.json` and selected direct versions unchanged. Claude reports successful `npm ci`; Codex independently reran `pages:build` / `typecheck` / `npm audit --json`, all exit 0, audit = 0 vulnerabilities. The remaining `glob@10.5.0` deprecation notice does not establish an unresolved advisory in this audit snapshot. No further full /recheck is requested without a concrete new risk. Actual mobile/keyboard/scroll/viewport/DPR and Cloudflare evidence remain open; next is the prepared preview publication after owner authorization and settings confirmation. No quality definition has changed.

## Inputs, exceptions and limits

Owner-supplied portraits, Calendly configuration and the historical-route indexing decision have the owners and timing recorded in brief.md. Preview validation needs actual branch publication and dashboard configuration; an unavailable preview must remain explicitly unverified.

The viewport set, selected contrast checks, dark default and reduced-motion behavior are project choices. They are not a claim that all accessibility or legal requirements have been audited. There is no promised Lighthouse score, search ranking or measured field performance at this stage. Do not create additional audits merely to fill the area catalog.

The explicit cookie/GTM exclusion overrides the original PDF suggestion. No other exception has been approved. Material changes to these criteria require the owner's decision, not only a review-plugin recommendation.
