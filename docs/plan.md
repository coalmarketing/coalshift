# Coalshift — Plan and handoff

## Approved scope

See [brief](brief.md), [content](content.md), [design reference](design-reference.md) and [quality profile](quality.md). The owner approved the following five phases. On 3 September 2026 Phase 03 was accepted; the new cleanup/release Phase 04 precedes the renumbered Calendly Phase 05. Initialization and planning review are workflow steps, not additional phases.

## Phases

| ID | Phase | Status | Dependencies | Expected outcome |
| --- | --- | --- | --- | --- |
| 01 | [Preview and technical foundation](Phases/01-preview-and-technical-foundation.md) | done | Initialization and planning review | Coherent static build, patched dependencies, working images and mobile CTA, documented preview setup |
| 02 | [Homepage and design](Phases/02-homepage-and-design.md) | done | Accepted 01; supplied portraits | Reference-faithful blue design, working homepage controls, monthly pricing/contacts, two Waulter legal pages and healthcare redirect |
| 03 | [Pages and SEO](Phases/03-pages-and-seo.md) | done | Accepted 02; post-recheck corrections reviewed and accepted by Jakub 3 Sep 2026 | Public references page, four-route SEO and 301s from retained legacy URL sources to `/` |
| 04 | [Cleanup, optimization and release](Phases/04-cleanup-optimization-and-release.md) | ready | Accepted 03; `/replan` (6 subagents) complete 3 Sep 2026; local cleanup stage A + `/recheck`, then release stage B | Concise current documentation, purposeful code/assets, reviewed Git integration and verified Cloudflare production deployment |
| 05 | [Calendly and integrations](Phases/05-calendly-and-integrations.md) | planned | Completed 04; owner finalizes integration scope and supplies event/organizer | Approved Calendly integration and any separately agreed additions |

Allowed status values: `planned`, `ready`, `in_progress`, `in_review`, `blocked`, `done`. This table is the only source of phase status. `ready` does not start implementation. Only Codex records `done` after review and Jakub's acceptance.

## Quality mapping

Requirements are defined once in [quality.md](quality.md). A later phase rechecks only affected earlier behavior.

| Phase | Requirements |
| --- | --- |
| 01 | Q-001, Q-002, Q-003, Q-004, Q-005, Q-006, Q-016 |
| 02 | Q-003, Q-004, Q-006, Q-007, Q-008, Q-009, Q-010, Q-011, Q-012, Q-013, Q-014 (new legal pages only), Q-015, Q-016 |
| 03 | Q-004, Q-006, Q-007, Q-008, Q-009, Q-010, Q-011, Q-014, Q-015, Q-016 |
| 04 | Q-001–Q-016, Q-018; Q-005 expanded to requested cleanup, earlier UI/content requirements apply as regression protection |
| 05 | Q-017; Q-008, Q-009, Q-010, Q-013, Q-015, Q-016, Q-018 for affected integration behavior; other IDs only where touched |

## Current handoff — Phase 04 planning review complete; stage A ready, 3 September 2026

- **Owner decision:** Jakub accepted Phase 03 and explicitly requested repository/documentation/code cleanup followed by commit, push, merge to the main branch and Cloudflare production deployment. Phase 04 contains that work; Calendly moved to Phase 05.
- **`/replan` complete and accepted with focused Codex corrections — 3 September 2026.** Six Claude Code review agents found no blocker and strengthened the safe two-stage structure. Accepted: an A0 local checkpoint commit before destructive cleanup; a temporary backup only for untracked historical files scheduled for deletion; explicit extraction/mapping before document removal; preservation of every honest `NOT_RUN`/`BLOCKED` state; expanded usage-verified dead-code/asset checks; clean-build and diff-driven regression gates; a non-mutating A5 fingerprint; exact fast-forward release mechanics; current production-branch verification; deployment evidence and a concrete rollback path. Corrected scope creep: rendered application data/components remain the canonical copy source, while `content-and-seo.md` stores concise rules/facts/limitations and exact source pointers; unused logo variants are removed under the owner's existing cleanup instruction; no repeat publication sign-off is required because Jakub already accepted Phase 03 for production and authorized the Phase 04 release; a Cloudflare cache purge occurs only if stale-edge evidence requires it; exact runtime deployment IDs live in the immutable Release Report instead of causing a post-release documentation commit. Phase 04 is ready for local stage A.
- **Verified Git state:** `redesign`/`origin/redesign` at `68fdbcb`; `master`/`origin/master` at `2aaef8e`; `origin/cloudflare-deploy` at `0138dbb` (no local branch). `master` and `cloudflare-deploy` are both ancestors of `redesign` HEAD — fast-forwardable today; re-verify at stage A start and before every stage-B push. All accepted Phase 02 corrections + Phase 03 work remain uncommitted and are the accepted starting tree.
- **Next work:** stage-A local cleanup assignment (A0–A5 in the phase file), then the bounded `/recheck` (cleanup delta only). Stage B is a separate continuation prompt naming the reviewed version by its A5 fingerprint. No commit that leaves `redesign` beyond the A0 checkpoint, no push, no deploy during stage A. Node: `.nvmrc` pins 24.20.0 (local 24.15.0) — install the pin for the validation build or record the deviation.
- **Cleanup findings:** ~66 MB under `docs/references/` (committed historical Phase 02 artifacts — deleting it shrinks the working tree only); ~18 top-level `phase-02-*`/`phase-03-*` correction/prompt files; `brief.md`/`content.md`/`design-reference.md`; `Phases/01–03`; stale comments; verified-dead `Button.tsx` + Tailwind tokens/keyframes + default public assets + two image-registry entries. Evidence-based removal per the phase file, not a blanket deletion.
- **Preservation:** accepted design/content/SEO, GTM/Waulter, current portraits and runtime behavior remain unchanged. The three legacy route implementations and their required dependencies remain in source; all six URLs keep 301 to `/`. Keep the current four-public-route sitemap and both healthcare 301s.
- **Evidence and content limits:** acceptance and publication authorization do not turn old NOT_RUN results into PASS, labelled illustrative metrics into measured results, or testimonial provenance into independent verification. Carry those facts into the concise handoff; retain visible illustrative labels and unchanged attribution. Phase 04 performs actual preview/production checks. Calendly remains intentionally absent until Phase 05.
- **Documentation preparation:** new Phase 04; former Phase 04 replaced by Phase 05; plan/brief/quality/agent context updated. The large historical documents remain temporarily as extraction inputs for Claude's cleanup. No permanent archive or standalone correction-prompt file is required.
- **Learning:** this is a project-specific documentation lifecycle/cleanup decision. Existing shared maintenance and release guidance covers it; no shared workflow changes were made.

## Phase 03 post-recheck handoff (historical; superseded by the accepted current handoff above)

- **Owner acceptance:** after Codex's focused review of I1–I8, Jakub explicitly accepted Phase 02. Codex records the phase as complete in the table above. This acceptance covers the reviewed homepage/design result including F/G/R/H1, coalfamily tooltips and I1–I8. No further full `/recheck` is needed without a new concrete risk.
- **Accepted version:** `/Users/jakubtesarik/Programování/coalshift`, branch `redesign`, HEAD `68fdbcbd2562f73db91a683315d259c4fca4ef04`, plus all reviewed uncommitted Phase 02 corrections and their documentation. Complete review baseline remains `8f1db89f2e01dad80eb6678a5cbd0df0187b797a`. Acceptance does not mean those corrections have been committed or published.
- **Evidence retained:** Claude's fresh successful typecheck/build/export and Chrome 152 matrix, Codex's source/diff review and independent passing typecheck, and Jakub's acceptance. Codex's attempted build repeat remains environment-blocked by Turbopack local-port denial; it is not an additional PASS. Physical-device/screen-reader/Back-position checks, real Waulter policy population, preview version verification, illustrative-metric confirmation and later performance/Calendly work retain their existing limitations and follow-up points in quality.md. Acceptance does not turn `NOT_RUN` into `PASS` or approve production release.
- **Final owner route decision:** `/reference` is a public/indexable subpage in the accepted design, gains **Reference** in footer **Navigace**, uses a production self-canonical and enters the sitemap. Source for `/registrace`, `/wait-list` and `/wait-list/thank-you` remains in the repository for possible reactivation, but the three routes are unavailable as pages: all six slash/no-slash forms return HTTP 301 to `/`. They have no active internal links and no sitemap entries. `/`, `/reference`, `/gdpr` and `/cookies` are the complete public sitemap set. `/zdravotnici` remains only the accepted 301 to `/#industries`.
- **Phase 03 boundary (final owner clarification after `/recheck`):** active public design/content work is the accepted homepage plus `/reference`. Keep the already rewritten legacy bodies in source; do not roll them back, delete them, render-review them or polish their copy, metadata, breadcrumbs, link styling/targets or component names. Their only current public acceptance criteria are six exact 301 responses to `/`, no active internal links and sitemap exclusion. Retained `noindex` body metadata is defense-in-depth/history, not a substitute for redirects.
- **Planning review accepted with Codex corrections:** `/replan` for [03-pages-and-seo.md](Phases/03-pages-and-seo.md) completed 3 September 2026 with seven parallel review subagents. Codex confirmed the route matrix and the reported source defects, then closed the remaining scope choices: public routes use text-only Open Graph metadata and Twitter `summary` with no image; legacy `Button.tsx` and Tailwind tokens stay untouched; a branded 404 is outside Phase 03; sitemap entries omit fabricated `lastModified` values; and the shared subpage-intro extraction preserves semantics and observable behaviour rather than requiring byte-identical RSC output. The allowlist explicitly includes the metadata-only `/gdpr` and `/cookies` page edits required by the shared SEO source. The phase now has locked decisions, a coherent shared-file allowlist, LOCAL vs DEPLOYMENT-ONLY validation, `app/lib/seo.ts` as the route-data source, and explicit copy, landmark, theme and contrast requirements.
- **Phase 03 implemented locally — 3 September 2026 (Claude Code).** Branch `redesign`, HEAD unchanged `68fdbcbd2562f73db91a683315d259c4fca4ef04`, all work uncommitted; no push/deploy/external change. `npm run typecheck` + `npm run pages:build` exit 0; verified in isolated headless Chrome 152 against the served `out/`. New: `app/lib/seo.ts` (single route/SEO source), `app/sitemap.ts` (exactly 4 URLs), `app/robots.ts` (allow-all + sitemap, no `Disallow`), `app/components/ui/SubpageIntro.tsx`, `app/components/reference/ReferenceList.tsx`, `app/components/legacy/LegacyPage.tsx`. Modified: `app/layout.tsx` (canonical/OG/Twitter removed — GTM/noscript/bootstrap/font-preloads/viewport untouched), `app/page.tsx` + `app/gdpr/page.tsx` + `app/cookies/page.tsx` (own metadata from `seo.ts`; legal bodies/Waulter IDs unchanged), `app/reference/page.tsx` (redesigned to shared shell: real H1, sibling landmarks, `#main`, `<details>` quote disclosure, no stars/logo, both themes ≥4.5:1), the three dead-route pages + `LegacyPage` (copy rewrite to `content.md`, vykání, `noindex, follow`, `/registrace`↔`/wait-list` split, dark-safe), `app/components/LegalPage.tsx` (consume `SubpageIntro`), `app/components/Footer.tsx` (one `Reference` `<li>`), `next.config.ts` (`trailingSlash: false`). Deleted: `References.tsx`, `WaitList.tsx`, `WaitListRegistration.tsx`. `Button.tsx` + legacy Tailwind tokens left untouched (now unused; deferred cleanup). Homepage canonical/OG/sitemap render as bare `https://coalshift.cz` (Next resolver returns the root origin; slash-consistent). Phase 03 → `in_review`. Deployment-only checks (Q-001 preview, `X-Robots-Tag`, production canonical/crawler, real Waulter injection, `_redirects` `Location` fragment) stay BLOCKED/NOT_RUN.
- **Permissions:** the implementation changed Phase 03 source within the allowlist plus local docs/evidence. No commit, push, deploy, external submission or external/Cloudflare/GTM change. Existing uncommitted Phase 02 work is intact and separately identified; HEAD still `68fdbcb`.
- **Codex assessment — 3 September 2026:** source/diff inspection, `git diff --check` and an independent `npm run typecheck` on the pinned Node 24.20.0 pass. Parsed the existing seven-route export: correct route metadata, three `noindex, follow` pages without canonical/social metadata, exactly four public sitemap URLs, allow-all robots, one H1 and a focusable `#main` per route, sibling landmarks, the footer Reference link and no dead-route links. The three quotes, author names and roles match their pre-port source exactly. This inspection did not rebuild the export or repeat Claude's browser matrix; Claude's reported build/browser evidence remains separately attributed.
- **Post-recheck decision:** the review found no critical issue and independently passed the build/typecheck plus the public SEO output. Accept two small `/reference` corrections: `line-clamp-4` → locked `line-clamp-3`, and replace the newly authored “zkušenosti uživatelů” intro with the approved neutral “Přečtěte si zkušenosti s plánováním směn v coalshiftu.” Add six exact Cloudflare Pages 301 rules to `/` for the retained legacy URL families. Ratify the existing neutral-200/800 avatar circle as a justified adaptation because neutral-100/900 would merge with this implementation’s card interior. The native `<details>/<summary>` composition exactly follows coalios and has no demonstrated WCAG failure; keep it. The proposed SEO invariant, import organization, legacy-route polish/DRY/naming/link-target changes and optional build assertion do not correct a current failure and are not assigned.
- **Technical clarifications:** accept the consistent bare homepage origin `https://coalshift.cz` in canonical/OG/sitemap for this project's Next 16.3.4, `metadataBase` and `trailingSlash: false` combination. Root forms with/without `/` are equivalent; this does not justify a universal claim that a trailing slash is impossible through configuration. Cloudflare's default `X-Robots-Tag: noindex` concerns preview deployments, not a proven rule for every `*.pages.dev` hostname. An authorized preview can later verify its own exclusion, build and redirects; production headers, canonical resolution and crawler/indexing outcomes remain separate deployment checks. Do not assume switching branches guarantees their outcome.
- **P03-C1–C3 implemented locally — 3 September 2026 (Claude Code).** Branch `redesign`, HEAD unchanged `68fdbcb`, uncommitted, no push/deploy/external change. Changed exactly: `app/components/reference/ReferenceList.tsx` (`line-clamp-4` → `line-clamp-3`), `app/reference/page.tsx` (intro → `Přečtěte si zkušenosti s plánováním směn v coalshiftu.`), `public/_redirects` (healthcare rules byte-preserved + six legacy 301-to-`/` rules appended). `npm run typecheck` + `npm run pages:build` exit 0. Frozen source SHA-256 identical before/after for `app/registrace/page.tsx`, `app/wait-list/page.tsx`, `app/wait-list/thank-you/page.tsx`, `app/components/legacy/LegacyPage.tsx` — none touched. On `wrangler pages dev out` (4.128.0, "Parsed 8 valid redirect rules"): all six legacy URL forms → **301** `Location: <origin>/` without following (redirect wins over the still-generated `.html` assets), one hop, no loop; `/` → 200; healthcare → 301 `/#industries`; public routes + `/sitemap.xml` + `/robots.txt` → 200; sitemap still the four public URLs; no internal link to any legacy family. `/reference` at 320/390/768/1024/1280/1440 both themes: 3 collapsed visual lines, full quote intact on expand, pointer+keyboard disclosure, visible focus, exact new intro, no overflow; quote/name/role strings and the neutral-200/800 avatar unchanged. Q-006/Q-007/Q-010/Q-011/Q-014 get an appended P03-C evidence segment; no earlier result overwritten. P03-PRE-01 resolved; P03-PRE-02 ratified. Production 301 `Location` headers on the deployed domain remain deployment-only BLOCKED. Phase 03 stays `in_review`.
- **Codex post-correction assessment — 3 September 2026:** current source matches P03-C1–C3, all four frozen-source SHA-256 values match the report, `git diff --check` passes and `out/_redirects` contains the required eight rules. Codex independently started Wrangler 4.128.0 against the fresh export and requested every URL without following redirects: all six legacy forms returned 301 with exact `Location: /`; both healthcare forms returned 301 with exact `Location: /#industries`; `/` returned 200. No loop or scope regression was found. Claude's successful typecheck/build and 12-view `/reference` browser matrix retain their own attribution.
- **Next action:** Jakub accepts or rejects Phase 03 based on the completed result. No further Claude correction or repeated complete `/recheck` is indicated. Acceptance may move Phase 03 to `done`; it does not authorize commit, push, deployment or Phase 04.
- **Learning review:** existing shared technical-SEO guidance already covers URL consistency and the distinction between discovery, crawler access and indexing. The resolver observation is configuration-specific; keep it in this project. No shared-workflow rule was added.

## Phase 02 post-recheck corrections I1–I8 (historical)

- **Complete review result, 3 September 2026:** the full Phase 02 `/recheck` covered baseline `8f1db89f2e01dad80eb6678a5cbd0df0187b797a`, commit `68fdbcb`, all current tracked corrections and four untracked task documents. Fresh `typecheck` and `pages:build` passed; the served export passed the reported route, responsive, theme, browser, header, content, integration and structural checks. The review found no critical, data or security defect and confirmed every current plan item implemented without scope creep.
- **Phase decision:** Phase 02 stays `in_review`. Q-009 and Q-010 are not fully accepted because the review reproduced fragment-offset/semantics, reduced-motion, mobile-overlay focus, skip-link and contrast defects. Jakub's prior visual acceptance remains valid for the current design and H1/tooltip behavior; it does not waive these findings.
- **Codex assessment:** accepted corrections are I1–I8 in [the post-recheck assignment](phase-02-recheck-corrections.md): a single fragment offset; native-equivalent URL/history/focus with reduced-motion behavior; contained mobile-menu focus/background inertness; focusable `#main` skip destinations; readable illustrative labels; visible selected-tab state; sufficient non-parent family-icon contrast; and coherent first-paint theme-toggle state.
- **Finding reclassification:** the secondary CTA outline is not assigned as a defect because its visible high-contrast label and arrow identify the control; W3C SC 1.4.11 does not require a contrasting hit-area boundary in that case. Remaining minor observations are optional/later work and are not part of this correction. Waulter delivery, real-device evidence, preview build, illustrative metric confirmation, retained-page work/SEO, Calendly and CWV retain their recorded status.
- **Scope and permissions:** target `/Users/jakubtesarik/Programování/coalshift`, branch `redesign`, HEAD `68fdbcbd2562f73db91a683315d259c4fca4ef04`, tracking `origin/redesign`; all subsequent work is uncommitted. Local I1–I8 implementation, evidence and documentation are authorized. No commit, push, deployment, external configuration change, complete `/recheck` or Phase 03 is authorized.
- **I1–I8 implemented and locally verified — 3 September 2026.** Applied on `redesign` over the uncommitted recheck working tree; HEAD unchanged at `68fdbcb`; nothing committed, pushed or deployed. This correction's hunks: `app/globals.css` + `app/components/ui/Section.tsx` (I1); `app/lib/smoothScroll.ts` + `app/components/Header.tsx` (I2, the I3 modal effect, the I7 icon colour); `app/page.tsx` + `app/components/LegalPage.tsx` (I4); `app/components/home/FunctionsBrowser.tsx` (I5, I6); `app/components/theme/ThemeToggle.tsx` (I8); evidence in `docs/phase-02-recheck-corrections.md`, `docs/Phases/02-homepage-and-design.md`, `docs/quality.md`, this file. Pre-existing uncommitted G/R/H1/tooltip work (`Header.tsx`, `FunctionsBrowser.tsx`, `globals.css`, `ThemeToggle.tsx`, `Footer.tsx`, `home/{Hero,Capabilities,Industries}.tsx`, `docs/agent-instructions.md`, task docs) is preserved and untouched outside those hunks. `npm run typecheck` and `npm run pages:build` exit 0; verification in isolated headless Chrome 152 against a fresh `out/`. Q-009 and Q-010 recorded local PASS for the corrected behaviour; Q-008 records I8. Physical-device touch/keyboard, a screen-reader pass over the new dialog/inert semantics, and browser-native Back scroll restoration remain NOT_RUN. Phase 02 stays `in_review`.
- **Codex review of I1–I8 — 3 September 2026: accepted for owner review.** The report matches the current uncommitted source and the assigned correction boundaries. Codex inspected the fragment/history/focus helper, modal-menu inert/focus handling, skip targets, contrast classes and first-paint theme-toggle model; `git diff --check` and an independent `npm run typecheck` pass. Codex's independent `npm run pages:build` retry could not start Turbopack's CSS helper process because this review sandbox denied local port binding (`Operation not permitted`), including after the permitted escalation retry. This is an environment limitation rather than a reproduced application failure; Claude's fresh successful build/export and Chrome 152 matrix remain the build/browser evidence. No new actionable defect or repeat full `/recheck` is required. Existing physical-device, screen-reader, Back-position, Waulter/provider, preview and later-phase limitations keep their recorded status.
- **Next action:** Jakub reviews the small visible I5–I8 states and accepts Phase 02. Only then may Codex mark Phase 02 `done` and prepare the already-approved Phase 03 planning decision about historical/registration-route indexing. No Claude work, commit, push, deploy or Phase 03 implementation starts automatically.

## Complete Phase 02 recheck handoff (historical; superseded above)

- **Owner acceptance, 3 September 2026:** Jakub reviewed the completed H1 height correction and coalfamily tooltip correction, confirmed that the appearance and behavior work correctly, and requested the complete Phase 02 `/recheck`. This accepts the current visual/functional result for review; the Phase 02 status remains `in_review` until the full review is assessed. Phase 03 has not started.
- **Verified target and baseline:** `/Users/jakubtesarik/Programování/coalshift`, branch `redesign`, HEAD `68fdbcbd2562f73db91a683315d259c4fca4ef04`, tracking `origin/redesign`. Review against the accepted Phase 01 parent `8f1db89f2e01dad80eb6678a5cbd0df0187b797a`. Include the complete committed Phase 02 change, all staged/unstaged G1–G4 / R1–R6 / H1 / tooltip changes, and all untracked phase assignment/prompt files. No unrelated work has been identified. Documentation/evidence and removed files belong to the scope; generated `.next`, `out`, image derivatives and local Wrangler state are verification artifacts. Do not use `origin/redesign` or only the last local diff as the baseline.
- **H1 source and evidence accepted for recheck:** inactive panels use native `hidden` and do not size the component; the shared grid stack and CTA `mt-auto` are removed, with normal `gap-10 lg:gap-12` flow. Codex inspected current source and Claude's saved first-tab captures at 1728/light and 1440/dark; the large empty field is gone. Claude reports all five tabs across 320–1728 in both themes, stable column widths, natural compact height and working keyboard selection. The owner subsequently confirmed the visual/functional result. A forced zero-pixel height delta is no longer an acceptance criterion.
- **Tooltip correction accepted for recheck:** `Header.tsx` family bar is `relative z-20`; tooltips use `z-50`, a smaller vertical offset and padding, and retain `pointer-events-none`. The full mobile menu remains above the entire header. Claude reports all five tooltips at 1440/1728 in both themes on hover/focus with no viewport clipping or overlap with the theme toggle/login CTA. Codex confirmed the source changes; Jakub accepted the visible result.
- **Evidence boundaries:** both Claude reports say `npm run typecheck` and `npm run pages:build` passed on the local worktree. Codex's current source/diff check and `git diff --check` passed; Codex did not independently rerun the build or browser matrix during this handoff. Owner review is separate evidence, with no specific device/viewport matrix claimed. Historical unperformed checks are not automatically converted to PASS.
- **Current requirements:** use the Phase 02 acceptance criteria and Q-003, Q-004, Q-006–Q-016 (Q-014 only for the new legal pages). Current content/quality plus F1–F6, G1–G4 / R1–R6, H1 and the tooltip correction supersede conflicting historical checklists. In particular: two metric cards instead of the phone; compact active-only height; vertical tabs at every width; centre-out link motion; no trial CTA in any header; theme before the sole desktop login CTA; closed mobile header = logo/theme/hamburger. Preserve all other accepted design/copy/pricing/contact/integration/route behavior.
- **Publication and remaining inputs:** no new commit, push or deployment is authorized by this review. HEAD remains `68fdbcb`; all subsequent corrections are local and uncommitted. The preview URL/build for that commit is still unverified; the older `6011b67f` preview belongs to `8f1db89`. Real Waulter policy population on an appropriate published domain remains unverified; no cause or provider correction is confirmed. Keep the six illustrative metrics visibly labelled and confirm/replace them before final publication. Historical-route indexing and Calendly remain later-phase decisions; retain other outstanding quality evidence with its actual status.
- **Next action:** Jakub sends the review context followed by `/recheck /Users/jakubtesarik/Programování/coalshift/docs/Phases/02-homepage-and-design.md` to Claude Code. Review the full scope in manageable batches if needed; do not truncate it to a plugin file limit. Return **Implementation Review Report — Phase 02** in English, with coverage, evidence and deduplicated findings. Review only: no fixes, publication, shared-workflow edits or Phase 03. Codex evaluates the returned report before the next assignment.

## Previous H1 handoff (historical; superseded by the current handoff above)

- **[G1–G4 / R1–R6](phase-02-responsive-navigation-corrections.md) accepted by the owner; [H1 compact-height correction](phase-02-browser-height-correction.md) implemented locally by Claude Code and left `in_review` (see the dated log entry below).** The grid-stack / `lg:invisible` / `mt-auto` strategy in `FunctionsBrowser.tsx` is removed: inactive panels are native `hidden` (`display:none` at every width, one DOM copy each, not focusable), the CTA is back in normal flow with the reference `gap-10 lg:gap-12`, and the frame sizes to the active panel — wide-desktop outer frame ≈593 px (was ≈920 px), facts-to-CTA gap 40/48 px (was hundreds), the empty field gone; widths stay identical across tab selection. `typecheck` + `pages:build` pass. Phase 03 not started.
- **Verified starting state before this documentation:** `/Users/jakubtesarik/Programování/coalshift`, branch `redesign`, clean HEAD `68fdbcbd2562f73db91a683315d259c4fca4ef04` (`feat: complete phase 02 homepage redesign`), tracking `origin/redesign`. The current documentation changes are intentional uncommitted task context. Complete Phase 02 recheck baseline remains parent `8f1db89f2e01dad80eb6678a5cbd0df0187b797a`. Reference `/Users/jakubtesarik/Programování/coalios`, `main`, `f3d727dc32dd9cd04493915f512b3375ef7d0cf4`, read-only; do not inspect coalios-manual.
- **Publication record:** owner-authorized commit `68fdbcb` was pushed normally to `origin/redesign`; `npm run typecheck` and `npm run pages:build` passed immediately before publication. An automatic Cloudflare preview was expected, but its build and exact URL were not verified in the publication report. The earlier `6011b67f` preview belongs to commit `8f1db89`, not the current Phase 02 result.
- **Confirmed H1 cause:** `FunctionsBrowser.tsx` uses `flex-1 lg:grid`; at `lg+` every panel occupies one grid cell and inactive panels become `lg:flex lg:invisible`. They still participate in grid sizing, so the tallest of all five panels sets one oversized row. Flex stretching plus the visible card's `mt-auto` wrapper turns that excess into the owner-visible empty field before the CTA. Coalios uses native `hidden`, so only the active panel sizes the component and its CTA remains in normal flow.
- **Owner-required H1 result:** active-only natural compact height at every viewport; no large blank field; normal source-like content-to-CTA gap; consistent main/metric widths across tab selection. Natural height differences from text wrapping are accepted and must not be hidden by a fixed/min height, measuring clone, absolute overlay or nested scroller.
- **Preserve:** every successful G1–G4 / R1–R6 result plus F1–F6 content/surfaces, two metric cards per topic, visible illustrative labels, vertical tabs, audience plus, pricing glow/Lite state, brand highlighter, scoped icon accent, approved Czech copy, prices/bands/VAT/page trial CTAs, contacts/portraits, legal containers, GTM/Waulter, Quanda removal, redirects and hidden routes.
- **Current evidence scope:** Codex inspected the current diff, the reference `desktop-screen.njk`, Claude's report and both owner screenshots. This confirms the layout cause and invalidates the prior absolute-height acceptance; it is not evidence that H1 has already been implemented.
- **Required H1 evidence:** fresh typecheck/static build/served `out`; all five tabs at 1440/1728 in both themes with panel display state, outer height per tab, main/metric widths, facts-to-CTA gap and screenshots; focused no-overflow/reflow at 320/390/768/1024/1280 and one real keyboard switch.
- **Permissions consumed:** local G/R **and H1** implementation, docs and verification are done; **Correction Report — Phase 02, G1–G4 and R1–R6** and **Correction Report — Phase 02, H1 FunctionsBrowser height** returned in English. All work uncommitted (HEAD still `68fdbcb`). No commit, push, deploy, Cloudflare/GTM/Waulter change, external submission, `/recheck` or Phase 03 was performed. Phase 02 stays `in_review` (overall `PARTIAL`).
- **Ready for Codex review of the H1 diff, then the complete Phase 02 `/recheck` against `8f1db89`.** The accumulated implementation is everything in commit `68fdbcb` plus the uncommitted G/R diff plus the H1 change (`home/FunctionsBrowser.tsx` only, + `docs/phase-02-browser-height-correction.md` / `quality.md` / `plan.md` / `Phases/02` evidence).
- **Known separate limits:** real Waulter population on an appropriate published domain remains unverified and no provider correction is assigned. Illustrative metrics `planning-time` 50 %, `planning-month` 20 h, `team-search` 40 %, `absence-admin` 30 %, `export-time` 60 % and `reporting-time` 50 % must remain visibly labelled and be confirmed/replaced before final publication. Historical-route indexing and Calendly remain later decisions.
- **Next action:** Jakub sends the [H1 prompt](phase-02-browser-height-correction-prompt.md) to Claude. Claude completes only H1 and returns **Correction Report — Phase 02, H1 FunctionsBrowser height** in English. Codex then reviews the focused diff/report and prepares the complete Phase 02 `/recheck`; Phase 03 does not start automatically.

## Owner acceptance and deferred verification — 2 September 2026

The owner opened the exact phase 01 preview, reported that it appeared to work as expected, and explicitly requested moving to phase 02 while addressing remaining detailed checks later. Codex accepts the technical foundation on that basis and records phase 01 as done **with the following owner-approved deferrals**. This is not an assertion that the unperformed checks passed or that production is approved.

| Carried item | Evidence remains | Next responsible verification |
| --- | --- | --- |
| Q-003 actual narrow/wide image rendering and DPR 1/2 selection | NOT_RUN for the missing combinations; metadata and HTTP checks pass | Phase 02: real phone mockup and new portrait rendering at the agreed viewports; close any still-open combination before the phase 04 release handoff |
| Q-004 real mobile pointer/keyboard, modified-link outcomes, menu focus and completed smooth-scroll landing | NOT_RUN for actual interactions; source/synthetic-event checks pass | Phase 02: verify the redesigned shared navigation together with Q-009/Q-010; recheck affected route bodies in 03 and account for any remaining item in 04 |

No additional test category is introduced. Keep the existing 320/390/768/1440 CSS px and theme requirements. Known automation limitations must be recorded honestly; do not repeatedly run the unchanged fixed-viewport harness or infer that an unobserved behavior is defect-free. Complete the new design and all feasible checks, then report any remaining owner-assisted checks against that version. The owner’s general preview inspection is recorded separately from a scripted keyboard/mobile matrix.

This decision supersedes earlier phase 01 instructions that made those missing interaction checks a prerequisite to proceeding. Their historical evidence remains below. No common [[AI]] workflow rule is changed.

## Implementation review — 2 September 2026

Baseline: `0138dbb2073c31e72a9a61c987ee02a8ae9ce4dd`, branch `redesign`, complete uncommitted phase 01 scope including new source/configuration files. The existing `out/` was inspected as Claude's prior artifact, not represented as a fresh successful Codex build. Codex also reproduced its image attributes directly with the installed Next 16.3.4 `getImgProps`, current loader and configuration; Sharp confirmed actual file dimensions.

### R1 — Responsive-image descriptors (Q-003, local FAIL)

- The portrait emits `adamec-240.webp 32w` through `128w`, `adamec-320.webp 256w`, `adamec-480.webp 384w`, and `adamec-640.webp` for widths through `3840w`. Actual file widths are 240, 320, 480 and 640. Next emits its configured requested width as the descriptor even when the loader rounds/caps the returned asset.
- The phone emits the same 426px asset as both `1x` and `2x` and never offers its generated 213/320px variants. The ceiling/cap rule in the original plan was incomplete as a full responsive-image integration; preserve the native resolution limit, but ensure the final candidate metadata describes the actual files.
- Correct candidate generation and `sizes` for both raster uses. A small registry-driven rendering helper inside this Next application is an acceptable implementation detail if needed; preserve Sharp/WebP generation, browser-safe code, dimensions, existing SVG behavior and static export. Do not upscale originals or replace the pipeline with raw PNG delivery merely to make a check pass.
- Verify final emitted candidates against decoded file widths, then actual browser selection at narrow/wide viewports and DPR 1/2. `naturalWidth` is density-corrected CSS width, not necessarily file pixel width; a density-halved value alone is not evidence of a broken automation bridge. Do not dismiss an unexplained zero/currentSrc result without checking loading state.

Evidence basis: installed `next/dist/shared/lib/get-img-props.js`, current source and existing HTML, Sharp metadata, [HTML srcset requirements](https://html.spec.whatwg.org/multipage/images.html#srcset-attributes) and [naturalWidth semantics](https://developer.mozilla.org/en-US/docs/Web/API/HTMLImageElement/naturalWidth), opened 2 September 2026. Actual browser choice remains unverified by Codex.

### R2 — Preserve default link activation (Q-004, local FAIL)

`Header`, `Footer`, `Hero`, `HealthcareHero` and `PricingSection` call `preventDefault()` for fragment links without guarding modifier keys or ensuring a destination exists before cancelling. Header's home-logo handler also intercepts modified clicks. The helper's internal missing-target return occurs too late. This violates the agreed ordinary-activation-only contract: Cmd/Ctrl-click can scroll the current page instead of opening a tab.

Centralize or consistently apply the activation guard: preserve already-handled events, non-primary/modifier activations and non-self targets; intercept only a same-document destination actually handled. Preserve existing hrefs and default navigation otherwise. Retain mobile close behavior without cancelling external registration navigation. Verify mouse/keyboard activation, modified clicks, ordinary section landing and menu focus below the large-screen breakpoint. Observe actual header clearance and fix any proven native/JS mismatch; do not assume hardcoded 72px is sufficient or introduce a menu redesign.

### R3 — Correct evidence and close the focused verification gaps

Re-run the affected pointer/keyboard and image checks in a visible foreground browser with a recorded narrow viewport (for example 390 CSS px) and wide viewport (for example 1440 CSS px). Record actual viewport, DPR, destination/new-tab outcome, menu state/focus and completed scroll landing. Source inspection, an instant-scroll substitute or a desktop-only report cannot establish those outcomes. Keep unavailable checks `NOT_RUN`/`BLOCKED`.

Correct the report's blanket claim that every login opens a new tab: desktop login intentionally remains same-tab; mobile login and the actual new-tab CTAs retain their respective targets. Retain GTM/Quanda/video and the existing commercial/copy boundaries. The real Cloudflare build (Q-002) and Q-004 preview interactions are outstanding phase 01 remote checks alongside Q-001/Q-003/Q-006/Q-016; do not postpone them silently to phase 04.

Codex verification: image generation PASS (seven WebPs, phone maximum 426×519 / 33,522 B, portrait maximum 640×640 / 32,810 B); `npm run typecheck` PASS; direct current-source image-attribute reproduction confirms R1; source review confirms R2. Build verification BLOCKED by internal-port denial after the required escalation retry; interactive verification BLOCKED by browser runtime initialization failure. These tool limitations do not justify changing the application's bundler or stack.

### R1–R3 corrections applied — 2 September 2026 (Claude Code)

- **R1:** replaced the capping `next/image` custom loader with `app/components/ResponsiveImage.tsx` (registry-driven plain `<img>`, JSON registry only) for the two raster assets; `next.config.ts` → `images: { unoptimized: true }` (SVGs/logos keep `next/image`, served as-is); `image-loader.ts` removed; per-image `unoptimized` props removed (global covers them) so `References.tsx` returned to baseline. `image-registry.json` gained native `width`/`height`; `generate-image-derivatives.mjs` now asserts registry↔decoded-source dimensions and descriptor↔file-width. Emitted `srcset` now: phone `213w/320w/426w`, portrait `240w/320w/480w/640w` — every descriptor verified against the decoded file; `sizes` layout-aligned. Browser selection at 1728/DPR2 correct within native limits; `naturalWidth` 299 for the portrait explained as the MDN density-corrected value.
- **R2:** added `isPlainActivation` / `shouldSmoothScroll` to `app/lib/smoothScroll.ts`; every fragment handler in Header (desktop + mobile nav + home-logo), Footer, Hero, HealthcareHero and PricingSection now `preventDefault`s only for an ordinary primary activation whose target exists. Synthetic-event tests confirm modifier clicks and missing targets fall through; a `metaKey` click on a mobile menu link closes the menu without scrolling the page. JS scroll targets computed exactly (`element − 72`); native `#hash` lands at `top: 72`; measured `header.offsetHeight` = 72 = `scroll-padding-top`.
- **R3:** re-verified in a focused browser. The harness renders at a fixed 1728 CSS px / DPR 2 (`resize_window` moves the OS window but not `innerWidth`; no device-emulation tool). The 390/1440 px rendered layouts, the true <1024 px hamburger layout and completed smooth-scroll *animation* (background-tab rAF throttling) are `NOT_RUN`; deterministic guard logic, scroll-target math, native fragment landing, image descriptors/selection, `inert`/`aria` menu state and anchor semantics are verified. Q-003 and Q-004 → PASS (local). The report's earlier "every login opens a new tab" phrasing is corrected: desktop login stays same-tab; mobile login and the new-tab CTAs keep `target="_blank"` + `rel="noopener"`.

Build (`pages:build`) and typecheck pass after the corrections; `npm ci` reproducible. No commit, push or deployment. Bundler and stack unchanged.

## Codex assessment of the correction report — 2 September 2026

- **R1 metadata: PASS.** Fresh exported HTML offers phone 213w/320w/426w and portrait 240w/320w/480w/640w; Sharp confirms the actual file widths. HTTP delivery passes for every variant. The registry-driven component is within the R1 implementation clarification and preserves the static Sharp pipeline. Actual narrow/wide and DPR 1/2 browser selection remains NOT_RUN where not demonstrated.
- **R2 original defects: corrected in source.** All affected callers now guard modifier/primary activation and target existence before cancelling. Claude's synthetic-event checks support this limited result. Real new-tab outcomes, keyboard activation, the native mobile menu and complete smooth scrolling remain unverified. The helper does not inspect the anchor's `target`; the explicit non-self-target clause from the correction assignment remains a review point. Current fragment callers have no non-self target, so no live failure of those callers is claimed from that omission alone.
- **R3: incomplete verification.** Do not rerun the same fixed-viewport harness merely to produce another identical report. `/recheck` can assess the whole implementation now, while the genuine narrow/mobile and pointer/keyboard checks remain prerequisites to phase acceptance. A usable browser or owner-assisted local/authorized-preview check will be needed for those outcomes.
- **Documentation: reconciled by Codex.** README no longer links the removed image-loader.ts. Current evidence separates passing source/build/HTTP checks from missing interaction checks. The earlier report's blanket new-tab claim and the old Codex build limitation are superseded. No change to quality definitions, phase boundaries or permissions.
- **Next review scope:** all 21 modified tracked files, two tracked deletions and new phase files at the current working tree. Include AGENTS.md, CLAUDE.md and docs as task context/evidence. Preserve the unused owner portraits; they are inputs for phase 02, not phase 01 implementation. Generated out/.next/derivatives and local Wrangler state are verification artifacts, not additional source changes. Recheck Git status before review; there are no phase commits or unrelated source changes to omit.

## Assessment of both implementation-review reports — 2 September 2026

The preliminary report and the actual `/recheck` found no critical live application defect. Their scope excludes neither the existing quality gates nor the current lockfile from dependency verification. Proposed refactors and extra tests are suggestions, not automatically new phase requirements. Baseline/branch/source scope are unchanged.

### R4 — Finish the existing non-self-target contract (Q-004)

`app/lib/smoothScroll.ts` guards event modifiers and target existence but does not inspect the activating anchor's target. Current fragment callers have no non-self target, so no current `_blank` failure has been established. Nevertheless, respecting such targets was already part of the R2 assignment; finish that small clause rather than replacing it with a comment.

Use the activating anchor (`event.currentTarget`, not a nested click target). Only absent/empty or `_self` targets may be intercepted; preserve `_blank`, `_parent`, `_top` and named targets. Apply the rule consistently to fragment handlers and the Header home-logo handler through the existing shared guard. Preserve the existing primary/modifier/defaultPrevented/missing-target checks and destinations. Use appropriate anchor event types. Verify the guard matrix directly and keep actual browser navigation evidence separate. A broader handler refactor is not required.

### R5 — Compatible transitive dependency fixes (Q-002/Q-005)

Codex independently ran `npm audit --json` against the current lockfile. Result: **7 affected packages, 5 high, 1 moderate, 1 low, 0 critical**, all marked `fixAvailable: true`. Lockfile entries below are `dev: true`:

| Package | Locked version | Parent/range evidence |
| --- | --- | --- |
| brace-expansion | 2.0.1 | minimatch: ^2.0.1 |
| browserslist | 4.24.4 | autoprefixer: ^4.23.3 |
| glob | 10.4.5 | sucrase: ^10.3.10 |
| minimatch | 9.0.5 | glob: ^9.0.4 |
| picomatch | 2.3.1 | anymatch/micromatch/readdirp: compatible 2.x ranges |
| postcss-selector-parser | 6.1.2 | postcss-nested/Tailwind: compatible 6.x ranges |
| yaml | 2.7.0 | postcss-load-config: ^2.3.4 |

These findings affect development/build dependencies; they do not establish a vulnerability reachable by visitors to the deployed static website. The report's description as only ReDoS/DoS is incomplete: [glob's advisory](https://github.com/advisories/GHSA-5j98-mcp5-4vw2) concerns command injection specifically through its `-c/--cmd` CLI option, not the ordinary library API. No exploit was executed and no exploited Coalshift path is claimed. Audit data, lockfile parent ranges and primary advisory details were inspected. The [brace-expansion advisory](https://github.com/advisories/GHSA-rgw5-rvv9-x895) and [Browserslist advisory](https://github.com/advisories/GHSA-c83g-rgw3-j3cx) were also opened.

Resolve the listed findings through compatible transitive updates within the existing dependency contract. Inspect the actual lockfile delta; do not blindly use `--force`, upgrade the selected framework/runtime or expand this into a security-hardening project. Keep the selected direct versions where no change is needed. Confirm the named advisories are fixed with an actual audit, then verify `npm ci`, `npm run pages:build` and `npm run typecheck`. Record any concrete remaining limitation. This is scoped dependency maintenance in the already approved phase, not a new universal zero-audit criterion.

### Suggestions closed or deferred with reasons

- **Generator negative cases: PASS for three meaningful cases.** Codex copied the unchanged generator and one existing source image into an isolated temporary fixture with the current node_modules available. Missing source, incorrect registry height and width above native resolution each returned exit 1 with the intended message. Project source/registry were untouched. Descriptor/file equality is already checked for all seven real outputs; forcing that assertion's failure would require a mocked transformer. No new framework, generator argument or redundant existsSync loop is required by this review.
- **Historical SPA navigation:** the former temporary fixture was intentionally removed to preserve homepage navigation. Its deletion does not invalidate the recorded test. Recheck the routes in the already-required usable-browser/preview pass; no permanent test page or additional link is requested.
- **Header heights, focus and true mobile interaction:** keep the outstanding real viewport/keyboard/scroll checks. The conflicting w-32/w-48 logo classes and fixed 72px assumptions should be treated together when shared navigation is redesigned in phase 02; no current narrow-viewport result is invented. Source-only claims about orphaned focus need actual interaction evidence before a broader focus rewrite.
- **Media priority:** current exported HTML contains two image preload links for the phone and portrait. The preliminary claim that the plain img wrapper produces no preload is therefore incorrect. Evaluate priority/loading in the agreed Q-015 phase 02 work; no phase 01 loading redesign is required.
- **Duplicate healthcare IDs:** comparison/features/footer-cta appear on both page wrappers and child sections in the existing code. They belong to phase 03 healthcare-body semantics (Q-010), with phase 02 handling shared chrome. Reduced motion and the shared header belong to the already planned phase 02 work.
- **Other minor suggestions:** handler deduplication, replacing sound narrowing assertions, hypothetical /# links, registry validation beyond current invariants, orphan pruning and the stale commented WaitList block are not required to finish this correction. No CSP/GTM/consent work or new test system is added.

Do not repeatedly invoke the unchanged fixed-viewport harness to close NOT_RUN results. Both reports retain their actual evidence limits; the phase remains in_review until the required interactions and authorized preview are verified and the owner accepts it.

## Planning review record and accepted outcome

Initialization and both subsequent planning reports were reviewed on 2 September 2026. The first context response already edited planning documentation; the later `/replan` pass refined it further. These were documentation changes, not authorization to implement or publish. Codex inspected the final files rather than accepting either report as the current specification.

The consolidated [phase 01 specification](Phases/01-preview-and-technical-foundation.md) is now the execution source. The original four phases, AC-01–AC-05 and Q-001–Q-006/Q-016 are retained. Phase readiness does not mean implementation acceptance.

Accepted technical decisions:

- Next.js 16, compatible React 19.2/types and TypeScript, a supported Node 24 patch pinned exactly, and a coherent lockfile. Current official Next documentation identifies 16.3.4 as the review candidate. Verify exact package availability/compatibility when installing.
- Keep the static Cloudflare Pages architecture and `out` export, remove the conflicting adapter, and use explicit local Wrangler preview plus build-time Sharp image derivatives.
- Generate image files before every entry point that needs them: `dev`, `build`, `pages:build` and `typecheck`. The hosting command name remains unchanged.
- Use a browser-safe, path-allowlisted image loader with a shared source registry; preserve unknown/SVG sources and test actual generated image URLs on every route.
- Repair all affected Button/link callers, closed-menu focus behavior, duplicate anchors and native/JS scrolling offsets; preserve offer, destinations and current design.
- Verify active config resolution before cleanup, repair the font/CSS issues, remove the historical forced reload, and keep source/local-browser/remote evidence separate.

Corrections to the two reports and their plan edits:

| Finding | Corrected treatment |
| --- | --- |
| Earlier handoff still described `prebuild`, while /replan changed only some instructions | One command contract in the phase file; exact CI entry tested without stale generated assets |
| Broken image URLs were explicitly allowed in development | Development prepares the same required assets; no knowingly broken dev workflow |
| Loader selected a derivative below the requested width | Select a sufficient candidate when available, capped at the available maximum; verify browser choice and native dimensions |
| Generated ignored manifest could be missing during type checking | Shared source registry and preparation order must work from a clean checkout; client loader imports no Node-only modules |
| “Objednat” was said to link externally | Source already uses `#contact-section`; preserve it and fix only the handler/default-scroll interaction |
| Fixed CSS scroll-padding was described as repairing JS offsets | Native and programmatic scrolling must both use the actual header clearance |
| No build run, yet “current build already fails” and Cloudflare runtime success were stated as executed facts | Configuration diagnosis and provider support are distinguished from actual build/preview evidence |
| Mandatory new ESLint gate was claimed to restore existing lint coverage | No prior lint setup is established in this baseline. Keep agreed build/type/behavior checks; no new lint migration in phase 01 |
| Q-003 used `PARTIAL` and allowed a time-pressure fallback | Q evidence uses PASS/FAIL/NOT_RUN/BLOCKED. PARTIAL is an overall report outcome; do not weaken the planned final image pipeline |
| Cleanup introduced owner/GTM inspection and separate-commit prerequisites | Keep Quanda in phase 01, preserve GTM and video, and proceed locally without a new container audit or commit permission |
| Broad claims about “noreferrer breaks GTM”, “inert is the only method” and indefinite version support | Use the chosen behavior with bounded explanations; do not turn recommendations into universal technical facts |
| “Commit once” / reset instructions conflicted with permissions | Keep local work uncommitted; no reset, commit, push or deployment is part of this assignment |

Git history associates the forced reload with former Quanda form handling. Commit `98b3df2` still contained a Quanda trigger; it is not proof that this commit removed the form entirely or that current routes have been behaviorally tested. Current source has no form consumer. Test route behavior after removing the workaround and preserve the global script as stated above.

Primary evidence rechecked by Codex: [Next support policy](https://nextjs.org/support-policy), [Next 16 upgrade guide](https://nextjs.org/docs/app/guides/upgrading/version-16), [image loader configuration](https://nextjs.org/docs/app/api-reference/config/next-config-js/images), [npm script lifecycle](https://docs.npmjs.com/cli/v11/using-npm/scripts/), [Cloudflare build image](https://developers.cloudflare.com/pages/configuration/build-image/) and [Node release status](https://nodejs.org/en/about/previous-releases). Provider documentation supports the plan; it does not substitute for the forthcoming implementation checks.

## Cloudflare settings and preview instructions

The initial owner screenshot showed project `coalshift`, repository `coalmarketing/coalshift`, production branch **`cloudflare-deploy`**, automatic production deployments enabled, build command **`npm run pages:build`**, output **`out`**, and an empty root-directory setting. Build system is version 3 and build cache is disabled.

The owner supplied new Preview screenshots on 2 September 2026 at about 18:17. They confirm the same build settings, empty variables/secrets and production branch. The open Branch control editor initially contained both `*` and `redesign` as includes. Codex instructed removing only the `*` beside `redesign`, retaining the separate Build watch paths wildcard, leaving exclusions empty and then saving. The owner's subsequent "Hotovo" confirms completion of that correction and save. These are screenshot observations plus owner confirmation, not a fresh API read. No preview hostname or deployment has yet been verified.

Configuration procedure (completed by the owner; retained for the publication handoff):

1. Open Cloudflare → Workers & Pages → `coalshift` → Settings → Branch control. Use the current equivalent location if the dashboard labels have changed.
2. Keep the production branch `cloudflare-deploy` and its current deployment setting.
3. Set preview branch controls to **Custom branches** and include the exact branch `redesign`. For a new rule, leave exclusions empty. Preserve existing rules for other development branches and verify no exclusion matches `redesign`; do not silently erase existing exclusions. The owner confirms the resulting Preview settings before the first push.
4. Ensure the Preview build settings use `npm run pages:build`, `out`, and the repository root. Phase 01 makes `pages:build` prepare images and then run `next build`, producing `out` without renaming the script. Node is pinned by `.nvmrc` to `24.20.0`; if Preview has a `NODE_VERSION` override, align it with that pin. Keep the production environment settings unchanged. This has not yet been tested on a Coalshift preview build.
5. After the separately authorized first push to `redesign`, inspect that branch's deployment in Deployments. Record the actual preview URL, source commit and build result here. Do not substitute the production URL for preview evidence.
6. Verify the preview's `X-Robots-Tag: noindex` behavior and inspect links/canonicals. Search exclusion is not access protection; leave access policy under the owner's control.

Do not invent a working preview address. Cloudflare supplies a deployment URL and branch alias after the branch has been deployed. Do not merge into production, switch the production branch or run the existing deploy script to obtain a preview.

Provider guidance checked on 2 September 2026: [branch build controls](https://developers.cloudflare.com/pages/configuration/branch-build-controls/), [preview deployments](https://developers.cloudflare.com/pages/configuration/preview-deployments/), [build image and Node version selection](https://developers.cloudflare.com/pages/configuration/build-image/), [static Next.js on Pages](https://developers.cloudflare.com/pages/framework-guides/nextjs/deploy-a-static-nextjs-site/). Branch, preview and build-image guidance was reopened during the R4/R5 assessment.

### First publication procedure — completed under owner authorization

The first-publication procedure below is retained as history. The owner supplied the scoped publication prompt; Claude committed and pushed 8f1db89, producing preview 6011b67f. The consumed permission does not authorize further pushes. Current work and permissions are in the handoff above.

- Destination: existing remote `origin` (`https://github.com/coalmarketing/coalshift.git`), local `redesign` to remote `redesign`. Proposed commit title: `Prepare Coalshift technical foundation for preview`.
- Commit scope: the 21 modified tracked files and two tracked deletions listed in the current handoff; the five new implementation/configuration files; generated `AGENTS.md`, `CLAUDE.md`, and `docs/` including the source revision PDF. Inspect the staged diff and exact file list before committing. Preserve both owner portrait inputs untracked for their phase 02 integration. Exclude generated `out/`, `.next/`, image derivatives and local Wrangler state.
- After that permission and the branch/settings checks, the intended push is `git push --set-upstream origin redesign:refs/heads/redesign`. This should trigger the configured Pages preview build. It is not a production merge or the repository's direct deploy command. No force push is part of the proposal.
- Record the new commit SHA and Cloudflare's actual deployment URL/build log before checking all six routes, image responses and rendering, `X-Robots-Tag: noindex`, preserved integrations and the outstanding real mobile/keyboard/scroll/viewport behavior. Reuse existing evidence where unaffected. If browser automation is still unavailable, use owner-assisted checks on the actual preview and keep unperformed checks explicitly NOT_RUN.
- Return an English Preview Verification Report and update the current handoff/evidence. Keep phase 01 `in_review` until the required checks pass and the owner accepts it. No subsequent phase or production publication follows automatically.

## Approved plan changes

- 2 September 2026 — Initial four-phase baseline generated after explicit approval. Free trial finalized at 14 days across the entire site.
- Production evidence resolved the starting branch: use the Cloudflare production baseline, not the stale local master. The already absent Netlify file does not need deletion.
- Owner decisions supersede the PDF's cookie item and obsolete contact identity; Calendly is deferred to 04, with portraits supplied for 02.
- During generation — Both contact portraits became available in `public/img/`; the documentation now names the actual PNG files. No image transformation or website integration has been performed yet.
- 2 September 2026 — First phase 01 planning report received with version/build/image/config/navigation proposals; subsequently assessed and corrected as recorded above.
- 2 September 2026 — /replan report received after five reported review passes. Codex retained the supported fixes and corrected inaccurate or conflicting execution details; the consolidated phase 01 specification supersedes those proposals.

- 2 September 2026 — Codex completed planning assessment, corrected only phase 01/plan documentation, and marked phase 01 ready for local implementation. Phase structure, acceptance criteria, quality IDs and publication permissions remain unchanged.
- 2 September 2026 — Phase 01 implemented locally by Claude Code and marked `in_review`. Next.js 15.1.7 → `16.3.4`, React 19 → `19.2.8`, TypeScript `5.9.3`, Node pinned `24.20.0`; `@cloudflare/next-on-pages` removed, `wrangler` + `sharp` added; `pages:build` = `images:generate && next build` → `out/`. Sharp/WebP build-time image pipeline replaces the failing `/_next/image` dependency. Shared `Button` link/action split, all affected callers and the nested-anchor cases fixed, `inert` closed mobile menu with `aria-expanded`, shared header-height scroll offset. `refreshed=true` reload workaround removed from `/registrace` and `/wait-list`. Duplicate `tailwind.config.ts` / `postcss.config.mjs` and the broken Inter-italic / `CustomFont` / Geist references removed. GTM, Quanda and the `/zdravotnici` video preserved. All work uncommitted; no publication. No change to phase structure, acceptance criteria, quality IDs, commercial terms or permissions.
- 2 September 2026 — Codex review found two local defects (R1 responsive-image descriptors, R2 modifier-click interception) and set Q-003/Q-004 to local FAIL.
- 2 September 2026 — Corrections R1–R3 applied by Claude Code: registry-driven `ResponsiveImage` with truthful `srcset`, `images.unoptimized` global; ordinary-activation guards on every fragment handler; focused-browser re-verification within the harness's fixed 1728 px viewport. Q-003 and Q-004 → PASS (local); harness-blocked interactive checks stay `NOT_RUN`. Q-001/Q-002/Q-005/Q-006/Q-016 unchanged (Q-002/Q-005 PASS, others PASS local / remote NOT_RUN). Phase 01 stays `in_review`, all work uncommitted; no publication. No change to phase structure, acceptance criteria, quality IDs, commercial terms or permissions.

- 2 September 2026 — Codex assessed the R1–R3 correction report, passed a fresh static build/typecheck and focused HTTP/decoded-image checks, reconciled README/current evidence and prepared the first full implementation `/recheck`. Required mobile/keyboard/scroll verification remains NOT_RUN; no acceptance or publication.

- 2 September 2026 — Both implementation-review reports assessed. Codex verified three generator failure cases and confirmed seven transitive development-dependency audit findings. R4/R5 are the bounded remaining local correction assignment; optional refactors/tests were not adopted, and actual mobile/preview gates remain open.
- 2 September 2026 — Corrections R4/R5 applied by Claude Code. R4: `isPlainActivation` in `app/lib/smoothScroll.ts` now also reads the activating anchor's `currentTarget.target` and intercepts only absent/empty or `_self`; `_blank`/`_parent`/`_top`/named targets fall through. Applied through the shared guard used by every fragment handler and the Header home-logo handler (`Header.tsx`/`Footer.tsx` param types adjusted). Guard matrix verified by focused direct checks (12 cases) plus the earlier browser synthetic-event run; no live caller supplies a non-self target, so this is latent hardening. R5: `npm audit fix` (no `--force`) updated 10 transitive dev-dependency nodes within the existing ranges — brace-expansion 2.0.1→2.1.4, browserslist 4.24.4→4.28.8 (+ electron-to-chromium/node-releases/update-browserslist-db), glob 10.4.5→10.5.0, minimatch 9.0.5→9.0.9, picomatch 2.3.1→2.3.2, postcss-selector-parser 6.1.2→6.1.4, yaml 2.7.0→2.9.0. `package.json` and all selected direct versions unchanged. `npm ci`, `npm run pages:build`, `npm run typecheck` exit 0; `npm audit` reports 0 vulnerabilities. Remaining: `glob@10.5.0` prints a maintainer deprecation notice (support-policy message, not a vulnerability; moving to glob 11 needs a `sucrase`/Tailwind update, out of scope). Phase 01 stays `in_review`, all work uncommitted; no commit, push, deploy or Cloudflare change. No change to phase structure, acceptance criteria, quality IDs, commercial terms or permissions.
- 2 September 2026 — Codex completed the focused R4/R5 assessment: actual exported guard checks, fresh static build/typecheck and current audit all passed (0 vulnerabilities). Direct versions remain as selected, remote production is still at the baseline and remote redesign does not yet exist. Current handoff now prepares the first preview publication, pending owner settings confirmation and commit/push authorization. No application code, dependency, Git ref or Cloudflare setting was changed by Codex; phase remains in_review and the outstanding interaction/remote gates remain open.
- 2 September 2026 — First preview published by Claude Code under explicit authorization. The reviewed phase 01 scope (21 modified tracked files, 2 tracked deletions, 5 new implementation/config files, plus `AGENTS.md`, `CLAUDE.md` and `docs/` incl. the source PDF; the two owner portrait PNGs left untracked; generated `out/`/`.next/`/derivatives/`.wrangler/` excluded) was staged explicitly, `git diff --cached --check` clean, and committed as `8f1db89f2e01dad80eb6678a5cbd0df0187b797a` ("Prepare Coalshift technical foundation for preview", parent `0138dbb`). Pushed with `git push --set-upstream origin redesign:refs/heads/redesign`. Cloudflare Preview deployment `6011b67f-f080-4428-9a64-87d0e0ad8ae6` built and deployed successfully → `https://6011b67f.coalshift.pages.dev` (alias `redesign.coalshift.pages.dev`); Node 24.20.0, `npm ci` 0 vulnerabilities, 7 WebP derivatives, Next 16.3.4, 9/9 static pages. Remote Q-001/Q-002/Q-003/Q-004/Q-005/Q-006/Q-016 verified on that deployment (HTTP 200 + `x-robots-tag: noindex` on all six routes, no `/_next/image`, no `refreshed=true`, correct `srcset` and derivative delivery, guard matrix intercepts only ordinary same-page activation, GTM/Quanda/YouTube preserved). Real mobile-viewport rendering, completed smooth-scroll animation and real keyboard tab-order remain NOT_RUN (environment). `cloudflare-deploy` and `master` unchanged; no merge, force-push, deploy script or Cloudflare settings change. Phase 01 stays `in_review`, overall `PARTIAL`; not accepted. Phase 02 not started. Post-publication documentation edits are uncommitted.
- 2 September 2026 — After inspecting preview 6011b67f, the owner accepted progression to phase 02 and explicitly deferred detailed interaction/rendering checks. Codex independently confirmed remote refs, six route responses/noindex, all 14 image sources and preserved integration markup. Phase 01 is done with documented Q-003/Q-004 deferrals; phase 02 is ready and locally authorized. Deferred checks remain NOT_RUN, assigned to the redesigned UI verification and final handoff; no production permission or shared workflow change is implied.
- 2 September 2026 — **Phase 02 (Homepage and design) implemented locally by Claude Code and marked `in_review`.** Coalios reference read at `f3d727dc32dd9cd04493915f512b3375ef7d0cf4` (read-only; selected assets copied in — 6 WOFF2 faces + inlined coalfamily brand marks — with an OFL note; no sibling build dependency). New shared design in COALsoft blue `#00B5E2`: Tailwind blue scale + family tokens + `4xl` radius, `darkMode:'selector'`, dark-first with a render-blocking theme bootstrap and an accessible persisted toggle (safe on blocked storage), floating rounded header pill + coalfamily bar + responsive `inert` mobile menu, rounded footer panel with `© 2026 coalsoft s.r.o.`, and `CtaButton`/`Section`/`Card`/`FragmentCta` primitives. Homepage rebuilt entirely from `content.md` (hero, AI benefits + phone mockup, practical benefits, capabilities, industries, five-tier canonical pricing with Měsíčně/Ročně switch + `Všechny ceny jsou uvedeny bez DPH.`, FAQ, contacts, closing CTA); consistent vykání and the 14-day trial throughout. Pricing data/contacts/links centralised in `app/lib/`. Martina Adamcová + Šárka Melišová integrated with the exact roles/tel/mailto and new 1080² portraits through the extended Sharp registry (originals preserved); obsolete Miroslav Adamec removed from all visible copy. Contact layout left ready for the phase 04 inline calendar with no placeholder/dead booking link. Homepage title/description/OG set to the canonical values (remaining per-route SEO is phase 03). 8 superseded homepage components deleted; unreferenced legacy TTF fonts removed. GTM `GTM-NQDZKVLF`, the Quanda `<Script>` and the `/zdravotnici` video preserved; the static Cloudflare Pages / `out` architecture and verified scripts unchanged; no framework or dependency change. `npm run typecheck` + `npm run pages:build` exit 0; all six routes still build and load. Verified locally in both themes on `wrangler pages dev out` at the fixed 1728 CSS px / DPR 2 harness viewport: contrast (light primary CTA 5.5:1, dark 8.7:1; body/eyebrow/nav all ≥ 8:1), visible focus, FAQ `aria-expanded`/`hidden` state, pricing period states + canonical values (annual = 11× monthly for Start/Lite/Advanced, Pro annual `Individuálně`), instant theme flip + reload persistence + first-visit dark, fragment guard (plain intercepted → `scrollTo({top: elementTop − headerHeight, behavior:"smooth"})`, modifier/middle not intercepted), `srcset`/derivative delivery (0 `/_next/image`, 0 `refreshed=true`), and preserved GTM/Quanda/video markup on every route. **NOT_RUN (environment):** real 320/390/768/1440 CSS px rendering + touch, completed smooth-scroll animation, real keyboard Tab traversal — owner checklist supplied in the Phase Report. Full healthcare/reference/historical body redesigns and per-route SEO remain phase 03; Calendly is phase 04. Phase 02 stays `in_review`, overall `PARTIAL`; all work uncommitted, no push/deploy/Cloudflare change. No change to phase structure, acceptance criteria, quality IDs, commercial terms or permissions.

- 2 September 2026 — Owner phase-02 feedback and follow-up: preserve the separate six-key-function overview; use a five-tab practical-use browser immediately before monthly-only pricing; merge repetitive benefits/closing CTA, finish exact CTA/card/family-bar details, use lowercase brands, and retire the healthcare page with a 301 to `/#industries`. Codex updated canonical copy, brief, design reference, quality and affected phases, and preserved seven screenshots plus the original report. This supersedes earlier annual pricing, six-content-route and uppercase-brand instructions only in the specified scope. Application changes remain assigned to Claude; phase 02 stays in_review and no publication permission is renewed.

- 2 September 2026 — **Phase-02 corrections C1–C4 implemented locally by Claude Code; phase 02 stays `in_review` (PARTIAL).** C1: homepage recomposed to hero → six-card key-functions overview (`#features`) → five-tab WAI-ARIA faux-browser (`#benefits`, roving focus, manual activation, real phone image in panel 1, canonical practical-use copy) → monthly pricing (`#pricing`, adjacent) → compact audience chips (`#industries`) → FAQ (`#faq`) → contact with integrated trial CTA (`#contact`); `AiBenefits`/`WhyCoalshift`/`ClosingCta` deleted. C2: read the mandatory reference sources (`desktop-screen.njk`, `modules.njk`/`.json`, `screen-url-updater.js`, `input.css` `.btn`/`.border-gradient`, `magic-bento.js`, `benefits.njk`, `header.njk`) + the seven screenshots; `CtaButton` rebuilt as the layered pill (separated outline via box-shadow, diagonal-arrow disc, bottom-up fill + label/arrow swap ≈0.5s, single `aria-label`, `motion-reduce` safe, `:focus-visible` survives `overflow:hidden`); border-only pointer glow via `SpotlightGroup` + `.glow-border` (fine-pointer + motion gated, rAF-throttled, listener cleanup, no ambient element); family strip 26px, parent `coalsoft` persistent blue, one brand hover/focus colour each (generic hover removed), tooltip on hover + `:focus-within`. C3: `pricing.ts`/`Pricing.tsx` reduced to monthly-only — no switch, annual amount, 11-month note or "Domluvit roční tarif"; five bands + exact values + VAT + 14-day trial kept. C4: public brand spelling lowercased (headings, `<title>`/description/OG/siteName, alt, accessible names, retained-page brand tokens); `app/zdravotnici/page.tsx` + six `Healthcare*` components deleted after a reference search; header (desktop + mobile) and footer "Zdravotníci" links removed; `public/_redirects` created with `/zdravotnici` + `/zdravotnici/` → `/#industries` 301 (present in `out/`, both verified 301 on `wrangler pages dev out`); "Zdravotnictví" kept as an audience label; the five content routes (`/`, `/reference`, `/registrace`, `/wait-list`, `/wait-list/thank-you`) still build and serve; healthcare video retired with the page (approved exception). Also moved the `.cta`/`.glow-border` CSS out of `@layer components` so Tailwind's component-layer purge cannot strip the dynamically-composed classes. `npm run typecheck` + `npm run pages:build` exit 0. Verified locally at 1728×854 / DPR 2 in both themes: redirects, five-route + 404, 0 `/_next/image` / 0 `refreshed=true` / 0 "Coalshift"/"miroslav" on `/`, GTM ×3 + Quanda per route, no annual pricing artifacts, tab keyboard/ARIA model, CTA rest+hover, family strip states, contrast (primary CTA 8.7:1). NOT_RUN (environment): real 320/390/768/1440 rendering, real keyboard Tab traversal, completed smooth-scroll animation, live pointer-glow capture — owner checklist in `Phases/02-homepage-and-design.md` and the Correction Report. Uncommitted; no push/deploy/Cloudflare change. Phase structure, ACs, quality IDs, commercial terms and permissions unchanged.

- 2 September 2026 — Second owner phase-02 review: require faithful coalios reuse whenever uncertain, including content composition. Codex independently reproduced 1440px pricing clipping and a pending-Quanda startup failure, preserved the report/screenshots/diagnostic evidence, and prepared D1–D7. Canonical copy/design/quality/agent instructions and current handoff now supersede the former browser label, audience chips, long paid CTA labels and broad visual PASS. Documentation only; no app implementation, fresh build, commit, push or deploy by Codex. Phase 02 remains in_review.

- 2 September 2026 — **Phase 02 corrections D1–D7 implemented locally by Claude Code; phase 02 stays `in_review` (PARTIAL).** D1: `app/layout.tsx` Quanda `<Script strategy>` `beforeInteractive`→`afterInteractive` (URL/GTM/consent unchanged; no other consumer). Re-ran `docs/references/phase02-round2/reproduce-startup.cjs` verbatim with the bundled node against a fresh `out/` — `controlled-ready` **and** `quanda-pending` both switch all five tab panels (0 page errors); previously the pending fixture stuck on panel 1. `FunctionsBrowser` tablist `aria-orientation` now `vertical` ≥1024px / `horizontal` below; hidden panels `tabIndex=-1`. D2: `.cta` (globals.css) ported from `src/assets/css/input.css` + compiled `main.css` — real `outline:1px solid; outline-offset:2px` decorative separation, distinct `:focus-visible` box-shadow ring, fluid label `clamp(.875–1.125rem)`/`clamp(1–1.25rem)` + arrow `clamp(.875–1.25rem)`/`clamp(1–1.5rem)` + 6/8px disc padding, both label copies in one grid cell (align at any width). `pricing.ts`/`Pricing.tsx`: paid button label `Vyzkoušet` + `Prvních 14 dní zdarma.` outside the button; Free `Začít zdarma` with no helper; arrow on all five; fixture 1440px `client==scroll` on every button (was 155 vs 202). D3: `FunctionsBrowser` — centered address pill `https://coalshift.cz/#benefits` (3-col grid), Lekton 18px tab labels + 24px icons, neutral layered frame, `.glow-border` fact cards in a `SpotlightGroup`, register CTA moved **inside** the selected panel, phone image only in panel 1 (no reserved empty column). D4: `Industries` rebuilt as six `.glow-border` icon tiles (outlined `LineIcon` + Lekton 18px label, 3×2 grid, `SectionHeading` hierarchy). D5: `Contact` ported from `tym-item.njk` — round 112px portrait above a separate `.glow-border` card, `ml-6` offset, name Lekton 20px, role coalsoft-700/300 blue, phone+mail icon rows, 2-col grid; portraits `<ResponsiveImage fill object-cover rounded-full>` lazy. D6: `Header` nav `xl:h-20` (80px) / `font-inter text-base` (16px) / desktop nav + mobile-menu switch at `xl` (1280px); `.container-page` → `max-w-[80rem] 2xl:max-w-[96rem]`; family bar `h-11`, marks `size-7` (28px); `Section` padding `pt/pb-12 sm:16 lg:20` + new `tightTop` prop used by `Pricing`; `--header-height` recomputes (142px). D7: `Footer` bottom line = `© 2026 coalsoft s.r.o. Všechna práva vyhrazena.` + `Developed with 💜 by ` `coalmarketing.cz` → `https://coalmarketing.cz/` (from `footer.json`). Also: `.cta`/`.glow-border` kept outside `@layer`; `.glow-border` radius is now a `--gb-radius` var (`--sm`/`--lg` modifiers); unused `app/components/ui/Card.tsx` deleted; `LineIcon` gained industry + `phone` glyphs. Six-function overview, monthly amounts/bands/VAT, 14-day trial, lowercase brands, both portraits/contacts, five routes and the healthcare 301 all preserved; GTM/Quanda/consent and the static Pages architecture unchanged; no dependency or framework change. `npm run typecheck` + `npm run pages:build` exit 0. NOT_RUN (environment): real 320/390/768 rendering + mobile menu, 200% zoom, real keyboard Tab traversal, completed smooth-scroll animation, live pointer-glow capture — owner checklist recorded in the phase file and the Correction Report. Uncommitted; no push/deploy/Cloudflare change. Phase structure, ACs, quality IDs, commercial terms and permissions unchanged.

- 2 September 2026 — Third phase-02 feedback and exact Waulter containers received. Codex prepared E1–E5: retire Quanda, diagnose the exact inert-UI case, expand/unify twelve cards, source border/underline effects, primary login, dynamic decorative paths, two-column footer and /gdpr + /cookies. Clean export/localhost interaction tests pass but do not close the now-withdrawn owner-browser report. New route shells are explicitly within this correction; old hidden routes remain pending clarification. Documentation/evidence only by Codex; no app change, fresh build or publication.

- 2 September 2026 — **Phase 02 corrections E1–E5 implemented locally by Claude Code; phase 02 stays `in_review` (PARTIAL).** E1: `app/layout.tsx` Quanda `<Script>` + its comment removed (no other Quanda reference in `app/`/`public/`/config); GTM `GTM-NQDZKVLF` inline + `<noscript>` and the render-blocking theme bootstrap unchanged; the GTM container itself still loads `https://cdn.waulter.cz/sdk.js` (Waulter preserved, never a Quanda-search casualty). `FunctionsBrowser` key handler is now orientation-aware — Up/Down move roving focus for the vertical rail (≥1024px), Left/Right for the horizontal strip; the unused axis is not `preventDefault`ed so Up/Down still scroll the page in horizontal mode; Home/End and manual Enter/Space unchanged. E2: new shared `app/components/ui/InfoCard.tsx` (bare 48px outlined blue `LineIcon`, Lekton 20px heading, 14px 2-sentence paragraph) now backs both `Capabilities` and `Industries` with the expanded `content.md` copy; the grey rounded icon chip is gone from Industries. `.glow-border` (globals.css) → `p-0.5` (2px), base is the coalios `.border-gradient` layering: pointer radial glow over `linear-gradient(to bottom, neutral.100, neutral.300)` light / `(neutral.700, neutral.900)` dark, `background-origin:border-box`; inner surface `bg-neutral-100` (#f5f5f5) light / `bg-neutral-900` dark; inner radius `calc(var(--gb-radius) - 2px)`. Fact/contact cards inherit the same base. E3: desktop nav `<li>` spans the full 80px nav height with a `before:` 2px underline pinned to `bottom-0`, `scale-x-0` → `hover:` / `has-[a:focus-visible]:` `scale-x-100` over 500ms, `bg-black dark:bg-white`; homepage fragment links are never marked current. Header + mobile menu action hierarchy swapped: `Přihlásit se` = `cta--primary` (login, same tab), `Vyzkoušet na 14 dní zdarma` = `cta--secondary` (register, `_blank`). E4: `FunctionsBrowser` address pill shows `https://coalshift.cz` (greyed) + the selected tab's display path (`/smeny-a-ai`, `/lide-a-pozice`, `/nepritomnosti`, `/exporty`, `/statistiky`) from React tab state — plain text, no hash/route/history; updates on pointer + Enter/Space, not focus alone; pill widened to a `minmax(0,25rem)` centre column. Real page anchor stays `benefits`. E5: `Footer` recomposed into two equal desktop columns (brand + `coalsoft s.r.o.`/both addresses/IČ/DIČ under the logo, then navigation), stacked on mobile, `© 2026 coalsoft s.r.o. Všechna práva vyhrazena.` + `Developed with 💜 by coalmarketing.cz` below both; footer navigation gains **Zásady ochrany osobních údajů (GDPR)** → `/gdpr` and **Podmínky cookies** → `/cookies` as ordinary full-document `<a>` (not `next/link`). New `app/components/LegalPage.tsx` (server component) + `app/gdpr/page.tsx` + `app/cookies/page.tsx`: shared Header/Footer, landing-style intro (real `<h1>`, breadcrumb Domů → title, subtle blue radial wash), the single initially-empty `<div id="waulterGdpr">` / `<div id="waulterCookies">` inside a hand-written `.legal-content` wrapper styling injected headings/paragraphs/lists/tables/links in both themes (no `@tailwindcss/typography`), `suppressHydrationWarning` so React never rewrites injected HTML; per-route title/description + own absolute canonical (`https://coalshift.cz/gdpr` / `/cookies`), not the homepage canonical. `npm run typecheck` + `npm run pages:build` exit 0; `out/` now has 7 content routes + both healthcare 301s. Verified on `wrangler pages dev out` at the fixed 1728 CSS px / DPR 2 harness in both themes: Quanda absent from source/export/DOM, GTM + `cdn.waulter.cz/sdk.js` present, all 5 tab selections sync panel heading **and** address path (pointer + Space), vertical arrow keys move focus without activating and pass Left/Right through, theme toggles both directions + persists, FAQ `aria-expanded` toggles, header/mobile CTA variants swapped, nav `::before` underline 2px at `bottom:0` `scaleX(0)` at rest / 0.5s, footer `695px 695px` columns with company data in the brand column, `/gdpr` + `/cookies` load directly with correct H1/breadcrumb/containers/canonical/title/description and a locally-injected sample renders readably through `.legal-content`. **NOT_RUN (environment):** real 320/390/768/1440 CSS px rendering + horizontal-tab keyboard, live pointer-glow capture (background-tab rAF throttling), real keyboard Tab traversal, completed smooth-scroll animation, real mobile-menu open/close. **Outstanding integration result:** the Waulter SDK loads via GTM but does not inject policy HTML on `127.0.0.1` — real `#waulterGdpr` / `#waulterCookies` population is gated to the production domain / provider configuration and stays unverified until the redesign preview is published; no GTM change or second loader was added. Monthly prices/bands/VAT, 14-day trial, six-function overview, both portraits/contacts, lowercase brands, prior CTA proportions/short pricing labels and the healthcare 301 all preserved; previously retained hidden routes untouched pending "only two subpages" clarification. Static Pages/`out` architecture, framework and dependencies unchanged. Uncommitted; no push/deploy/Cloudflare change. Phase structure, ACs, quality IDs, commercial terms and permissions unchanged.


### Final-polish handoff — 3 September 2026 (Codex)

Prepared F1–F6 and archived the E report plus fourteen owner screenshots. The latest owner follow-up authorizes replacing the browser phone with two numerical cards for every topic, including explicitly labelled provisional examples. Canonical content/design, active handoff, phase checklist and affected quality scope now reflect this change. Final metrics require product-team input before publication. Codex changed documentation/reference archives only; application work, fresh build and interactive verification belong to the next Claude correction. No phase accepted or started, Git publication or Cloudflare change performed.

- 3 September 2026 — **Phase 02 corrections F1–F6 implemented locally by Claude Code; phase 02 stays `in_review` (PARTIAL).** F1: `FunctionsBrowser.tsx` — phone removed; each topic renders two supporting numerical cards from a single `Metric` model (stable ids, `illustrative`/`confirmed` status, "Ilustrační údaj" on all six illustrative cards) ported from coalios `desktop-screen.njk`. Panels are grid-stacked (one DOM copy each; inactive = `inert` + `visibility:hidden`) so the outer frame height, main-card width/height and CTA Y are **identical across all five tabs at 1728 (0 px variance)**. Reference surface hierarchy reproduced with a scoped `.glow-border[data-surface="workspace"|"white"]` override (workspace `#e5e5e5`/`#171717`; main + metric cards `#f5f5f5`+`#d4d4d4` 2px / `#262626`+`#404040`; selected tab `#f5f5f5`+`neutral-300`; fact chips white / `#171717`) — the generic `.glow-border > *` fill no longer flattens them. Responsive: `md:grid-cols-2` then a `2xl` right column (reference behaviour); 320/390/768/1440 rendering NOT_RUN (harness). Orientation-aware arrows, address sync, register CTA unchanged. `mocup-coalshift.png` + registry + derivatives untouched. F2: `Industries` closing line centered below the grid with a decorative outlined blue `LineIcon name="plus"` (new glyph), `font-lekton` ~18–20px, no card/pill/border/CTA, `aria-hidden`, not a tab stop. F3: `Pricing` list wrapped in `SpotlightGroup as="ul"` (new `as` prop, keeps valid `ul > li`); each `<li className="glow-border" data-surface="white">` keeps the white/dark interior; Lite keeps a permanent `ring-2 ring-coalsoft-500` over the shared rim; prices/bands/VAT/labels/helper unchanged. F4: new `app/components/ui/BrandWord.tsx` + `.brand-word` (bottom-anchored translucent `rgba(0,181,226,0.3)` band ≈ lower 70 %, `box-decoration-break: clone`, foreground colour and text metrics unchanged) applied to the five `content.md` placements only (Capabilities H2, Industries H2, Contact CTA heading, Hero lead, Contact intro `coalshiftu`). F5: scoped `.icon-accent` — light `#009AC0` (measured 3.02:1 on `#f5f5f5`, 3.29:1 on the white fact chip), dark unchanged `#1FC3EC`; applied to InfoCard icons, browser fact icons and the F2 plus; `coalsoft-600` / `#00B5E2` / `.eyebrow` untouched. F6: `Header.tsx` ported to the coalios two-state model — scoped React scroll listener toggles at `scrollTop >= 100` (cleanup on unmount, synced on mount with a `mounted` gate so a scroll-restored load snaps without animating). Top: family strip visible, floating `rounded-full` `bg-white/50` pill. Scrolled: `<header>` `translateY(-var(--family-block-h))` (family strip out of view + `inert` so hidden links are not tab stops), nav surface flush `top:0`, full-viewport-width, `rounded-none`, wrapper `bg-white/90` + `shadow-sm`; scroll-back restores the float. ~300 ms surface / 500 ms translate transitions, `motion-reduce:!transition-none`. Desktop action order now **secondary trial → unframed theme toggle → primary `Přihlásit se`** (`ThemeToggle` border removed). `--header-height` = visible nav-bar height (82 px); `--family-block-h` published for the translate/spacer; `smoothScroll.ts` + `scroll-padding-top` retargeted to `[data-nav-bar]` + an 8 px gap; document-flow spacer = `calc(--family-block-h + --header-height)`. `npm run typecheck` + `npm run pages:build` exit 0. Verified on `wrangler pages dev out` at 1728/DPR2 both themes: 0 px tab geometry variance, surface computed colours per the F1 table, metric confirmed/illustrative split, pricing rest state + Lite ring, `.brand-word` × 5 (foreground contrast ≈ 13.9:1), icon-accent contrast, header top/scrolled/return states + `inert` family strip + retargeted anchor offset (Ceník → `#pricing` lands 90 vs nav-bottom 82) + `/gdpr` direct load, no console/hydration errors. **NOT_RUN (environment):** 320/390/768/1440 rendering + horizontal metric-card stacking, real scroll-driven header transition animation (`window.scrollTo` emits no `scroll` in automation — state logic verified via dispatched events), live pointer-glow capture, real mobile menu / 200 % zoom. Six-function overview, monthly prices/bands/VAT, 14-day trial, contacts/portraits, lowercase brands, primary login, two-column footer, exact `/gdpr` + `/cookies` shells, GTM, Quanda-absent and healthcare 301s all preserved. Static Pages/`out` architecture, framework and dependencies unchanged. Uncommitted; no push/deploy/Cloudflare/GTM change. Phase structure, ACs, quality IDs, commercial terms and permissions unchanged.

- 3 September 2026 — **Phase 02 corrections G1–G4 / R1–R6 (navigation + responsiveness) implemented locally by Claude Code; phase 02 stays `in_review` (PARTIAL).** Baseline `68fdbcbd2562f73db91a683315d259c4fca4ef04`; G/R work uncommitted. G1: `Header.tsx` nav `<li>` — `before:origin-left` removed → centre-out underline (coalios `header.njk` default origin); new `.link` class in `globals.css` ported verbatim from coalios `input.css` (`before:-bottom-0.5 before:h-px before:w-full before:scale-x-0 hover:/focus-visible:before:scale-x-100` 500 ms), used by `Footer.tsx` navigation + legal + login links (replaces the instant `hover:underline`); `coalmarketing.cz` credit keeps its permanent underline; reduced motion snaps both. G2: footer heading `Web` → **`Navigace`** (links/destinations/company/credit unchanged). G3: `Vyzkoušet na 14 dní zdarma` removed from every header branch and the mobile menu; xl+ order logo → nav → theme → primary `Přihlásit se`; below xl logo + theme + hamburger only; mobile menu = 4 vertical nav links + `Přihlásit se` as the sole CTA. **Latent bug fixed:** `.cta` (raw CSS outside `@layer`) sets its own `display`, overriding `hidden xl:inline-flex` on the login button (it was visible at every width) — the login `CtaButton` is now wrapped in `<span class="hidden xl:inline-flex">`. G4: nav `<ul>` gap `clamp(0.5rem,-0.21rem+1.79vw,1.5rem)`, item `<Link>` pad `clamp(0.375rem,-0.071rem+1.116vw,1rem)`, group gap `xl:gap-8`, theme↔login `xl:gap-4`, item `h-20`; top/scrolled/return states + 100 px threshold + `[data-nav-bar]` fragment clearance + stable spacer unchanged from F6 and re-verified. R1: `Header.tsx` mobile menu rebuilt to the coalios `header.njk` full-screen pattern — `origin-top` `scale-y-0 → scale-y-100` (`duration-300`, `motion-reduce` safe), full-viewport `bg-white dark:bg-black`, own logo + close (X) row, vertical `<nav><ul>` list, `overflow-y-auto`, `z-50` over the now-`z-40` header which gets `inert` while open; body scroll lock, Escape → close + focus return to the hamburger, `aria-expanded`/`aria-controls`, link activation closes + navigates; focus moves to the close control on open (new `menuCloseRef` + `wasOpenRef` effect). `ThemeToggle` container `size-9` → `size-8` (unframed, matches coalios `~size-7/8`). R2: `FunctionsBrowser.tsx` — removed the `matchMedia` orientation flip and the horizontal `overflow-x-auto` strip; tablist is `flex w-full flex-col` + `aria-orientation="vertical"` at every width (all 5 labels visible, `scrollWidth == clientWidth`); key handler is Up/Down only (Left/Right pass through); top strip `flex justify-center lg:justify-between`, dots `hidden lg:flex`, spacer `hidden lg:block`, address `w-full lg:max-w-[25rem]`; panels `flex-1 lg:grid` — below lg the active panel renders alone at natural height (inactive `hidden` + `inert`, no desktop-sized blank area), at lg+ grid-stacked so the frame height is **0 px across all 5 tabs at 1024/1280/1440/1536/1728** (fixed a `.cta`-style `display` clobber on the panel `block`/`hidden` toggle by switching to `flex` / `hidden lg:flex lg:invisible`); metric `<ul>` `grid-cols-1 md:grid-cols-2 2xl:grid-cols-1 2xl:max-w-sm`. All 10 metric values/badges/notes, the 6 `Ilustrační údaj` labels, display-only paths, F1 surfaces preserved; no phone. R3: `Hero.tsx` centred at every width (was `items-start` <sm); CTA row `items-center` when stacked; `Capabilities.tsx` + `Industries.tsx` grid `lg:grid-cols-3` → `xl:grid-cols-3` (coalios `benefits.njk`). R4: `/gdpr` + `/cookies` re-verified — 200 at 320 & 1440, no overflow, one empty exact-ID container each, real `<h1>`, breadcrumb, footer full-document links; real Waulter population still a separate check. R5/R6: `documentElement.scrollWidth === clientWidth` at **320/390/768/1024/1280/1440/1728 in both themes** on `/`, `/gdpr`, `/cookies`; 200 % zoom (640 px layout) no overflow; reduced-motion rim present; 0 page/hydration errors; every item traced to a named coalios file, no coalios content/identity/routes/groups imported, no build dependency on the reference. `npm run typecheck` + `npm run pages:build` exit 0. **Real-browser evidence:** isolated headless system Chrome 140 via the bundled Playwright runtime (permitted; round-3 archive uses the same), serving fresh `out/`, third-party hosts request-aborted, widths 320–1728 both `colorScheme` values. **NOT_RUN:** header float↔pin transition animation on a real scroll gesture (automation `window.scrollTo` fires no `scroll` event — states verified), live pointer-glow travel, physical touch/device rendering, real Waulter injection. **Minor findings:** (a) at 1280–1440 the browser CTA shifts ≤20 px on the Statistiky tab (metric-note wrapping in the intermediate column layout — frame height 0 px stable, CTA Y 0 px at ≥1536); (b) on a phone a mobile-menu fragment lands the target heading ~50 px below the pinned bar (visible, not covered) vs ~8 px on desktop. Preserved: six-function overview, monthly prices/bands/VAT, 14-day page trial CTAs, contacts/portraits, lowercase brands, two-column footer, exact `/gdpr` + `/cookies` shells, GTM, Quanda-absent, healthcare 301s, hidden retained routes. Uncommitted (HEAD `68fdbcb`); no commit/push/deploy/Cloudflare/GTM change. Phase structure, ACs, quality IDs, commercial terms and permissions unchanged. Ready for the complete Phase 02 `/recheck` against `8f1db89`.

- 3 September 2026 — **Phase 02 correction H1 (FunctionsBrowser compact height) implemented locally by Claude Code; phase 02 stays `in_review` (PARTIAL).** Baseline `68fdbcbd2562f73db91a683315d259c4fca4ef04`; H1 delta = `app/components/home/FunctionsBrowser.tsx` only (+ evidence in `docs/phase-02-browser-height-correction.md`, `quality.md`, `plan.md`, `Phases/02-homepage-and-design.md`). Cause (confirmed): panel wrapper `flex-1 lg:grid` put all five panels in one grid cell; inactive panels were `lg:flex lg:invisible`, so `visibility:hidden` panels still sized one shared row to the tallest panel, then flex-stretch + the main card's `mt-auto` CTA wrapper exposed the surplus as a large empty field. Fix, matching coalios `desktop-screen.njk`: wrapper → `flex-1` (grid-stack removed); inactive panels → native `hidden` attribute + `hidden` class (`display:none` at every width, one semantic DOM copy each, `tabIndex=-1`, not focusable, not in layout, not announced); main card `gap-8` + `mt-auto` wrapper → `gap-10 lg:gap-12` (reference `~gap-10/12`) with the CTA in a `self-start` wrapper in normal flow. `npm run typecheck` + `npm run pages:build` exit 0. Verified — isolated headless system Chrome 140, fresh `out/`, all 5 tabs at 320/390/768/1024/1280/1440/1536/1728 both `colorScheme` values: inactive panels computed `display:none` and `inactiveInLayout` false at every width; facts-to-CTA gap **40 px (<lg) / 48 px (lg+)** on every tab (was hundreds); wide-desktop outer frame **≈593 px at 1536/1728** (was ≈920 px) with no empty field — screenshots of the first `Směny a AI` view (light+dark, 1440+1728) show the void gone; per-tab frame heights are the natural content height (dark 1728: 593/569/569/569/603; dark 1440: 771/747/747/747/767) with active main-column + metric-rail widths **identical across all 5 tabs** and no horizontal overflow at any width; real keyboard @1440 ArrowDown×2 → roving focus without activating, Enter → activate + heading/address sync. No fixed height, `min-height`, measuring clone, absolute overlay, nested scroller or JS reservoir introduced. AC-H1.1–AC-H1.5 PASS (owner + Codex visual sign-off against the reference NOT_RUN; physical-device NOT_RUN). Q-007 FAIL→**PASS**, Q-009 FAIL→**PASS**, Q-010/Q-015 **PASS**. All G1–G4 / R1–R6 and F1–F6 behaviour outside the browser height unchanged. Uncommitted (HEAD `68fdbcb`); no commit/push/deploy/Cloudflare/GTM change. Ready for Codex review of the H1 diff, then the complete Phase 02 `/recheck` against `8f1db89`.

- 3 September 2026 — **Phase 03 `/replan` planning review completed (Claude Code); no implementation, no source edits, HEAD still `68fdbcb`.** Seven parallel review subagents (codebase alignment, technical SEO/metadata, accessibility + design fidelity, content/truthfulness, feasibility/risks/ordering, fresh perspective, project standards) reviewed [Phases/03-pages-and-seo.md](Phases/03-pages-and-seo.md) against the current source, `brief.md`, `content.md`, `design-reference.md` and `quality.md`. No agent found the route/indexing matrix wrong; all seven found the plan under-specified. The phase file was rewritten to resolve nine open decisions and close the gaps: (1) `/registrace` and `/wait-list` render one shared component but need divergent copy/metadata → split; (2) the three dead pages are near-total **copy rewrites** against `content.md` (purge the 1 July 2025 launch, "seznamu čekatelů" framing, promised confirmation email/notifications, the "Přednostní přístup"/"Speciální cena"/"Exkluzivní obsah" launch offers, the uppercase CTA, the commented "Bonus" block) with a full tykání→vykání switch across all retained text, not a wording touch-up; (3) `/reference`, `/registrace`, `/wait-list`, `/wait-list/thank-you` render a bare `<main>` with no `#main` skip target (the shared Header always emits the skip link) → every `<Header/>` route gets `<main id="main" tabIndex={-1} className="outline-none">`; (4) `/reference` nests `banner`+`contentinfo` inside `<main>`, forces `bg-bila` (breaks dark), has no `<h1>`, and its `#00B5E2`/white cards + `text-modra` heading fail WCAG contrast → restructure to the `LegalPage` sibling shape, port the accepted primitives + a native `<details>` disclosure for the long quotes, drop the invented `stars: 5` graphic and the per-card mono logo; (5) removing the global `alternates.canonical` **and** `openGraph`/`twitter` from `layout.tsx` requires new per-route `metadata` exports on `/` and the four other routes simultaneously, plus an `app/lib/seo.ts` single source, `app/sitemap.ts` (exactly four production URLs), `app/robots.ts` (allow-all, no `Disallow`, advertises the sitemap), explicit `trailingSlash: false`, no `title.template` (content.md titles are intentionally non-uniform), and an explicit OG-image decision (no asset exists — build one from brand assets or drop to `summary`); (6) the Cloudflare preview injects `X-Robots-Tag: noindex` on every `*.pages.dev` route, so index/noindex intent is verified **locally** via the rendered `<meta name="robots">` + canonical, and the HTTP-header check is production-only/deferred — Validation is split into LOCAL (PASS/FAIL reachable) and DEPLOYMENT-ONLY (BLOCKED until a separate publish authorization: Q-001, `X-Robots-Tag`, production canonical resolution, real crawler behaviour, real Waulter injection); (7) a shared-file allowlist limits changes to `Footer.tsx` (one added `<li>`), `layout.tsx` (canonical/OG only — GTM/noscript/bootstrap/font-preloads/viewport untouched), `app/page.tsx`, the four route files + their bodies, and the new `seo.ts`/`sitemap.ts`/`robots.ts`; `Header.tsx`, `globals.css`, `CtaButton`, `ThemeToggle`, `smoothScroll.ts`, `Section.tsx` and the `LegalPage` Waulter container stay untouched barring a named regression; (8) the step-E accessibility work is scoped to `/reference` + the three dead pages only (homepage/legal Q-010 was settled in Phase 02); (9) the "phase-01 refresh-workaround" step is verification-only — nothing remains to remove. Evidence discipline: append a dated Phase 03 segment to each Q-row, never overwrite a Phase 02 result or flip a `NOT_RUN`/`BLOCKED` item to `PASS`. Provenance sign-off items for the owner recorded: Michal Uhlíř's "coalfamily" position and the Petr Svoboda quote's HR-system-integration claim. Phase 03 stays `planned` pending Codex's acceptance of this reviewed plan; no `-prompt.md` implementation assignment authored yet.
- 3 September 2026 — **Codex accepted the Phase 03 `/replan` after focused corrections; Phase 03 is `ready`.** Source inspection confirmed the report's route/component, stale-copy, skip-target, landmark, theme, contrast, metadata-inheritance and invented-star findings. The route/indexing matrix and the full `/reference` design port remain unchanged. Codex removed the remaining ambiguity and scope creep: public routes use text-only Open Graph plus Twitter `summary` without an image; `Button.tsx` and legacy Tailwind tokens are preserved; a branded 404 is outside this phase; sitemap entries omit unverifiable `lastModified`, `changeFrequency` and `priority`; `SubpageIntro` preserves semantic markup and observable behaviour without a byte-identical RSC-output requirement. The stray 404 validation row and planning-only completion language were removed. [phase-03-pages-and-seo-prompt.md](phase-03-pages-and-seo-prompt.md) now authorizes only local Phase 03 implementation, its documentation and verification; no commit, push, deployment, external action or Phase 04. No reusable shared-workflow change was needed.

- 3 September 2026 — **Phase 03 implemented locally by Claude Code; phase 03 → `in_review` (COMPLETED_FOR_REVIEW).** Baseline `redesign` @ `68fdbcb` + accepted uncommitted Phase 02 tree; Phase 03 delta kept separate and uncommitted. `/reference` redesigned into the accepted system (shared `SubpageIntro`, real `<h1>`, header/main/footer as siblings, `<main id="main" tabIndex={-1}>`, `.glow-border`+`SpotlightGroup` cards, native `<details>`/`<summary>` "Zobrazit více"/"Skrýt" disclosure, coalios person glyph, three quotes/attributions verbatim, no star rating, no per-card logo, both themes — measured card contrast: name 16.4/17.9, role 5.06/10.5, quote 9.5/12.1, all ≥4.5:1; no page-level horizontal overflow 320–1440). `/registrace`, `/wait-list`, `/wait-list/thank-you` rewritten from `content.md` via a shared `LegacyPage` (own H1/lead/CTA/secondary per route, `/registrace`↔`/wait-list` split, full vykání, `noindex, follow`, no canonical/OG, dark-safe surfaces, `<Footer/>` added to thank-you, `#main` skip target); every stale/false string removed (`1. 7. 2025`, "seznamu čekatelů", promised email/notifications, "Přednostní přístup"/"Speciální cena", uppercase CTA, commented Bonus block, dead `gift-icon`). Route metadata centralised in `app/lib/seo.ts`; `app/layout.tsx` no longer owns a canonical/OG/Twitter (GTM/noscript/theme-bootstrap/font-preloads/viewport untouched); `app/page.tsx` + `/gdpr` + `/cookies` own their metadata; public routes emit self-canonical + text-only Open Graph + Twitter `summary` (no image); the three dead routes emit `noindex, follow` and no canonical. New static `app/sitemap.ts` (exactly `https://coalshift.cz`, `/reference`, `/gdpr`, `/cookies`) and `app/robots.ts` (allow-all, `Sitemap:` line, no `Disallow`); `next.config.ts` pins `trailingSlash: false`; no JSON-LD anywhere. Footer gains one `Reference` → `/reference` `<li>` (centre-out `.link`); still zero internal `href` to `/registrace`/`/wait-list`/`/wait-list/thank-you`/`/zdravotnici`; `/zdravotnici` 301 preserved. `References.tsx`/`WaitList.tsx`/`WaitListRegistration.tsx` deleted; `Button.tsx` and the legacy Tailwind colour tokens left untouched (now unused — deferred cleanup). Homepage canonical/OG/sitemap normalize to bare `https://coalshift.cz` (Next resolver returns the root origin under `trailingSlash: false`; canonical = og:url = sitemap loc, so slash-consistent) — a documented deviation from the plan table's `https://coalshift.cz/`. `npm run typecheck` + `npm run pages:build` exit 0; served-`out/` checks in isolated headless Chrome 152 pass (per-route title/description/robots/canonical/OG; 4-URL sitemap.xml; allow-all robots.txt; no preview hostname; direct load + reload on all three dead routes incl. the two-level thank-you; cross-route `/#contact` from `/wait-list` lands 8 px below the pinned bar; footer order on `/`, `/reference`, `/gdpr`; GTM/dataLayer/noscript/bootstrap/font-preloads intact; `/gdpr` + `/cookies` keep one empty exact-ID Waulter container and their intro/breadcrumb). **DEPLOYMENT-ONLY BLOCKED/NOT_RUN:** Q-001 preview build, `X-Robots-Tag` header intent (Cloudflare injects `noindex` on every `*.pages.dev` route), production canonical resolution, real crawler `noindex`, `_redirects` `Location` fragment on a real preview, real Waulter policy injection. Quality rows Q-004/006/007/008/009/010/011/014/015/016 get an appended dated Phase 03 segment; no Phase 02 result or `NOT_RUN`/`BLOCKED` item was overwritten. Provenance sign-off items still open for the owner: Michal Uhlíř's "coalfamily" role; the Petr Svoboda HR-system-integration sentence. Uncommitted (HEAD `68fdbcb`); no commit/push/deploy/Cloudflare/GTM change. Phase structure, ACs, quality IDs, commercial terms and permissions unchanged.

- 3 September 2026 — **Codex assessed the Phase 03 implementation report and prepared its bounded `/recheck`.** Source/diff and existing-export inspection plus independent typecheck on pinned Node 24.20.0 passed; build/browser results remain attributed to Claude. P03-PRE-01 (four-line quote clamp) and P03-PRE-02 (avatar background tokens) are open design-fidelity deviations. Accepted the consistent bare homepage origin as a project-specific root normalization and clarified preview-vs-production verification. Updated only project documentation; no application edits, commit, push, deploy or shared-workflow change. Phase 03 remains `in_review`; Phase 04 remains planned.

- 3 September 2026 — **Phase 03 `/recheck` assessed and owner scope clarified.** Review found no critical issue and independently reconfirmed AC-03. Jakub limited active development to the accepted homepage and public `/reference`; the three legacy URLs are frozen and judged only by route/indexing state. Codex accepted two reference fixes (three-line clamp and neutral intro copy), ratified the contrasting avatar surface, and rejected optional/speculative or legacy-only findings from correction scope. Phase 03 remains `in_review`; no application, Git, deployment or shared-workflow action by Codex.

- 3 September 2026 — **Owner corrected the final legacy-route behavior before the post-recheck assignment.** “Dead” means unavailable on the website, not directly accessible: keep the three route implementations in the repository for possible reactivation, but configure `/registrace`, `/registrace/`, `/wait-list`, `/wait-list/`, `/wait-list/thank-you` and `/wait-list/thank-you/` as HTTP 301 redirects to `/`. No body redesign, metadata cleanup or deletion is assigned. Codex updated project documentation and the pending Claude Code correction prompt only; no application source, redirect file, Git state or deployment was changed by Codex.

- 3 September 2026 — **Phase 03 post-recheck corrections P03-C1–C3 implemented locally by Claude Code; phase 03 stays `in_review`.** Baseline `redesign` @ `68fdbcb` + accepted uncommitted Phase 02/03 tree; P03-C delta = 3 files. **P03-C1:** `app/components/reference/ReferenceList.tsx` collapsed testimonial quote `line-clamp-4` → locked `line-clamp-3` (single class token; `<details>/<summary>`, `group-open:line-clamp-none`, quote/name/role strings, `bg-neutral-200 dark:bg-neutral-800` avatar, grid, disclosure labels all unchanged). **P03-C2:** `app/reference/page.tsx` intro set exactly to `Přečtěte si zkušenosti s plánováním směn v coalshiftu.` (drops `uživatelů` → no unverified customer-status claim). **P03-C3:** `public/_redirects` — two healthcare rules preserved byte-for-byte, six new rules appended (`/registrace`, `/registrace/`, `/wait-list`, `/wait-list/`, `/wait-list/thank-you`, `/wait-list/thank-you/` → `/ 301`); `out/_redirects` byte-identical to the required 8 lines. Verified on `wrangler pages dev out` (4.128.0): all six legacy forms → 301 to same-origin `/` without following (redirect wins over the still-generated route assets), one hop, no loop; `/` 200; healthcare 301 → `/#industries`; four public routes + `/sitemap.xml` + `/robots.txt` 200; sitemap unchanged (4 public URLs); zero internal `href` to any legacy family. `/reference` at 320/390/768/1024/1280/1440 in both themes on `wrangler` + headless Chrome 152: exactly 3 collapsed visual lines (`-webkit-line-clamp: 3`), complete unchanged quote on expand, pointer + keyboard `<details>` disclosure, visible focus, exact new intro string, no page-level horizontal overflow; three testimonial quotes/names/roles re-confirmed verbatim; neutral-200/800 avatar treatment unchanged. `npm run typecheck` + `npm run pages:build` exit 0. **Frozen source SHA-256 identical before/after** — `app/registrace/page.tsx` `2328da7e…2d51`, `app/wait-list/page.tsx` `27b00163…4ddb`, `app/wait-list/thank-you/page.tsx` `363fa58d…a4c5`, `app/components/legacy/LegacyPage.tsx` `26e86fcc…d2e8`. No change to `seo.ts`/`sitemap.ts`/`robots.ts`, homepage, header, footer, global styles, legal pages or integrations. Q-006/Q-007/Q-010/Q-011/Q-014 evidence appended (dated P03-C segment; no prior result overwritten). P03-PRE-01 resolved by P03-C1; P03-PRE-02 (avatar) ratified as a justified adaptation. Production 301 `Location` headers + no-loop on the deployed domain remain deployment-only BLOCKED, consistent with the other Phase 03 deployment-only items. Uncommitted (HEAD `68fdbcb`); no commit/push/deploy/Cloudflare/GTM change. Phase structure, ACs, quality IDs, commercial terms and permissions unchanged.

- 3 September 2026 — **Codex assessed P03-C1–C3 as ready for owner acceptance.** Source, frozen-file hashes and `git diff --check` match the Correction Report. Independent Wrangler 4.128.0 requests against the fresh export returned exact local results: all six legacy URL forms 301 with `Location: /`, both healthcare forms 301 with `Location: /#industries`, homepage 200. Claude's successful typecheck/build and `/reference` 12-view browser matrix remain separately attributed. The only documentation defect found during review was three unchecked completed redirect checklist items; Codex corrected those evidence markers. No application file was changed by Codex. Phase 03 remains `in_review` until Jakub explicitly accepts it; no further Claude work, full `/recheck`, commit, push, deployment or Phase 04 is authorized.

- 3 September 2026 — **Jakub accepted Phase 03 and authorized the new cleanup/release scope.** Codex prepared Phase 04 (cleanup, optimization, review and production release) and moved Calendly to Phase 05. Read-only Git fetch confirmed both master and cloudflare-deploy are ancestors of redesign; live Wrangler settings inspection was authentication-blocked. Documentation-only preparation; no source implementation, commit, push, merge or deploy.

- 3 September 2026 — **Phase 04 `/replan` planning review completed (Claude Code); no source edits; HEAD `68fdbcb`.** Six parallel review subagents. All six returned ISSUES FOUND / CONCERNS — no blocker, but the phase file was substantially rewritten. Changes: (1) new **A0 safety-checkpoint** stage — the accepted Phase 02+03 tree is committed as its own commit on `redesign` before any cleanup (supersedes the prior "no unrequested baseline commit" line for this release-prep case); cleanup is the second commit; both fast-forward. (2) Mandatory out-of-repo archive (`tar` of the untracked `docs/*.md` + `git bundle`) — `docs/references/` is committed and recoverable, but the ~13 untracked correction/prompt files and the uncommitted current states of `brief`/`content`/`quality`/`plan` are not. (3) Corrected the false "committed history remains available" implication for the size claim: `.git`/clone size is unchanged by this phase; `git filter-repo` history rewrite is not authorized. (4) Frozen-legacy clause: freeze lifted only for the `LegacyPage.tsx` "directly loadable" comment (comment-text only, new SHA-256 recorded); `registrace`/`wait-list`/`thank-you` bodies stay byte-frozen. (5) A2: `content-and-seo.md` carries the verbatim approved Czech copy; `design-system.md` retains the coalios SHA `f3d727dc…` + ported-pattern mapping + source dimensions; extraction→mapping-matrix→"nothing unhomed"→delete gate; ownership tie-breaker table; `AGENTS.md` `nextjs-agent-rules` block kept verbatim; concrete grep-based link check with the offender files named; `quality.md` NOT_RUN/BLOCKED carry-forward enumeration with the "never NOT_RUN→PASS" invariant retained. (6) A3: expanded verified-dead list (also `background`/`background-light`, `fade-up` keyframe, `public/manifest.json` + root `favicon.png`, `public/icons/*.svg`, `waiting-list.svg`, `mocup-coalshift` + `adamec` registry entries); `page.tsx:6` import-order tidy permitted + bounded; Phase 03 `/recheck` "not assigned" list carried forward as still-declined; GTM/Waulter/consent preservation pointer added; `robots.ts`/`seo.ts` stale comments listed. (7) A4: explicit clean-state command (`rm -rf .next out public/img/derivatives` + `.DS_Store` delete), diff-driven regression rather than blind replay, pre-declared NOT_RUN list, Node-pin handling. (8) New **A5** stage-gate producing a reviewed-version fingerprint that stage B re-verifies before committing; explicit `/recheck` scope statement. (9) New **Publication gate** — owner sign-off to publish the six `Ilustrační údaj` figures and the three unverified testimonials on `coalshift.cz`, or Q-011/F1 is `BLOCKED` and stage B stops. (10) Stage B rewritten: exact ref-to-ref fast-forward push commands with server-enforced FF, hard stop when the live production branch is unverifiable, owner-supplied preview/production deployment IDs (missing leg = `PARTIAL`/`BLOCKED`, not `PASS`), concrete rollback procedure, explicit ban on `npm run deploy`/`wrangler pages deploy` and on `git switch`/`merge`/`reset`/`--force` of `master`/`cloudflare-deploy`, edge cache purge. (11) Reports bound to the agent-instructions 8-item structure + "update plan.md before returning"; dated `plan.md` change-log entries required for stage A and stage B and must survive the A2 consolidation; explicit A0–B task checklist added. Phase 04 moved `planned` → `ready` in the phase table pending Codex acceptance of the reviewed plan; no `-prompt.md` stage-A assignment authored yet. Learning: project-specific cleanup/release lifecycle; existing shared maintenance/release guidance covers it; no shared-workflow change.
