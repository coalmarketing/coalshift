Correction Report — Phase 02, D1–D7

Outcome: COMPLETED_FOR_REVIEW. Phase 02 stays in_review, overall PARTIAL (real narrow-viewport / keyboard / completed-animation / live-pointer-glow checks are NOT_RUN — environment-bound). Not accepted. Phase 03 not started.

Baseline / branch: 8f1db89f2e01dad80eb6678a5cbd0df0187b797a on redesign — unchanged (no commit/push/deploy/Cloudflare change). Reference read read-only at coalios f3d727dc32dd9cd04493915f512b3375ef7d0cf4; coalios-manual untouched. All prior phase-02 work, the two portrait originals and Codex's doc/evidence updates preserved.

Environments:
- Fixture (D1 + geometry) — Codex's docs/references/phase02-round2/reproduce-startup.cjs rerun verbatim with the bundled node against a fresh out/: headless system Chrome 152.0.7977.65, 1440×1000 CSS px, DPR 1, local static server. Output in /private/tmp/coalshift-ui-repro/.
- Interactive — Claude-in-Chrome on wrangler pages dev out, actual innerWidth 1728 / innerHeight 854 / DPR 2 (still not shrinkable), both themes.

---

D1 — five tabs work independently of Quanda · PASS

- app/layout.tsx: Quanda <Script> strategy="beforeInteractive" → "afterInteractive" (URL unchanged; source search confirms no other consumer; GTM + <noscript> + consent unchanged).
- Fixture, both modes, 0 page errors:

| mode                                        | 5 tab clicks switch panel              |
|---------------------------------------------|----------------------------------------|
| controlled-ready                            | ✅ all 5                               |
| quanda-pending (embed stalled indefinitely) | ✅ all 5 — previously stuck on panel 1 |

- FunctionsBrowser tablist aria-orientation is now dynamic (vertical ≥1024px / horizontal below). Interactive keyboard checks (scripted KeyboardEvent): roving tabindex 0/−1, Arrow keys move focus without selecting, Enter/Space select + reveal the panel, Home/End jump, Space doesn't scroll the page, hidden panels tabIndex=-1 (no focusable children).

D2 — CTA proportions/outline/animation + pricing labels · PASS (desktop)

- .cta ported from src/assets/css/input.css + compiled main.css (~L1440–1620):
  - Real outline: 1px solid; outline-offset: 2px (hover → #000/#fff) — genuine separation, replacing the transparent outer shadow. Distinct :focus-visible box-shadow: 0 0 0 4px ring on top.
  - Fluid label clamp(.875–1.125rem) / clamp(1–1.25rem); fluid diagonal-arrow clamp(.875–1.25rem) / clamp(1–1.5rem); disc padding 6/8px; pl 20px / pr 8px.
  - Both label copies occupy one grid cell → identical rest/hover position at any width, including full-width buttons.
  - Fill sweep + label-swap + clipped diagonal-arrow-swap, ~0.5s; one aria-label, aria-hidden clones; motion-reduce kills travel.
  - Fixture 1440px: label 17.6px, arrow ~19.4px, outline 1px solid #0091b8 offset 2px.
- Pricing: paid button Vyzkoušet + Prvních 14 dní zdarma. outside the button; Free Začít zdarma, no helper; diagonal arrow on all five; same register URL. Fixture 1440px: every button clientWidth == scrollWidth — no clipping (was 155 vs 202). Contrast: primary CTA 8.71:1 both themes.

D3 — the actual browser component · PASS (desktop)

Centered address pill https://coalshift.cz/#benefits (3-column top bar, verified centered), Lekton 18px desktop tab labels, 24px outlined icons, 16px gap, neutral layered frame + panel, 16px body, CTA inside the selected panel, fact cards carry the .glow-border treatment inside a SpotlightGroup, phone image only in panel 1 (fixture: panel-0 height 494px vs ~371px for the others — no reserved empty column). Below lg the tab rail is a horizontal overflow-x-auto strip. Pricing follows directly (Section tightTop).

D4 — audience icon cards · PASS (desktop)

Industries rebuilt as six .glow-border icon tiles (outlined LineIcon in a rounded chip + Lekton 18px label), 3×2 desktop grid, SectionHeading hierarchy matching the features section; id="industries", intro and closing line kept; not links; healthcare stays a plain tile and the 301 target. Label contrast ≥17:1.

D5 — team/contact pattern · PASS (desktop)

Contact ported from tym-item.njk / our-team.njk: round 112px portrait, ml-6 offset, above a separate .glow-border card; name Lekton 20px bold, role in coalsoft blue (5.5:1 light / 10.5:1 dark), phone + mail icon rows; 2-column grid for Martina Adamcová (Obchod a produkt) + Šárka Melišová (Podpora); exact tel:/mailto: (inspected, not dialled/sent); portraits <ResponsiveImage fill> object-cover rounded-full loading="lazy", originals preserved. Both people render correctly (screenshot).

D6 — header + layout proportions · PASS (desktop)

- Fixture header now 1278 × 80 px, nav font 16px (was 1150 × 56). Nav xl:h-20, font-inter text-base, desktop nav + mobile-menu switch at xl (1280px).
- .container-page widened to max-w-[80rem] / 2xl:max-w-[96rem] (was 72rem).
- Family bar h-11, marks 28px (size-7).
- Section vertical padding cut to pt/pb-12 sm:16 lg:20; new tightTop prop used by Pricing so the browser→pricing gap is compact.
- --header-height recomputes (now 142px); native #hash and the JS smooth-scroll helper share it. Mobile-menu focus-return / Escape / inert / link-activation guards preserved.

D7 — footer credit · PASS

Built HTML: © 2026 coalsoft s.r.o. Všechna práva vyhrazena. and Developed with 💜 by  + coalmarketing.cz → https://coalmarketing.cz/. Purple heart kept; lowercase brands; company data / consent unchanged; no newsletter or coalios footer data.

Regression · PASS

npm run typecheck + npm run pages:build exit 0 (5 content routes, no /zdravotnici). Both /zdravotnici + /zdravotnici/ still 301 → /#industries on Wrangler. Five routes 200, unknown 404. GTM-NQDZKVLF ×3 + Quanda on /. 0 "Coalshift" capital, 0 "miroslav", 0 /_next/image, 0 refreshed=true on /. Six-function overview, monthly amounts/bands/VAT, 14-day trial, both portraits/contacts intact.

Changed files (uncommitted, vs 8f1db89)

- Modified: app/layout.tsx (D1), app/globals.css (.cta port, .glow-border --gb-radius, .container-page width), app/page.tsx, app/components/Header.tsx (D6), app/components/Footer.tsx (D7), tailwind.config.js, app/components/References.tsx / WaitList.tsx (earlier spelling), image-registry.json
- Rewritten (untracked app/components/* dirs): home/{FunctionsBrowser,Pricing,Industries,Contact}.tsx, ui/{CtaButton,Section,SpotlightGroup}.tsx, icons/LineIcon.tsx, lib/pricing.ts
- Deleted: app/components/ui/Card.tsx (unused after D4/D5); (plus the phase-01 homepage/healthcare components and legacy TTF fonts removed earlier this phase)
- Docs: docs/plan.md, docs/quality.md, docs/Phases/02-homepage-and-design.md. Codex's phase-02-fidelity-corrections.md, references/phase02-round2/, brief/content/design-reference and phases 01/03/04 preserved.

Screenshots: …/scratchpad/phase02-shots/ — d-fixture-baseline-1440.png, d-fixture-browser-1440.png, d-fixture-pricing-1440.png (sent), plus d-capabilities-browser-light-1568w.jpg.

NOT_RUN — owner-assisted checklist (environment)

The interactive harness can't set innerWidth below 1728, run behavior:"smooth", or freeze a pointer-following glow to a screenshot. Against light and dark:

1. 320 / 390 / 768 / 1440 px: no horizontal page overflow; nav collapses to the menu ≤1279px; capability/audience tiles + pricing stack; the practical-use tab strip scrolls horizontally with a visible selected state; browser panel prose unclipped, no nested scroll.
2. 200% text zoom on /: reflow, nothing clipped, all five pricing arrows visible.
3. Keyboard-only: Tab into the tablist → Arrow/Enter/Space/Home/End; Tab each family mark → brand colour + tooltip on focus; Tab the CTAs → the focus ring stays distinct from the decorative outline.
4. Pointer: hover CTAs (fill + label/arrow swap) and a capability/audience/fact/contact card (blue border glow); then OS "reduce motion" → stable label/border, no travel, glow off.
5. /zdravotnici typed in the address bar → lands at the visible "Pro koho je coalshift" section below the header.

Git / deployment actions

None. HEAD still 8f1db89; nothing committed, pushed, merged, deployed; no Cloudflare change.

✻ Brewed for 21m 19s