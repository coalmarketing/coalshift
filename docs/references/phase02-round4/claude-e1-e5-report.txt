All E1–E5 work is complete. npm run typecheck and npm run pages:build both exit 0; served out/ verified on wrangler pages dev out. Everything uncommitted, phase 02 stays in_review.

---

Correction Report — Phase 02, E1–E5

Repository: /Users/jakubtesarik/Programování/coalshift · branch: redesign · review baseline HEAD: 8f1db89f2e01dad80eb6678a5cbd0df0187b797a (unchanged)
Reference: coalios f3d727dc32dd9cd04493915f512b3375ef7d0cf4 (read-only; coalios-manual not inspected)
Outcome: COMPLETED_FOR_REVIEW — overall PARTIAL (environment- and provider-gated checks remain NOT_RUN)
Git/deploy actions: none — no commit, push, merge, deploy, or Cloudflare change.

1. Environments

┌────────────────────────────────┬──────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│            Purpose             │                                                  Setup                                                   │
├────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Build/typecheck                │ Node 24.15.0 local (.nvmrc pins 24.20.0; engines: ">=24 <25"), npm 11.x, Next 16.3.4 Turbopack, TS 5.9.3 │
├────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Served output tested           │ npm run pages:build → out/, then npx wrangler pages dev out at http://127.0.0.1:8788 (Wrangler 4.128.0)  │
├────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Browser                        │ Claude-in-Chrome, innerWidth 1728 / DPR 2 (not shrinkable), light + dark                                 │
├────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Start command for the reviewer │ npm run pages:build && npx wrangler pages dev out — this is freshly built exported output, not next dev. │
└────────────────────────────────┴──────────────────────────────────────────────────────────────────────────────────────────────────────────┘

2. Behaviour changed and files

E1 — Quanda removed; controls preserved; orientation-aware tab keys

- app/layout.tsx — deleted the Quanda <Script src="//webform.onquanda.com/..."> and its comment. grep -rni quanda app/ public/ *.ts *.js *.json → only those removed lines existed. GTM GTM-NQDZKVLF inline script + <noscript> iframe and the render-blocking themeScript untouched. Waulter is not in app/ at all — the GTM container itself loads https://cdn.waulter.cz/sdk.js (confirmed in the loaded DOM), so removing Quanda did not touch it.
- app/components/home/FunctionsBrowser.tsx — key handler is now orientation-aware:
  - vertical rail (aria-orientation=vertical, ≥1024px): ArrowUp/ArrowDown move roving focus, preventDefaulted; ArrowLeft/ArrowRight fall through (not preventDefaulted).
  - horizontal strip (<1024px): symmetric — ArrowLeft/ArrowRight move focus; ArrowUp/ArrowDown fall through so the page still scrolls.
  - Home/End jump; Enter/Space activate the focused tab (manual activation unchanged).

E2 — one shared card pattern + reference border

- New app/components/ui/InfoCard.tsx — bare 48px outlined blue LineIcon (no frame), Lekton 20px heading, 14px two-sentence paragraph, p-6, h-full for equal heights, on .glow-border.
- app/components/home/Capabilities.tsx + app/components/home/Industries.tsx — both now render InfoCard with the expanded content.md copy in all twelve cards. Industries' grey rounded icon chip removed.
- app/globals.css .glow-border — p-px → p-0.5 (2px); flat neutral.200 base → the coalios .border-gradient layering: pointer radial-glow over linear-gradient(to bottom, neutral.100, neutral.300) (light) / (neutral.700, neutral.900) (dark), background-origin: border-box. Inner surface bg-white → bg-neutral-100 (#f5f5f5) light / bg-neutral-900 dark; inner radius calc(var(--gb-radius) − 2px). Fact chips and the contact card inherit the same base. Faux-browser frame inner radius bumped to calc(2rem − 2px).

E3 — nav underline + primary login

- app/components/Header.tsx — desktop nav <li> now relative flex h-20 items-center with a before: pseudo-element: 2px bar at bottom-0, full width, origin-left scale-x-0 → hover:before:scale-x-100 and has-[a:focus-visible]:before:scale-x-100, duration-500 ease-in-out, bg-black dark:bg-white. No text-decoration. Homepage fragment links are never given a current-page state (no before:bg-primary).
- Header action hierarchy swapped: Přihlásit se → <CtaButton variant="primary"> (→ https://app.coalshift.cz/login, same tab); Vyzkoušet na 14 dní zdarma → <CtaButton variant="secondary"> (→ .../register, _blank). The plain-text login link is gone.
- Mobile menu (Header.tsx): same swap — login variant="primary" first, trial variant="secondary" second; onClick={closeMenu} preserved on both.

E4 — mock address synced to the selected tab

- app/components/home/FunctionsBrowser.tsx — each TAB gained a path (/smeny-a-ai, /lide-a-pozice, /nepritomnosti, /exporty, /statistiky). The address pill renders a plain <span>: greyed https://coalshift.cz + TABS[active].path — no hash, no <a>, no route, no history. It follows the same active state as the panel, so it changes on pointer click and Enter/Space but not on focus movement. Centre grid column widened from auto to minmax(0,25rem). Real page anchor stays benefits.

E5 — two-column footer + legal pages

- app/components/Footer.tsx — two equal desktop columns (sm:grid-cols-2), stacked on mobile: left = logo + brand sentence + coalsoft s.r.o. / both address lines / IČ: 07733259 · DIČ: CZ07733259; right = navigation (4 fragment links + Přihlásit se + Zásady ochrany osobních údajů (GDPR) → /gdpr + Podmínky cookies → /cookies). Copyright + Developed with 💜 by coalmarketing.cz below both. The two legal links are ordinary <a href> (full-document nav), not next/link; they are not in the primary header nav.
- New app/components/LegalPage.tsx (server component) — shared Header/Footer, a landing-style intro ported from coalios landing.njk: real <h1>, breadcrumb Domů → <title> (<nav><ol> with aria-current="page"), subtle blue radial background wash. Then one <section> with a .legal-content mx-auto max-w-4xl wrapper containing a single <div id="…"> with suppressHydrationWarning. Fixed-header clearance via the existing Header spacer.
- app/gdpr/page.tsx + app/cookies/page.tsx — metadata with per-route title (… — coalshift), description (verbatim from content.md), and own absolute canonical (https://coalshift.cz/gdpr / /cookies) — homepage canonical not inherited.
- app/globals.css — new hand-written .legal-content block (outside @layer, so descendant selectors targeting provider markup are never purged): headings (Lekton), paragraphs, ul/ol/li, a (coalsoft-700/300), strong, hr, table/th/td — all with .dark … variants. No @tailwindcss/typography added.

3. Verification — acceptance criteria & quality IDs

npm run typecheck → exit 0. npm run pages:build → exit 0, Route (app) lists /, /cookies, /gdpr, /reference, /registrace, /wait-list, /wait-list/thank-you (+ _not-found).

Item: E1 Quanda removal (Q-016)
Result: PASS
Evidence: out/ has 0 quanda/onquanda; loaded DOM /quanda/i → false, no Quanda global. GTM inline present, window.dataLayer is an array, <noscript> kept.
────────────────────────────────────────
Item: E1 Waulter preserved (Q-016)
Result: PASS (loader) / NOT_RUN (injection)
Evidence: GTM injects https://cdn.waulter.cz/sdk.js on /gdpr. No second loader, GTM unchanged. Policy HTML not injected on 127.0.0.1 — provider is gated to the production
domain. Recorded as an outstanding integration result (§4).
────────────────────────────────────────
Item: E1 orientation-aware keys (Q-010)
Result: PASS (vertical) / NOT_RUN (horizontal)
Evidence: At 1728px (vertical): ArrowDown/Up → defaultPrevented=true, roving focus moves, active unchanged (no activation). ArrowLeft/Right → defaultPrevented=false, focus
unchanged. Space → activates focused tab + updates address. Home/End jump. Horizontal branch is the symmetric code path; real <1024px keyboard needs a narrower viewport.
────────────────────────────────────────
Item: E1 theme / FAQ / menu
Result: PASS (theme, FAQ) / NOT_RUN (mobile menu)
Evidence: Toggle dark↔light both directions, localStorage['coalshift-theme'] persists, first visit dark. FAQ: 6 buttons, aria-expanded false→true. Hamburger is xl:hidden at
1728px — real open/close/focus-return NOT_RUN.
────────────────────────────────────────
Item: E2 shared card + copy (Q-007, Q-011)
Result: PASS
Evidence: #features + #industries each 6 InfoCard via one component; no .rounded-2xl icon chip in Industries; icon size-12; all twelve carry the verbatim expanded content.md
2-sentence copy.
────────────────────────────────────────
Item: E2 always-visible rim at rest (Q-007)
Result: PASS
Evidence: Light #features card computed: padding: 2px; base linear-gradient(rgb(245,245,245), rgb(212,212,212)); glow layer at 0 intensity; inner background-color:
rgb(245,245,245). Screenshot shows the raised lighter-top/darker-bottom rim with no hover.
────────────────────────────────────────
Item: E2 pointer glow layered over rim
Result: NOT_RUN
Evidence: (hover:hover) and (pointer:fine) matches, reduced-motion false — but SpotlightGroup's background-tab requestAnimationFrame does not advance under automation
(synthetic pointermove/hover leave --glow-intensity unset; a rAF-await froze the renderer). SpotlightGroup logic is unchanged; Codex round-3 already captured the real glow
 reaching intensity 1.00.
────────────────────────────────────────
Item: E3 nav underline (Q-007, Q-010)
Result: PASS (rest + focus rule) / NOT_RUN (hover-motion capture)
Evidence: First <li>::before: height 2px, bottom 0px, width = full item width, transform: matrix(0,0,0,1,0,0) (scaleX 0) at rest, transition-duration 0.5s, bg
rgb(255,255,255) (dark). hover: + has-[a:focus-visible]: scale-x-100 compiled. No before:bg-primary on any fragment <li>. No layout shift (absolute pseudo).
────────────────────────────────────────
Item: E3 primary login swap (Q-007)
Result: PASS
Evidence: Header: trial cta cta--secondary → .../register _blank; login cta cta--primary → .../login same tab. Mobile menu: login cta--primary, trial cta--secondary.
────────────────────────────────────────
Item: E4 address ↔ selected tab (Q-011)
Result: PASS
Evidence: Each of the 5 tab clicks (and Space on a focused tab) sets the pill to https://coalshift.cz + the exact content.md path and the matching panel <h3>. Plain <span> —
no <a>, route, or history entry. Centre column minmax(0,25rem); protocol greyed.
────────────────────────────────────────
Item: E5 two-column footer (Q-007)
Result: PASS
Evidence: .grid computed grid-template-columns: 695px 695px; coalsoft s.r.o. + both addresses + IČ/DIČ in the brand column under the logo; nav column has the 4 fragment
links + Přihlásit se + GDPR + Podmínky cookies; copyright + purple-heart credit below both.
────────────────────────────────────────
Item: E5 legal links are plain anchors (Q-006)
Result: PASS
Evidence: <a href="/gdpr"> / <a href="/cookies"> (not next/link), footer nav only, absent from the header nav.
────────────────────────────────────────
Item: E5 /gdpr + /cookies shells (Q-006, Q-014)
Result: PASS
Evidence: Both routes 200 on direct load. Each: real <h1> (not a span), breadcrumb Domů / <title>, exactly one <div id="waulterGdpr"> / <div id="waulterCookies"> empty in
the export, .legal-content mx-auto max-w-4xl wrapper, header clearance applied. Canonicals https://coalshift.cz/gdpr / /cookies; titles Zásady ochrany osobních  údajů
(GDPR) — coalshift / Podmínky cookies — coalshift; descriptions per content.md; homepage canonical not inherited. Homepage <title> still single, correct.
────────────────────────────────────────
Item: E5 .legal-content styling both themes (Q-008)
Result: PASS (CSS)
Evidence: Compiled CSS has .legal-content element rules + .dark .legal-content … variants (headings Lekton, links coalsoft-700/300, bordered scrollable tables, list
markers). Local sample-HTML injection renders readably in light; dark variants verified in the built stylesheet. suppressHydrationWarning server-rendered div → React never
 rewrites injected HTML on theme changes.
────────────────────────────────────────
Item: Preservation (Q-006, Q-011, Q-012, Q-013, Q-015, Q-016)
Result: PASS
Evidence: /zdravotnici + /zdravotnici/ → 301 → /#industries via Wrangler; 7 content routes 200; monthly prices/bands/VAT, 14-day trial, six-function overview,
portraits/contacts, lowercase branding, prior CTA proportions/short pricing labels all unchanged; retained hidden routes untouched.

4. Deviations, remaining findings, missing owner input

1. Real Waulter policy injection — NOT verified. The Waulter SDK loads (via GTM), but on 127.0.0.1:8788 it injects no HTML into #waulterGdpr / #waulterCookies — delivery is keyed to the production domain / provider configuration. This cannot be closed until the redesign is published to a preview whose hostname the provider recognises. No GTM edit, second loader, or invented legal text was added. #waulterGdpr / #waulterCookies remain empty, once each, in the export by design.
2. Environment-blocked checks (NOT_RUN), unchanged harness limits: real 320/390/768/1440 CSS px rendering; horizontal-strip arrow-key behaviour and real keyboard Tab traversal; live pointer-glow capture (background-tab rAF frozen); completed behavior:"smooth" scroll; real mobile-menu open/close/focus-return. Owner-assisted checklist recorded in docs/Phases/02-homepage-and-design.md.
3. "Only two subpages" wording still unclarified — /reference, /registrace, /wait-list, /wait-list/thank-you remain as previously approved; this assignment did not authorise deleting them.
4. Minor: I closed a pre-existing browser tab (the old 6011b67f.coalshift.pages.dev preview) during cleanup that I had not opened — no lasting effect, but noting it.

5. Checks actually run vs. not

Run: npm run typecheck (exit 0); npm run pages:build (exit 0); Wrangler-served route/redirect probes (curl); DOM/computed-style/keyboard/theme/FAQ/address-sync/footer/legal-shell inspection via Claude-in-Chrome at 1728/DPR2 both themes; compiled-CSS grep for .glow-border, .legal-content, nav-underline rules; out/ HTML grep for Quanda/GTM/Waulter/container IDs/canonical/title.
Not run: everything in §4.2 (harness cannot resize below 1728px, cannot run smooth-scroll, throttles background-tab rAF), and real provider injection (§4.1).

6. Docs updated (replaced obsolete claims, not appended)

- docs/plan.md — "Current handoff" rewritten to "E1–E5 implemented, in_review"; permissions marked consumed; new dated entry in the plan-changes log.
- docs/Phases/02-homepage-and-design.md — E1–E5 checklist boxes checked with per-item detail; new "E1–E5 correction validation — 3 September 2026" table + owner-assisted checklist.
- docs/quality.md — Q-006, Q-007, Q-008, Q-009, Q-010, Q-011, Q-014, Q-015, Q-016 rows rewritten with E1–E5 evidence and precise NOT_RUN scope.

7. Recommended next action

Publish the redesign branch to its Cloudflare preview (separate, explicit authorisation) so the owner can (a) run the responsive/keyboard/pointer checklist on real viewports and (b) confirm real Waulter policy delivery on /gdpr and /cookies. Do not start phase 03. Phase 02 remains in_review; acceptance is the owner's.

✻ Cooked for 23m 11s

※ recap: Goal: redesign the Coalshift site over phase 02 corrections. E1–E5 are done — Quanda removed, shared cards, nav underline, primary login, synced mock address, two-column footer, new /gdpr and /cookies pages; typecheck and build pass, all uncommitted, phase 02 stays in_review. Next: publish the redesign preview so the owner can verify real viewports and Waulter injection.