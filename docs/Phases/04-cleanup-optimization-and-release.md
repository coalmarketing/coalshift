# 04 — Repository cleanup, optimization and production release

## Approved outcome

On 3 September 2026 Jakub accepted Phase 03 and requested this phase before production publication. Deliver a maintainable repository containing the accepted website, concise current documentation and only purposeful source/assets/configuration. Then integrate the reviewed release into the GitHub default branch and the Cloudflare production branch and verify the production deployment. The former Phase 04 becomes [Phase 05](05-calendly-and-integrations.md); Calendly and any further integration choices remain there.

This is a **behavior-preserving** cleanup and release. It does not authorize another redesign, new product claims, a framework migration, new tracking, a new test framework, a broad dependency upgrade, or any Git history rewrite. Use [quality.md](../quality.md), especially its Phase 04 scope update. The sole phase status is in [plan.md](../plan.md).

This file was reviewed by six parallel `/replan` subagents on 3 September 2026; the gates and command blocks below incorporate their findings. `/replan` performs no source edits and does not touch `/Users/jakubtesarik/Programování/[[AI]]`.

## Baseline and release route

Repository: `/Users/jakubtesarik/Programování/coalshift`, remote `https://github.com/coalmarketing/coalshift.git`.

Verified with `git fetch --prune origin` on 3 September 2026 (re-verify at stage A start and again immediately before every stage-B push):

| Ref | SHA | Role |
| --- | --- | --- |
| `redesign`, `origin/redesign` | `68fdbcbd2562f73db91a683315d259c4fca4ef04` | Working/preview branch. **All** accepted Phase 02 corrections and **all** Phase 03 work (incl. P03-C1–C3) are still uncommitted on top of this SHA |
| `master`, `origin/master`, `origin/HEAD` | `2aaef8efea0cb74af2ce632b07deeaf8cbf4045f` | GitHub default branch — ancestor of `redesign` HEAD |
| `origin/cloudflare-deploy` (no local branch) | `0138dbb2073c31e72a9a61c987ee02a8ae9ce4dd` | Recorded Cloudflare production branch — ancestor of `redesign` HEAD, and predates `8f1db89` |

Both default and production tips are ancestors of `redesign` HEAD; integration can fast-forward **today**. The window closes if anyone pushes to `origin/master` or `origin/cloudflare-deploy` first. The intended final state is the same reviewed release SHA on `redesign`, `master` and `cloudflare-deploy`. Pushing only `master` is not evidence of a production deployment.

**Repository weight is not reduced by this phase.** `docs/references/` (55 files, ~66 MB): only `revize-webu-coalshift.pdf` is present at `8f1db89`; the complete 55-file tree first appears at `68fdbcb` and is fully recoverable from there, so deleting it from the working tree loses nothing — but the blobs stay in `.git` (~67 MB) forever, and fast-forwarding `cloudflare-deploy` from `0138dbb` carries that history into the production branch that Cloudflare re-clones on every build. History rewrite (`git filter-repo` + force-push) is **not authorized**. State plainly in `docs/operations.md`: the cleanup is working-tree + `out/` only; `.git`/clone size is unchanged by design.

The existing dashboard record identifies Pages project `coalshift`, production branch `cloudflare-deploy`, build command `npm run pages:build`, output directory `out`, production origin `https://coalshift.cz`. Codex's 3 September read-only `wrangler pages project list` could not authenticate non-interactively — an access limitation, not a configuration failure. `wrangler.toml` sets only `pages_build_output_dir = "out"` and cannot express the production branch (dashboard-only for Git integration). Never put a token in repository files or reports; never echo `wrangler whoami`, `env | grep -i cloudflare` or deployment-list output containing account IDs into a report.

## Execution stages

**A — Cleanup and review:** local work on `redesign`. A0 checkpoint → A1 inventory → A2 docs → A3 code/assets/config → A4 validate → A5 gate → Phase Report → bounded `/recheck`. No commit that leaves `redesign` (the A0 checkpoint is the only commit), no push, no publish during stage A.

**B — Release:** a **separate continuation prompt** naming the reviewed version (by the A5 fingerprint). Jakub has already authorized commit, push, merge and production deployment as the final part of this phase; the continuation sequences that authorization and does not require it to be re-granted. Stage B applies **zero** further cleanup edits — any `/recheck` finding is fixed as a stage-A iteration, not in B. Phase 05 never starts automatically.

## A0 — Safety checkpoint (do this first)

The entire accepted Phase 02+03 result is uncommitted; `68fdbcb` predates every Phase 02 correction and all of Phase 03. Stage A then deletes ~30 tracked files + ~13 untracked docs and rewrites 8 documents into the same uncommitted pile. A botched delete or migration would have **no `git reset` target**. Therefore:

- [ ] **Commit the reviewed pre-cleanup tree as its own checkpoint on `redesign`, before any cleanup edit.** Include the accepted Phase 02 corrections (F/G/R/H1/tooltip/I1–I8), Phase 03 (SEO/`/reference`/legacy 301s/P03-C1–C3) and the current Phase 04/05 planning documents needed to execute this assignment. Exclude untracked historical prompt/correction documents scheduled for deletion; back those up temporarily under the next bullet instead. Use reviewed `git status` and explicit path adds (never blind `git add -A`). Verify nothing under `out/`, `.next/`, `.wrangler/`, `public/img/derivatives/`, `.dev.vars` is staged. Do **not** push. This supersedes the earlier "do not make an unrequested baseline commit" instruction — it is a release-prep checkpoint of already-accepted/reviewed work, and it gives cleanup `/recheck` a clean diff base and a recovery target. Cleanup becomes the second commit in stage B.
- [ ] Before deleting untracked historical documents, create one **temporary operational backup** under `/private/tmp/coalshift-phase04-safety-<timestamp>/` containing only the untracked files scheduled for deletion, with a manifest and hashes. The A0 checkpoint commit protects tracked changes and existing Git history already protects `docs/references/`; a second permanent repository bundle is unnecessary. Keep the temporary backup only through stage-A review and release recovery, then remove it after Phase 04 is accepted.
- [ ] Add `.dev.vars` to `.gitignore` (currently only `.env*` and `.wrangler/` are covered).

## A1 — Inventory and boundaries

- [ ] Record actual branch / HEAD (the A0 checkpoint SHA) / staged-unstaged-untracked state and a source→classification list: **active** / **intentionally retained** / **replaceable by concise documentation** / **removable with a stated usage check**. Usage analysis must include imports, data-driven asset references (`image-registry.json`, `ResponsiveImage` `src`), CSS `url()`, `public/*.webmanifest` / `manifest.json`, App-Router file conventions (`app/favicon.ico`, `app/sitemap.ts`, `app/robots.ts`), metadata icon links and Tailwind `content` globs. A grep with no direct import is not enough to delete a dynamically-referenced asset.
- [ ] **Frozen legacy source.** Phase 03 P03-C hash-froze four files. Phase 04 lifts that freeze **only** for inaccurate comment text in `app/components/legacy/LegacyPage.tsx` (its docstring says "They stay directly loadable" — now false; all six URL forms 301 at the edge). The edit is comment-text only — no markup, no class dedup, no component rename. The three route bodies stay **byte-frozen**:
  - `app/registrace/page.tsx` — `2328da7e6ea3c9b33004543f31ca7cded1a03a610ca4c8cc26fb687e3c2a2d51`
  - `app/wait-list/page.tsx` — `27b00163d0043fa028bf8ec87b49c97e4ec90866b5dba66e788d9d3e60d04ddb`
  - `app/wait-list/thank-you/page.tsx` — `363fa58d2cb3c54a16dfe7543210328af6cd2af0def41f664ba154915eb7a4c5`
  After editing `LegacyPage.tsx`, `git diff` it to confirm only comment lines changed, then record its **new** SHA-256 as the baseline in `docs/operations.md` and the stage-A report.
- [ ] Preserve: the four public routes (`/`, `/reference`, `/gdpr`, `/cookies`), all six legacy 301s to `/`, both `/zdravotnici` 301s to `/#industries`, the four-URL sitemap, current content/offer/prices/contacts, image quality, responsive behavior, accessibility behavior, GTM `GTM-NQDZKVLF`, the Waulter loader + `waulterGdpr`/`waulterCookies` containers + `suppressHydrationWarning`, the owner-managed cookie system, and Quanda's absence. Do not add a second consent loader, change GTM, or run a cookie audit.

## A2 — Consolidate documentation

**Gate: extract before delete.** In order, no skipping:

1. [ ] Write the complete target-document set below.
2. [ ] Produce a **source→target mapping matrix**: every file slated for deletion → the exact target sections that now carry its live decisions / facts / unresolved limitations. Include it in the stage-A report (this is the "consolidation/deletion summary").
3. [ ] Confirm nothing is unhomed — every current decision, commercial fact, SEO/redirect rule, accessibility requirement, coalios provenance value and open limitation appears in a target doc.
4. [ ] Only then delete the superseded files.

Target documentation structure and **ownership tie-breakers** for cross-cutting topics:

| File | Single purpose | Owns (tie-breakers) |
| --- | --- | --- |
| `README.md` | What the repo is, local setup, current commands, documentation map | — (points to the docs below) |
| `AGENTS.md`, `CLAUDE.md`, `docs/agent-instructions.md` | Short entry points + execution/preservation rules; no copied phase history | Execution contract, git/deploy permissions, preservation list |
| `docs/architecture.md` | Static Next.js architecture, component/data ownership, image-pipeline **design**, retained legacy source + reactivation implications | Image registry / Sharp / `ResponsiveImage` design; the "legacy route files exist and are redirect-intercepted" note |
| `docs/design-system.md` | coalshift identity/tokens, **coalios provenance (revision SHA `f3d727dc32dd9cd04493915f512b3375ef7d0cf4`), the ported-pattern mapping table, and the numeric "source dimensions to preserve" values**, primitives, themes, motion, header/browser/responsive + accessibility rules | Everything design; the coalios pin + "recheck if the reference changes" baseline |
| `docs/content-and-seo.md` | Concise current content rules and source map: voice, lowercase-brand and trial-wording rules; current commercial/contact facts; the ten numerical-card values with `illustrative`/`confirmed` status; the §79 break/rest wording rationale; testimonial attribution/provenance caveat; metadata title/description table; route/redirect matrix with **exact `Location` values** for all eight 301 rules; and exact source-file pointers for rendered Czech copy | Content/SEO operating rules and source map; rendered application data/components remain the canonical copy source; redirect rules + Location values; container IDs + shell rules |
| `docs/operations.md` | Verified environments, `.nvmrc` pin, exact build chain + ordering, the Git release procedure (the command blocks from stage B), Cloudflare dashboard settings + verification, GTM/Waulter loader ownership, **the concrete rollback procedure**, the frozen-file SHA baselines | Cloudflare settings; release + rollback; build-chain ordering; how `_redirects` reaches `out` |
| `docs/quality.md` | Q-ID definitions scoped to current behavior; latest meaningful evidence; honest unresolved/deferred items. No accumulated report transcripts | The "population unverified" / NOT_RUN limitations (cross-referenced, not duplicated) |
| `docs/plan.md` | Concise accepted-phase summary, current phase/stage and pending Phase 05 owner inputs | Project status; exact release SHA and deployment IDs belong in the immutable stage-B Release Report because a tracked document cannot reliably identify the commit that contains itself |
| `docs/Phases/04-*.md`, `docs/Phases/05-*.md` | Only the active cleanup/release assignment and the future integration scope | — |

- [ ] **`AGENTS.md`:** rewrite only the human-authored preamble. Keep the `<!-- BEGIN:nextjs-agent-rules -->…<!-- END:nextjs-agent-rules -->` block **verbatim** — it is a framework-managed instruction (regenerated by `next dev`), not phase history.
- [ ] **`quality.md` condensation — no silent status upgrades.** Before condensing, snapshot every non-`PASS` Q-item and every scoped-PASS caveat, and require each to survive verbatim **in status** (condensing prose ≠ upgrading a result; "never turn `NOT_RUN`/`BLOCKED` into `PASS`" is a hard invariant). The carry-forward set that must remain explicit: Q-001/Q-002 production (resolved in stage B), Q-003 real narrow/wide + DPR-2 rendering, Q-004 real mobile pointer/keyboard + completed smooth-scroll, Q-008 injected-Waulter re-theming, Q-009 native/JS fragment landing + browser-native Back scroll restoration on a physical device, Q-010 physical-device keyboard/focus/contrast + a screen-reader pass over the dialog/`inert`/`<details>` semantics, Q-013 real portrait crop at narrow/wide, Q-014 `X-Robots-Tag` / production canonical resolution / real crawler `noindex` / the six legacy-URL 301 `Location` headers, Q-015 CWV/transfer comparison + live pointer-glow, Q-016 real Waulter policy population (cause unverified), Q-017 Calendly, plus the two content limits: **the six `Ilustrační údaj` figures are not measured** and **testimonial provenance is unverified** (Michal Uhlíř role "coalfamily" = in-family voice; Petr Svoboda "Integrace s naším stávajícím HR systémem proběhla hladce" + "Návratnost investice byla téměř okamžitá").
- [ ] **Link + reference integrity.** After deletion run `grep -rnE '\]\([^)]*\.(md|pdf)\)|@docs/' README.md AGENTS.md CLAUDE.md docs/ app/ scripts/` and require **zero** hits resolving to a deleted path. Confirmed touch-points to fix: `AGENTS.md:3` (→ `docs/brief.md`), `docs/agent-instructions.md:5` (→ `brief.md` / `content.md` / `design-reference.md` / phase files), `CLAUDE.md` body prose ("Read the current brief, plan, quality profile") and its `@docs/agent-instructions.md` import (target survives — keep), `docs/plan.md` line 5 + the Phases table (`Phases/01–03` rows become plain text) + every historical handoff section, `docs/quality.md` any Q-row links pointing into `phase-02-*.md` / `phase-03-*.md` (a link naming an old correction round, for example) + the `references/phase02-round4/README.md` mention, `app/lib/contacts.ts` + `app/lib/seo.ts` docstrings pointing at `docs/content.md §…`, `app/components/ResponsiveImage.tsx` JSDoc example.
- [ ] **`README.md` stale facts** (fix during the rewrite, **after** the A3 registry cleanup so counts are right): "two registered raster assets" (registry currently has four; correct only once `mocup-coalshift` + `adamec` entries are removed), "The phone and portrait callers supply their responsive `sizes`" (the browser phone `<img>` was removed during the Phase 02 corrections — portraits only), the Deployment section (now also puts the release on `master` with an explicit procedure → point to `docs/operations.md`).
- [ ] After migration, delete: `docs/brief.md`, `docs/content.md`, `docs/design-reference.md`; `docs/Phases/01-*.md`, `02-*.md`, `03-*.md`; **all** top-level `docs/phase-02-*.md` and `docs/phase-03-*.md` correction + prompt + recheck + replan-prompt files (≈18); and `docs/references/` (55 files). Record the recovery SHAs in `operations.md`: `revize-webu-coalshift.pdf` from `8f1db89`, the complete `docs/references/` tree from `68fdbcb`, the tracked historical docs from the A0 checkpoint, and the eleven never-committed untracked docs from the temporary safety backup only. Keep every license notice / attribution required by retained fonts/icons/logos.
- [ ] Keep the Phase 04 file as the complete active assignment through stage A; update only its checklist/evidence where required. Its reduction to a concise release summary is a **post-acceptance** action. Phase 05's file is already concise — leave it.

## A3 — Clean code, assets and configuration

**Preservation pointer (this bullet-set authorizes removals):** GTM `GTM-NQDZKVLF`, the Waulter loader + containers + `suppressHydrationWarning`, the owner-managed cookie system, Quanda's continued absence, error handling, reduced-motion handling, event-listener cleanup, focus/history behavior, service-contract comments, meaningful type annotations, licensing, and framework-managed instructions (`@type`, the AGENTS.md block, `app/favicon.ico`) are **not** "noise" — do not remove them.

- [ ] **Code.** Remove `app/components/Button.tsx` (0 importers verified — the frozen legacy pages render through `LegacyPage` which imports `CtaButton`, not `Button`). Remove the now-dead Tailwind entries in `tailwind.config.js`: the legacy color tokens (`modra`, `modraHover`, `cerna`, `bila`, `bilaHover`, `sparta`, `lightBlue`), the `background` + `background-light` colors, and the `carousel` **and** `fade-up` keyframes + their `animation` entries — trace each to 0 references across `app/**`, `app/globals.css`, the frozen legacy files and the built `out/` before removing. Keep `coalsoftBrand` (used) and `borderRadius.4xl` (used). Optionally drop the stale `./components/**` entry in the Tailwind `content` glob (no root `components/` dir).
- [ ] **`app/page.tsx`** — move the `export const metadata = metadataFor("/")` line below all `import` statements (it currently sits between imports). This one behavior-preserving tidy is explicitly permitted; no other change to `page.tsx`.
- [ ] **Not assigned** (carried verbatim from the Phase 03 `/recheck` — declined there, still declined): no `app/lib/seo.ts` discriminated-union / type-shape rewrite (the only cast, `route.canonical as string` in `sitemap.ts`, is guarded); no new build-time assertions or tests; no sitemap-count assertion; no DRY refactor of working components; no `<details>` restructure; **nothing** inside the three frozen legacy route bodies (including the repeated `"link font-semibold …"` class).
- [ ] **Assets** — remove source + registry entry + generated derivatives **together**, never a text-search-only delete:
  - `image-registry.json`: remove `mocup-coalshift` (old browser phone — Hero/browser are text-only since the Phase 02 corrections) and `adamec` (old portrait) — 0 callers each. Delete `public/mocup-coalshift.png`, `public/img/adamec.png`, and any `public/img/derivatives/{mocup-coalshift,adamec}-*`. **Keep** `martina-adamcova` + `sarka-melisova` (live via `app/lib/contacts.ts` → `Contact.tsx` → `ResponsiveImage`). Confirm `scripts/generate-image-derivatives.mjs` (throws if a registered source file is missing) and `ResponsiveImage`'s src lookup still work with two entries.
  - `public/` root: `file.svg`, `globe.svg`, `next.svg`, `window.svg` (Next scaffold), `landingImg.jpg` (obsolete hero), and the dead pair `public/manifest.json` + `public/favicon.png` (root — `layout.tsx` links `/favicon/site.webmanifest`; `manifest.json` is the sole reference to `/favicon.png`).
  - `public/icons/*.svg` (admin, bell, calendar, email, heart, phone — icons are inline React SVG in `LineIcon.tsx` / `FamilyIcons.tsx`), `public/illustrations/waiting-list.svg` — 0 references.
  - **Logo assets:** keep `public/favicon/` and the two live `public/logo/coalshift_logo_long-{dark,light}-color.svg` files. Remove the other demonstrably unused `public/logo/` variants, including `public/logo/favicon.png`, after checking metadata, CSS, data registries and generated output. The owner explicitly requested removal of files with no current purpose; do not retain an unreferenced kit or ask for a second confirmation.
- [ ] **Stale comments** (comment-text only; files listed are not frozen except `LegacyPage.tsx` per A1): `app/lib/contacts.ts` ("domluvit roční tarif" — monthly-only since Phase 02), `app/components/home/Contact.tsx` ("The phase-04 inline consultation calendar" → Phase 05 / generalize), `app/robots.ts` ("a crawler must be able to reach them to read their `noindex` meta tag" — now false; the six paths 301 at the edge), `app/lib/seo.ts` (the dead-legacy `noindex` branch description — note it is retained-for-reactivation, not the live mechanism), `app/components/ResponsiveImage.tsx` JSDoc example (`/mocup-coalshift.png` → a surviving key), `LegacyPage.tsx` "directly loadable" (per A1).
- [ ] **Config / deps.** `next.config.ts` keeps `output: "export"` and `trailingSlash: false` (both load-bearing). The `images: { unoptimized: true }` block was removed as verified-unused — `next/image` is imported nowhere in `app/`; logos are plain `<img>`, favicons are `<link>` output, the two portraits use `ResponsiveImage`. `postcss.config.js` and `tsconfig.json` are unchanged. Keep package versions stable (the `next` package stays — it is the framework). Do **not** add a `lint`/`test` script, run a blanket updater or `npm audit fix --force`. A lockfile change is allowed **only** if removing a verified-unused dependency requires it (none currently identified) — explain any such change separately.
- [ ] **Local junk** — `public/.DS_Store` and `public/img/.DS_Store` are gitignored (not committed, not a production risk since Cloudflare builds from the Git tree) but `next` copies them into the local `out/`. `find public -name .DS_Store -delete` before the A4 build.

## A4 — Validate the cleanup

- [ ] **Node:** run on the pinned version — `nvm install 24.20.0 && nvm use`. If unavailable, record the explicit, owner-visible deviation: the local gate ran on the installed 24.x (`>=24 <25` satisfied) and the shipped artifact is Cloudflare's `.nvmrc` `24.20.0` build — not bit-identical. Correct the stale "Verified locally on Node 24.20.0" line in `agent-instructions.md`/`CLAUDE.md` to match reality.
- [ ] **Clean build:** `find public -name .DS_Store -delete` → `rm -rf .next out public/img/derivatives` → `git diff --check` → `npm ci` → `npm run typecheck` → `npm run pages:build` (all exit 0). Assert `find out -name .DS_Store` is empty.
- [ ] **Diff-driven regression, not blind replay.** The UI does not change in cleanup; Phases 02/03 `/recheck` already covered it. Establish which shared surface *actually changed* (`app/globals.css`, `tailwind.config.js`, a shared component, an asset reference) and scope the regression to those + a smoke pass. Reuse the Phase 02/03 recheck evidence for everything unchanged.
  - Served `out/` (`npm run preview` / `wrangler pages dev out`): four public routes direct-load + reload; `/sitemap.xml` = exactly the four production URLs; `/robots.txt` unchanged (allow-all + `Sitemap:`, no `Disallow`); the six legacy 301s + the two healthcare 301s — assert status **and** `Location` **without following**; then check the `/#industries` fragment destination separately. No dead URL in navigation or the sitemap.
  - Homepage + `/reference` at 320 / 390 / 768 / 1440 in both themes, plus 1024 / 1280 / 1728: no horizontal overflow; natural compact browser height; five vertical tabs; header top/scrolled/return + mobile open/closed; centre-out links; CTA animation + focus ring; theme persistence; FAQ; `/reference` `<details>` disclosure (3 collapsed lines, full quote on expand, keyboard + pointer, visible focus); skip-link / anchor focus. Zoom + reduced-motion only where CSS / shared controls changed.
  - Active portraits + fonts + actual requested image variants at narrow/wide + DPR 1/2. Compare exported asset inventory and representative CSS/JS/media bytes before/after under identical build conditions — report **measured** working-tree + `out/` savings. Do **not** invent a Lighthouse/CWV number or add a performance target; `.git` size is unchanged by design (state it).
  - Unchanged: monthly prices, trial/login destinations, contacts, testimonial text + attribution, visible `Ilustrační údaj` labels, per-route metadata/canonical/robots/OG, GTM inline + `dataLayer` + `<noscript>` iframe, theme bootstrap, font preloads, `waulterGdpr`/`waulterCookies` containers.
- [ ] **Pre-declared NOT_RUN** (headless Chrome + `wrangler pages dev out` cannot reach these — neither skip silently nor guess): real physical-device rendering + DPR-2, real Waulter policy injection, production response headers / `X-Robots-Tag`, browser-native Back scroll restoration on a device, any CWV/Lighthouse figure.
- [ ] Documentation acceptance: the source→target mapping matrix shows every deleted file represented; a new maintainer can find commands, architecture, design rules, content source, SEO/redirect rules and release steps without reading a historical report; link check returns zero dangling refs.

## A5 — Stage-A gate and fingerprint

- [ ] Resolve every concrete finding before the report.
- [ ] Emit the **reviewed-version fingerprint** to the stage-A report and to the temporary safety directory: the A0 checkpoint SHA; a full binary diff from that checkpoint; a sorted path + SHA-256 manifest for every untracked file; and SHA-256 hashes of both fingerprint files. Do not mutate the real index to compute the fingerprint and do not use `git add -A`. Stage B step 1 recomputes this and asserts equality before committing explicit reviewed paths.
- [ ] Mark the implementation `in_review` in the `plan.md` table (only Codex records `done`, after Jakub's acceptance). Add a dated `plan.md` "Approved plan changes" entry for stage A that survives the A2 consolidation.
- [ ] **`/recheck` scope statement for the report:** "the Phase 04 cleanup delta layered on the accepted Phase 02/03 tree (the A0 checkpoint). Phase 02/03 changes are accepted and are not re-reviewed. Regression coverage = the diff-driven affected-UI matrix + link/build integrity + the source→target doc mapping. The 66 MB `docs/references/` deletion is verified by target-doc coverage + reachability of the complete tree from `68fdbcb` on `origin` (and `revize-webu-coalshift.pdf` from `8f1db89`), not by re-reading the files."

## Existing publication authorization and content limits

Jakub accepted Phase 03 as the production-ready content/design result and explicitly authorized commit, integration and production deployment at the end of Phase 04. Do not ask him to approve the same content or publication again. Preserve the visible `Ilustrační údaj` labels and record that those figures are illustrative rather than measured. Preserve the accepted testimonial text/attribution and record that independent provenance was not established. Acceptance and publication authorization do not convert either limitation into factual verification.

## B — Commit, integrate and deploy the reviewed version

Stage B is a separate continuation prompt. It performs the release only; it does not re-open cleanup scope.

1. **Verify the reviewed version + record recovery target.** Recompute the A5 fingerprint and assert it matches the stage-A report. Through owner-supplied dashboard data or authenticated read-only tooling, record the **current production deployment ID** serving `0138dbb` (the rollback target) before touching anything.
2. **Re-fetch and re-verify.** `git fetch --prune origin`. Confirm `origin/master` and `origin/cloudflare-deploy` are still ancestors of the release. **Hard precondition:** verify from current authenticated Cloudflare data, or current owner-provided dashboard evidence, that the Pages production branch is `cloudflare-deploy`. This is a factual configuration check, not another publication-permission request. If current evidence is unavailable, stage B stops after step 3 (preview push only) and reports the production push as `BLOCKED` pending that configuration fact. Check `gh api repos/coalmarketing/coalshift/branches/master/protection` (and the production branch); if a branch is protected against direct pushes, report and stop.
3. **Commit + push `redesign`.** Reviewed `git status` → explicit path adds (never `git add -A`; verify nothing under `out/`, `.next/`, `.wrangler/`, `public/img/derivatives/`, `.dev.vars` is staged) → the cleanup commit (a structured message enumerating: docs consolidation; dead-code/token/keyframe removal; asset + registry removal; the `LegacyPage.tsx` comment fix + new hash). After the A4 build, `git status AGENTS.md` — if the Next generator re-touched the `nextjs-agent-rules` block, include it (do not revert). `git push origin redesign`. Obtain from the owner (as `6011b67f-…` was recorded for `8f1db89`) the preview **deployment ID + build status + per-commit URL** for this exact SHA; without it the preview leg of AC-04 is `PARTIAL`/`BLOCKED`, not `PASS`. Run the smoke matrix (step 5's list) against the preview and diff it against the stage-A local `out/` evidence.
4. **Fast-forward `master` and `cloudflare-deploy`.** Ref-to-ref pushes so the server enforces fast-forward and nothing is checked out:
   ```
   git fetch --prune origin
   REL=$(git rev-parse redesign)
   git merge-base --is-ancestor origin/master "$REL"            || { echo "STOP: master diverged"; exit 1; }
   git merge-base --is-ancestor origin/cloudflare-deploy "$REL" || { echo "STOP: cloudflare-deploy diverged"; exit 1; }
   git push origin "$REL:refs/heads/master"
   git push origin "$REL:refs/heads/cloudflare-deploy"   # last deliberate action → triggers production
   ```
   **Forbidden in stage B:** `git switch`/`checkout` of `master`/`cloudflare-deploy`, `git merge` (non-`--ff-only`), `git reset`, `git rebase` of these branches, `git push --force` / `--force-with-lease`, branch deletion, and `npm run deploy` / `wrangler pages deploy` / any direct-upload path (release is exclusively `git push` to the Git-integrated production branch — Cloudflare performs the build on `.nvmrc` `24.20.0`). If a fast-forward is no longer possible after the re-fetch, stop: report the options (rebase `redesign` onto the new `master` then re-`/recheck`; a merge commit; owner decision) and take none without owner direction. Never ship the release to production while `master` has diverged.
5. **Verify production.** Confirm the Cloudflare production deployment identifies the exact release SHA (owner-supplied deployment ID if tooling is unavailable — HTTP 200 + content match is necessary, not sufficient; label HTTP-only evidence `PARTIAL`). Then inspect `https://coalshift.cz`: four public routes; registration/login navigation (no submission); all eight redirect rules — status + `Location`, without following, including the nested `/wait-list/thank-you`; canonical / robots / sitemap; portraits + fonts; theme + navigation; GTM + `dataLayer`; real Waulter policy population in `#waulterGdpr` / `#waulterCookies` (mark "verify after live" — a domain-scoped check, not a gate that can strand the release). Preview `noindex` vs production indexing intent are separate; index **eligibility** is testable, immediate indexing is not a deploy gate. A cache purge is not routine for a Git-integrated Pages release; use one only if production evidence demonstrates stale edge content, and record why.
6. **Handoff + recovery.** Record the production URL, release SHA, deployment ID, every check result and remaining limitations in the immutable Release Report. Do not edit tracked documentation after the reviewed release commit merely to embed runtime IDs; that would create an unreviewed second release SHA. `docs/operations.md` must already contain the generic procedure and lookup steps before the cleanup commit. **Rollback procedure:** primary = Cloudflare dashboard "Rollback to this deployment" on the recorded pre-release deployment ID (no Git change); fallback = `git revert` the release commit and fast-forward-push the revert to all three release branches so they remain coherent. Force-reset of `cloudflare-deploy` to `0138dbb` is forbidden.

## Acceptance criteria and quality coverage

- **AC-01 / Q-005:** every removed code / config / dependency / asset item has a stated usage-based reason; the three frozen route bodies are byte-unchanged and `LegacyPage.tsx` changed by comment text only with a re-recorded hash; the retained legacy source + its shared dependencies remain; no unnecessary architecture change.
- **AC-02 / Q-005, Q-018:** the concise target-document set exists; the source→target mapping matrix shows **no unhomed current decision, fact or limitation**; `content-and-seo.md` carries current rules, facts, limitations and exact source pointers without duplicating every rendered string; `design-system.md` retains the coalios SHA + pattern mapping + source dimensions; every license/attribution survives; the link check returns zero dangling references; the `AGENTS.md` framework block is intact.
- **AC-03 / Q-002–Q-016:** clean `typecheck` + `pages:build` exit 0; the diff-driven affected-UI / route / media / integration regression shows no change from the accepted Phase 02/03 result; pre-declared NOT_RUN items stay distinct from passes; no NOT_RUN/BLOCKED became PASS in the `quality.md` rewrite.
- **AC-04 / Q-001, Q-018:** the reviewed release is committed on `redesign` and pushed; `master` and `cloudflare-deploy` fast-forward to the same SHA; the exact Cloudflare preview and production deployment IDs/URLs are evidenced (or the missing leg is honestly `PARTIAL`/`BLOCKED`).
- **AC-05 / Q-006, Q-014, Q-016, Q-018:** production smoke checks + actual redirect / SEO / provider results are recorded; the concrete rollback target (deployment ID) is captured before release; each unverifiable external service is an actionable limitation, not a silent gap.

Q-017 is deferred to Phase 05. No new CMS, forms, analytics plan, authentication audit, SEO campaign or performance-score target. Existing accessibility / design / content requirements apply as **regression protection**, not an invitation to redesign accepted work.

## Task checklist

- [x] A0 — checkpoint commit `9beb38e2afca1281dc07003a5093ab059632d623` of the reviewed pre-cleanup tree; temporary backup of untracked historical docs under `/private/tmp/coalshift-phase04-safety-20260903T215352/`; `.dev.vars` gitignored
- [x] A1 — inventory + classification; freeze-lift applied to `LegacyPage.tsx` docstring only (new SHA-256 `d374b9a0…cb6b5`); three route bodies re-confirmed byte-frozen; preservation list confirmed
- [x] A2 — target-document set written (4 new + README/AGENTS/CLAUDE/agent-instructions rewritten + quality/plan condensed); source→target mapping matrix in the Phase Report; nothing unhomed; ~30 historical files deleted; link check zero dangling; README carries the post-A3 facts
- [x] A3 — `Button.tsx` + dead Tailwind tokens/keyframes removed with 0-ref traces; verified-unused `images: { unoptimized: true }` block removed from `next.config.ts` (`next/image` unused app-wide); `page.tsx` import tidy; assets + registry entries + derivatives removed together; stale comments corrected; no forbidden edits
- [x] A4 — clean build on the pinned Node; `typecheck` + `pages:build` exit 0; diff-driven regression; measured savings; pre-declared NOT_RUN listed
- [x] A5 — fingerprint emitted; `plan.md` → `in_review` + dated entry; `/recheck` scope statement
- [ ] `/recheck` (bounded, cleanup delta only)
- [ ] B — verify fingerprint; re-fetch + confirm production branch; commit + push `redesign`; preview evidence; FF-push `master` + `cloudflare-deploy`; verify production SHA; smoke; Release Report + rollback target recorded

## Report and stop

Both reports follow the eight-item English structure in [agent-instructions](../agent-instructions.md) §"Reports". Stage A updates `plan.md` before its report. Stage B keeps runtime release identity in its immutable report and makes no post-fingerprint source/documentation edit.

**Stage A — Phase Report:** assignment/outcome; repository/branch/baseline (the A0 checkpoint SHA) + the full accepted Phase 02/03 change set identified separately; the inventory classification + source→target mapping matrix + consolidation/deletion summary; every code/asset/config change with its usage check; measured working-tree + `out/` size differences; each AC / Q result with source vs build vs interactive evidence distinguished and pre-declared NOT_RUN honest; the reviewed-version fingerprint; unresolved factual limitations; Git actions = the single A0 checkpoint commit, no push; recommended next action. Mark `in_review`; stop for `/recheck`.

**Stage B — Release Report:** the reviewed SHA + fingerprint match; every actual branch / commit / push / fast-forward operation with explicit refs; preview + production deployment IDs / URLs (or the honest PARTIAL/BLOCKED leg); the full smoke matrix results; the recorded rollback deployment ID + procedure; remaining limitations (real Waulter population, production `X-Robots-Tag`, device/screen-reader, illustrative-metric measurement, testimonial provenance); coherent final local branch/tracking state. Do not mark the phase accepted for Jakub. Do not start Phase 05.
